# SDD(Specification-Driven Development) 방법론

## 목차

1. [시대적 배경: AI가 코딩 방식을 바꾸다](#1-시대적-배경-ai가-코딩-방식을-바꾸다)
2. [AI 코딩의 현실](#2-ai-코딩의-현실)
   - 2.1 ["그냥 시키면 되지 않나?" — 자유 형식 지시의 한계](#21-그냥-시키면-되지-않나--자유-형식-지시의-한계)
   - 2.2 ["더 많이 알려주면 되지 않나?" — 정보 과부하의 역설](#22-더-많이-알려주면-되지-않나--정보-과부하의-역설)
   - 2.3 [진짜 문제: 커뮤니케이션 구조의 부재](#23-진짜-문제-커뮤니케이션-구조의-부재)
3. [SDD란 무엇인가?](#3-sdd란-무엇인가)
   - 3.1 [SDD의 정의와 등장 배경](#31-sdd의-정의와-등장-배경)
   - 3.2 [기존 개발 방식과 SDD의 차이](#32-기존-개발-방식과-sdd의-차이)
   - 3.3 [SDD의 3가지 구현 레벨](#33-sdd의-3가지-구현-레벨)
   - 3.4 [스펙과 Memory Bank: 두 가지 컨텍스트의 역할](#34-스펙과-memory-bank-두-가지-컨텍스트의-역할)
4. [SDD의 4가지 핵심 원칙](#4-sdd의-4가지-핵심-원칙)
5. [SDD 4단계 워크플로](#5-sdd-4단계-워크플로)
6. [실전 도구 비교](#6-실전-도구-비교)
   - 6.1 [AWS의 Kiro](#61-aws의-kiro)
   - 6.2 [GitHub Spec Kit](#62-github-spec-kit)
   - 6.3 [Tessl Framework](#63-tessl-framework)
   - 6.4 [Böckeler가 던진 핵심 질문들](#64-böckeler가-던진-핵심-질문들)
7. [엔터프라이즈 환경에서 SDD가 중요한 이유](#7-엔터프라이즈-환경에서-sdd가-중요한-이유)
8. [정리](#8-정리)

---

## 1. 시대적 배경: AI가 코딩 방식을 바꾸다

SDD를 이해하려면 먼저 AI 시대에 개발 방식이 어떻게 변화해 왔는지를 살펴볼 필요가 있습니다.

---

### 바이브 코딩(Vibe Coding)의 등장

2025년 2월, OpenAI 공동 창업자이자 전 Tesla AI 총괄 책임자인 **안드레이 카파시(Andrej Karpathy)** 는 소셜미디어(X)에 짧은 글을 올렸습니다.

> *"There's a new kind of coding I call 'vibe coding', where you fully give in to the vibes, embrace exponentials, and forget that the code even exists."*
>
> "나는 이것을 '바이브 코딩'이라고 부른다. 분위기에 완전히 몸을 맡기고, 지수적 성장을 받아들이며, 코드가 존재한다는 사실조차 잊어버리는 것이다."

이 글은 전 세계 개발자 커뮤니티에 즉각적인 반향을 일으켰습니다. 바이브 코딩이란, 개발자가 AI에게 자연어로 원하는 것을 말하면 AI가 코드를 생성해 주는 방식입니다. 개발자는 코드를 한 줄씩 작성하는 대신, **무엇을 만들지(What)** 를 설명하고 AI가 **어떻게 만들지(How)** 를 결정하는 구조입니다.

```
[기존 방식]   개발자 → 직접 코드 작성 → 실행
[바이브 코딩] 개발자 → 자연어 지시 → AI가 코드 생성 → 실행
```

초기에는 주로 개인 프로젝트나 프로토타이핑에 활용되었습니다. 복잡한 기술 지식 없이도 동작하는 소프트웨어를 빠르게 만들 수 있다는 점에서 큰 인기를 끌었습니다. 실제로 Y Combinator의 2025년 동계 배치(Winter 2025)에 참여한 스타트업 중 21%가 전체 코드베이스의 91% 이상을 AI로 생성했다고 알려져 있습니다.

> **💡 핵심 개념**
>
> 바이브 코딩은 AI가 코딩의 진입 장벽을 획기적으로 낮춰준 혁신이었습니다. 비개발자도 소프트웨어를 만들 수 있게 되었고, 개발자들은 반복적인 코드 작성에서 해방되기 시작했습니다.

---

### 바이브 코딩의 한계

그러나 바이브 코딩이 빠르게 확산되면서 한계도 분명하게 드러났습니다.

**문제 1: 품질과 일관성 부족**
AI가 생성한 코드가 당장 동작하더라도, 전체 시스템과의 정합성이나 장기적인 유지보수를 고려하지 않는 경우가 많았습니다. '지금 되면 그만'이라는 접근 방식은 기술 부채를 빠르게 쌓이게 만들었습니다.

**문제 2: 의도와 구현 사이의 간극**
개발자가 AI에게 내린 지시가 모호하면, AI는 합리적이지만 의도와 전혀 다른 코드를 생성합니다. 지시가 불명확할수록 결과물의 예측 가능성이 떨어졌습니다.

**문제 3: 추적 불가능한 코드베이스**
바이브 코딩으로 빠르게 생성된 코드는 왜 이런 구조로 만들어졌는지 근거가 없었습니다. 팀원이 합류하거나 기능을 변경할 때 전체 코드를 다시 파악해야 하는 상황이 자주 발생했습니다.

**문제 4: 프로덕션 수준에 미치지 못하는 품질**
보안, 성능, 규정 준수 등 엔터프라이즈 환경의 요구 사항을 만족시키기 위해서는 단순히 '동작하는 코드' 이상이 필요했습니다.

---

### 에이전틱 엔지니어링(Agentic Engineering)으로의 진화

2025년 말에서 2026년 초, AI 코딩 도구들의 성능이 또 한번 크게 도약했습니다. 카파시는 이 변화를 **"상변이(Phase Transition)"** 에 비유했습니다. 물이 특정 온도에서 순간적으로 얼음이 되듯, AI 에이전트의 코딩 능력이 어느 순간 임계점을 넘어섰다는 것입니다.

2026년 초, 카파시는 바이브 코딩의 후계 개념으로 **에이전틱 엔지니어링(Agentic Engineering)** 을 제시했습니다.

> *"'Agentic' because the new default is that you are not writing the code directly 99% of the time, you are orchestrating agents who do and acting as oversight — 'engineering' to emphasize that there is an art & science and expertise to it."*
>
> "'에이전틱'이라고 부르는 이유는, 이제 개발자가 직접 코드를 작성하는 것이 아니라 99%의 시간을 코딩하는 에이전트를 조율하고 감독하는 데 쓰기 때문이다. '엔지니어링'을 붙인 이유는, 이 과정에 예술과 과학, 그리고 전문성이 있음을 강조하기 위해서이다."

| 구분 | 바이브 코딩 (2025) | 에이전틱 엔지니어링 (2026~) |
|------|-------------------|--------------------------|
| 주요 사용자 | 비개발자, 초보자, 빠른 프로토타이핑 | 전문 개발자, 엔터프라이즈 팀 |
| AI의 역할 | 코드 생성기 | 자율 에이전트 |
| 개발자의 역할 | 지시자 | 조율자(Orchestrator) + 감독자(Oversight) |
| 품질 기준 | 일단 동작하면 성공 | 프로덕션 품질 유지 |
| 핵심 스킬 | 자연어 지시 | 구조 설계 + 에이전트 관리 |

에이전틱 엔지니어링에서 개발자의 핵심 역할은 **코드를 직접 작성하는 것이 아니라, AI 에이전트가 올바른 방향으로 움직이도록 명확한 구조와 지침을 제공하는 것** 입니다.

---

### Software 3.0과 SDD의 연결고리

카파시는 이러한 흐름을 **Software 3.0** 시대로 설명합니다.

```
Software 1.0  →  개발자가 직접 명시적인 코드 작성
Software 2.0  →  데이터와 학습을 통해 모델이 동작 결정 (머신러닝)
Software 3.0  →  자연어 지시로 AI가 소프트웨어 생성 및 운영
```

Software 3.0 시대에 개발자에게 가장 중요한 것은 **"AI가 무엇을 만들어야 하는지를 정확하게 정의하는 능력"** 입니다. AI가 아무리 뛰어나도, 명확한 요구 사항과 구조 없이는 올바른 결과물을 만들어 낼 수 없기 때문입니다.

바로 이 지점에서 **SDD(Specification-Driven Development)** 가 등장합니다.

> **🔑 핵심 메시지**
>
> 바이브 코딩이 AI 시대의 가능성을 열었다면, 에이전틱 엔지니어링은 그 가능성을 프로덕션 수준의 품질로 끌어올리는 방법론입니다. 그리고 SDD는 에이전틱 엔지니어링을 실천하기 위한 구체적인 프레임워크입니다.

---

## 2. AI 코딩의 현실

AI 코딩의 진짜 문제가 무엇인지를 살펴봅니다.

---

### 2.1 "그냥 시키면 되지 않나?" — 자유 형식 지시의 한계

AI가 코드를 생성해 준다는 사실을 처음 접한 사람들의 첫 번째 반응은 단순합니다.

> **"그냥 말로 시키면 되는 거 아닌가?"**

이 직관은 절반은 맞고 절반은 틀립니다. AI는 자연어 지시를 처리할 수 있습니다. 하지만 **자유 형식(Free-form) 지시만으로는 일관된 결과를 보장할 수 없습니다.**

#### 실제로 어떤 일이 벌어지는가

```
개발자: "로그인 기능 만들어 줘."

AI가 가정해야 하는 것들:
├── 어떤 인증 방식? (JWT? 세션? OAuth?)
├── 어떤 데이터베이스? (MySQL? PostgreSQL? MongoDB?)
├── 비밀번호 해싱 방식? (bcrypt? SHA-256?)
├── 실패 시 처리? (잠금? 재시도 제한?)
├── 어떤 프레임워크? (FastAPI? Django? Express?)
└── 어떤 보안 수준? (금융급? 일반 웹?)
```

AI는 이 모든 것을 **스스로 가정(Assumption)** 합니다. 그 가정이 개발자의 의도와 일치할 확률은 낮습니다.

#### 반복적인 수정 루프

자유 형식 지시가 만들어 내는 전형적인 패턴이 있습니다.

```
[1회] "로그인 기능 만들어 줘"
      → AI가 세션 기반으로 구현 (JWT를 원했는데)

[2회] "JWT로 바꿔줘"
      → AI가 refresh token 없이 구현

[3회] "refresh token도 추가해줘"
      → AI가 이전 세션 코드와 충돌하는 새 코드 생성

[4회] "이전 코드와 합쳐줘"
      → AI가 컨텍스트를 잃고 다시 처음부터 시작...
```

이 루프에서 빠져나오려면 평균 5~15회의 추가 대화가 필요합니다. 처음에 5분이면 끝날 것 같았던 작업이 2시간짜리 수정 루프가 됩니다.

#### 컨텍스트 창(Context Window)의 함정

AI는 대화 기록을 유한한 범위(컨텍스트 창)까지만 기억합니다. 대화가 길어질수록 AI는 초기에 내린 지시와 결정을 **점점 잊어버립니다.** 100번째 질문에서 AI가 생성하는 코드는 1번째 질문에서 합의한 설계 원칙을 기억하지 못할 수 있습니다.

```
[짧은 대화]   컨텍스트 내 모든 내용 기억 → 일관된 결과
[긴 대화]     초기 결정사항이 컨텍스트 밖으로 밀려남 → 비일관된 결과
```

> **💡 핵심 교훈**
>
> "그냥 시키면 된다"는 생각은 AI를 **마법처럼** 이해하는 것입니다. AI는 마법이 아니라 입력에 반응하는 시스템입니다. 입력이 모호하면 출력도 모호해집니다.

---

### 2.2 "더 많이 알려주면 되지 않나?" — 정보 과부하의 역설

자유 형식 지시의 실패를 경험한 개발자들은 두 번째 전략을 시도합니다.

> **"그럼 더 자세하게, 더 많은 정보를 주면 되겠네?"**

이것도 반쪽짜리 해결책입니다. **정보의 양이 많다고 해서 AI가 더 잘 이해하는 것은 아닙니다.** 오히려 구조화되지 않은 대량의 정보는 새로운 문제를 만들어 냅니다.

#### "Lost in the Middle" 현상

AI 연구자들이 발견한 흥미로운 패턴이 있습니다. AI는 긴 텍스트를 처리할 때 **처음과 끝 부분을 잘 기억하고, 중간 부분을 상대적으로 잘 처리하지 못하는** 경향이 있습니다. 이를 "Lost in the Middle(중간에서 길을 잃다)" 현상이라고 합니다.

```
긴 프롬프트 구조:
┌─────────────────────────────────────────────┐
│ [앞부분] 잘 처리됨 ✅                         │
│ [중간부분] 중요한 제약사항, 비즈니스 규칙... ⚠️ │
│ [끝부분] 잘 처리됨 ✅                         │
└─────────────────────────────────────────────┘

결과: 중간에 있는 중요한 요구사항이 누락된 코드 생성
```

#### 구조 없는 정보의 문제

1,000줄의 요구사항 문서를 통째로 AI에게 붙여넣는 상황을 생각해 봅시다.

```
나쁜 예: 구조 없는 대량 정보 투입
──────────────────────────────────
"이 시스템은 사용자가 로그인할 수 있어야 하고, 관리자는 별도 권한이
있어야 하며, 비밀번호는 안전해야 하고, API 응답은 빨라야 하고,
모바일에서도 동작해야 하고, 영어와 한국어를 지원해야 하며, GDPR을
준수해야 하고, 결제 기능도 필요하고, 소셜 로그인도 지원하고..."

AI의 처리:
- 어디서 시작해야 하는가?
- 무엇이 핵심이고 무엇이 부가적인가?
- 어떤 순서로 구현해야 하는가?
- 서로 충돌하는 요구사항이 있는가?
→ AI는 자신이 판단하기에 "적당한" 해석을 선택
```

#### 정보 과부하가 만드는 역설

| 상황 | 기대 | 실제 결과 |
|---|---|---|
| 짧은 지시 | "뭔가 빠뜨릴 것 같다" | AI가 스스로 가정하여 일관되지 않은 결과 |
| 긴 비구조화 정보 | "더 잘 알겠지" | AI가 중요도 판단을 못해 핵심 요구사항 누락 |
| 구조화된 명세 | "번거롭다" | AI가 정확하게 이해하고 예측 가능한 결과 생성 |

> **💡 핵심 교훈**
>
> AI에게 필요한 것은 **정보의 양**이 아니라 **정보의 구조**입니다. 100줄의 잘 구조화된 명세가 1,000줄의 비구조화 문서보다 훨씬 효과적입니다.

---

### 2.3 진짜 문제: 커뮤니케이션 구조의 부재

두 가지 실패 사례를 보고 나면, 진짜 문제가 무엇인지 명확해집니다.

> **문제의 본질은 정보의 양도, 지시의 길이도 아닙니다. 커뮤니케이션 구조의 부재입니다.**

#### 인간 간 커뮤니케이션과 AI와의 커뮤니케이션의 차이

숙련된 개발자가 팀 내 다른 개발자에게 작업을 넘길 때는 어떻게 하는지 생각해 봅시다.

```
[인간 간 커뮤니케이션]
시니어 개발자 → 주니어 개발자
  ├── 공유하는 것: 조직 문화, 코딩 컨벤션, 도메인 지식
  ├── 가정하는 것: 기본적인 엔지니어링 판단력
  ├── 참조하는 것: 기존 코드베이스, 이전 결정사항
  └── 소통 방식: 암묵적 지식 + 명시적 지시 혼합
```

```
[AI와의 커뮤니케이션]
개발자 → AI 에이전트
  ├── 공유하는 것: 없음 (각 세션은 새로 시작)
  ├── 가정하는 것: 아무것도 가정하면 안 됨
  ├── 참조하는 것: 해당 대화 내에서 명시적으로 제공된 것만
  └── 소통 방식: 모든 것을 명시적으로 구조화해야 함
```

AI는 조직의 역사, 팀의 암묵적 규칙, 이전 기술 결정의 맥락을 알 수 없습니다. 개발자가 알아서 해주겠지 라고 넘기는 것들을 AI는 직접 가정해야 합니다.

#### 구조가 없으면 생기는 일들

```
커뮤니케이션 구조 부재
        │
        ├── AI가 임의로 기술 스택 선택 → 조직 표준과 불일치
        ├── AI가 임의로 보안 수준 결정 → 규정 준수 실패
        ├── AI가 임의로 아키텍처 패턴 선택 → 기존 시스템과 불일치
        ├── 세션마다 다른 결과 → 일관성 없는 코드베이스
        └── 수정 반복 → 시간과 비용 낭비
```

#### SDD가 제공하는 것: 커뮤니케이션의 공통 언어

SDD는 이 문제를 정면으로 해결합니다. **개발자와 AI 사이에 공통의 언어와 구조를 만드는 것**이 SDD의 핵심입니다.

```
[SDD 적용 후]

커뮤니케이션 구조 확립
        │
        ├── constitution.md → "우리 팀은 이렇게 개발합니다" (공통 규칙)
        ├── spec.md         → "이 기능은 이렇게 동작해야 합니다" (명세)
        ├── plan.md         → "이렇게 구현할 것입니다" (설계)
        └── tasks.md        → "이 순서로 작업합니다" (실행 계획)

AI는 이 구조를 기반으로 예측 가능한 결과를 생성
```

> **🔑 핵심 메시지**
>
> AI 코딩이 실패하는 이유는 AI가 부족해서가 아닙니다. **커뮤니케이션 구조가 없어서**입니다. SDD는 바로 이 커뮤니케이션 구조를 제공하는 방법론입니다.
>
> "좋은 명세는 좋은 코드보다 먼저 존재한다."

---

## 3. SDD란 무엇인가?

---

### 3.1 SDD의 정의와 등장 배경

#### Birgitta Böckeler의 분석

사양 기반 개발(Specification-Driven Development, SDD)을 가장 체계적으로 정리한 인물 중 한 명은 **Birgitta Böckeler** 입니다. 그녀는 전 세계 45개국 1만 명 이상의 기술 전문가 조직인 **Thoughtworks의 Distinguished Engineer(수석 기술자)** 입니다.

Böckeler는 소프트웨어 아키텍처의 세계적 권위자인 **Martin Fowler**가 운영하는 martinfowler.com에 SDD에 관한 분석 글을 게재했습니다. 이 글은 AI 코딩 도구의 확산 속에서 "어떻게 하면 AI와 협업을 체계화할 수 있는가"라는 질문에 답하는 중요한 참고 문헌이 되었습니다.

> **martinfowler.com이란?**
>
> Martin Fowler는 『Refactoring』, 『Patterns of Enterprise Application Architecture』 등의 저서로 유명한 소프트웨어 개발의 선구자입니다. 그의 웹사이트 martinfowler.com은 소프트웨어 개발 방법론에 관한 가장 권위 있는 블로그 중 하나로, Thoughtworks 소속 전문가들이 깊이 있는 기술 글을 발표하는 플랫폼입니다.

Böckeler의 분석은 SDD를 "AI 에이전트가 단순한 코드 생성기를 넘어 실제 개발 파트너로 기능하게 만드는 방법론"으로 정의합니다.

#### SDD의 핵심 정의

SDD의 핵심 아이디어는 다음 한 문장으로 요약됩니다.

> **"코드보다 사양(Specification)을 먼저 정의하라. 사양이 명확하면, AI는 올바른 코드를 만들 수 있다."**

SDD는 소프트웨어를 구축하는 방식에 있어 근본적인 패러다임 전환을 의미합니다. 기존 방식에서 **코드**가 진실의 근원(Source of Truth)이었다면, SDD에서는 **사양(Specification)** 이 진실의 근원이 됩니다.

```
[기존 패러다임]
요구사항(말로) → 설계(머릿속) → 코드(실체) ← 진실의 근원
                                    ↑
                     문서는 코드를 따라가며 구식이 됨

[SDD 패러다임]
사양(문서) ← 진실의 근원 → AI가 코드로 변환
    ↑                              ↓
코드 변경 시 사양 먼저 수정      코드는 사양의 표현
```

이러한 패러다임의 전환은 AI 지원 개발 환경에서 의도(Intent)와 구현(Implementation) 사이의 정합성을 지속적으로 유지할 수 있게 해 줍니다.

---

### 3.2 기존 개발 방식과 SDD의 차이

기존 개발 방식에서는 **코드**가 진실의 근원이었습니다. 사양(Specification)은 코드를 만들기 위한 수단에 불과했고, 구현이 발전하면서 사양은 점점 구식이 되어 갔습니다.

SDD는 이 관계를 역전시킵니다.

| 구분 | 기존 방식 | SDD |
|------|-----------|-----|
| 진실의 근원 | 코드 | 사양 |
| 사양의 역할 | 코드를 만들기 위한 수단 | 핵심 아티팩트(기본 산출물) |
| 코드의 역할 | 최종 결과물 | 사양의 구현체 |
| 유지보수 대상 | 코드 | 사양 + 코드 |
| AI 활용 방식 | 코드 생성 보조 도구 | 사양을 코드로 번역하는 실행자 |
| 변경 시작점 | 코드 수정 | 사양 수정 → 코드 재생성 |
| 문서 상태 | 코드와 점점 괴리됨 | 코드와 항상 동기화 |

---

### 3.3 SDD의 3가지 구현 레벨

Böckeler의 분석에서 특히 주목할 부분은 SDD를 하나의 고정된 방법론이 아니라, **성숙도에 따른 3가지 레벨로 구분**했다는 점입니다. 이 구분은 팀이 자신의 현재 수준을 진단하고 점진적으로 발전할 수 있는 로드맵을 제공합니다.

---

#### Level 1: Spec-First (명세 우선)

```
수준: 입문 단계
핵심 원칙: "코딩 전에 먼저 문서로 정의한다"
```

**Level 1은 "선(先) 명세, 후(後) 코딩"의 원칙**입니다. 코드를 작성하기 전에 무엇을 만들 것인지를 먼저 문서로 정의합니다. 이 수준에서 사양은 **참조 문서(Reference Document)** 의 역할을 합니다.

특징은 다음과 같습니다.

- 기능 개발 전에 요구사항 문서(spec.md)를 먼저 작성
- 사양은 AI와의 대화에서 맥락을 제공하는 용도로 활용
- 코드가 완성되면 사양은 그대로 보관하거나 일부 업데이트

한계는 다음과 같습니다.

- 사양과 코드가 점점 괴리될 수 있음
- 사양의 변경이 코드에 자동으로 반영되지 않음
- "문서는 문서, 코드는 코드"로 분리될 위험

Level 1 적용 예시는 다음과 같습니다.

```markdown
# [Level 1 실천 예시]

작업 시작 전:
  1. spec.md에 기능 요구사항 정의
  2. spec.md를 AI 프롬프트에 포함하여 코드 생성

작업 완료 후:
  - 코드는 완성됨
  - spec.md는 초안 상태로 보관 (업데이트 여부는 선택)
```

> **Level 1은 지금 당장 실천할 수 있는 가장 기본적인 SDD 형태입니다.** 바이브 코딩과의 차이는 "코딩 전에 생각을 문서로 정리한다"는 단순한 습관의 변화에서 시작됩니다.

---

#### Level 2: Spec-Anchored (명세 고정)

```
수준: 중급 단계
핵심 원칙: "사양이 개발의 기준점이다. 코드는 사양을 증명해야 한다"
```

**Level 2는 사양이 단순한 참조 문서를 넘어 개발의 기준점(Anchor)이 되는 수준**입니다. 코드가 변경될 때마다 해당 변경이 사양과 일치하는지를 지속적으로 검증합니다.

특징은 다음과 같습니다.

- 사양이 변경 관리의 기준이 됨
- 코드 리뷰 시 "이 코드가 사양을 올바르게 구현했는가?"를 핵심 질문으로 삼음
- 사양과 코드의 불일치가 발견되면 즉시 해결 (사양이 틀렸다면 사양을 수정, 코드가 틀렸다면 코드를 수정)
- 테스트 케이스가 수용 기준(Acceptance Criteria)과 1:1로 대응

Level 2에서 추가되는 실천은 다음과 같습니다.

```markdown
# [Level 2 추가 실천 예시]

코드 리뷰 체크리스트:
  □ 모든 수용 기준(AC)에 대응하는 코드가 있는가?
  □ 엣지 케이스가 처리되었는가?
  □ 사양에 없는 기능이 무단으로 추가되지 않았는가?

테스트 작성 원칙:
  - 모든 테스트는 사양의 수용 기준에서 도출
  - 테스트 코드에 "# Spec: AC-12-1" 형식으로 참조 표시
```

---

#### Level 3: Spec-as-Source (명세가 소스)

```
수준: 고급 단계
핵심 원칙: "사양이 코드의 소스다. 코드는 사양에서 생성된다"
```

**Level 3는 SDD의 완전한 실현 형태**입니다. 사양 자체가 코드를 생성하는 소스(Source of Truth)가 됩니다. AI는 사양을 입력받아 코드, 테스트, 문서를 모두 생성합니다.

특징은 다음과 같습니다.

- 사양 변경 → AI가 코드 자동 재생성
- 단일 사양에서 여러 아티팩트(코드, 테스트, API 문서, UI 목업) 파생
- 사양과 코드의 불일치가 구조적으로 불가능

Level 3 워크플로는 다음과 같습니다.

```
[Level 3 워크플로]

사양 작성/수정
      ↓
AI 에이전트가 사양을 해석
      ↓
┌─────────────────────────────┐
│  구현 코드         자동 생성 │
│  단위 테스트       자동 생성 │
│  API 문서          자동 생성 │
│  타입 정의         자동 생성 │
└─────────────────────────────┘
      ↓
개발자가 생성 결과 검증
      ↓
사양 정제 또는 승인
```

이 수준에서 GitHub Spec Kit, AWS Kiro 같은 도구들이 실질적인 지원을 제공합니다.

---

#### 3가지 레벨 비교 요약

| 구분 | Level 1: Spec-First | Level 2: Spec-Anchored | Level 3: Spec-as-Source |
|---|---|---|---|
| 핵심 변화 | 코딩 전 문서화 | 코드가 사양을 증명 | 사양이 코드를 생성 |
| 사양의 역할 | 참조 문서 | 기준점 | 소스 |
| 코드-사양 관계 | 느슨하게 연결 | 검증 관계 | 파생 관계 |
| AI 역할 | 코드 생성 보조 | 일관성 검증 | 사양 → 코드 변환 |
| 도입 난이도 | 낮음 | 중간 | 높음 |
| 적용 대상 | 모든 팀 | 체계적인 팀 | 성숙한 팀 |

> **💡 실용적 조언**
>
> 모든 팀이 처음부터 Level 3를 목표로 할 필요는 없습니다. **Level 1부터 시작해서 팀의 역량과 함께 점진적으로 높은 레벨로 이동**하는 것이 현실적입니다.
>
> 중요한 것은 어느 레벨에 있든 **"사양이 코드보다 먼저 존재한다"** 는 원칙을 지키는 것입니다.

---

### 3.4 스펙과 Memory Bank: 두 가지 컨텍스트의 역할

AI와 효과적으로 협업하기 위해서는 두 가지 종류의 컨텍스트를 모두 제공해야 합니다. 이 두 가지가 없으면 AI는 매번 새로운 세션에서 처음부터 시작하는 것과 같습니다.

---

#### 두 가지 컨텍스트

```
AI가 필요로 하는 정보
        │
        ├── [1] 스펙(Spec): "무엇을 만들어야 하는가?"
        │         → 기능별, 작업별로 달라지는 정보
        │
        └── [2] Memory Bank: "어떤 맥락에서 만들어야 하는가?"
                  → 프로젝트 전체에 걸쳐 일관되게 유지되는 정보
```

#### 스펙(Specification)의 역할

스펙은 **특정 기능이나 작업에 대한 구체적인 요구사항**입니다. 새 기능을 개발할 때마다 새롭게 작성됩니다.

스펙의 구성 요소는 다음과 같습니다.

```markdown
# 스펙 예시: 로그인 기능

## 사용자 스토리
사용자로서, 이메일과 비밀번호로 로그인하여 내 계정에 접근하고 싶다.

## 수용 기준 (Acceptance Criteria)
- Given: 올바른 이메일/비밀번호 입력 시
  When: 로그인 버튼 클릭
  Then: 대시보드로 이동하고 JWT 토큰 발급

## 비기능 요구사항
- 응답 시간: 2초 이내
- 실패 횟수: 5회 이상 시 계정 잠금

## 엣지 케이스
- 존재하지 않는 이메일
- 잠긴 계정으로 시도
- 세션 만료 후 재접근
```

스펙의 특성은 다음과 같습니다.

- 기능별로 독립적으로 작성
- 개발 완료 후에도 유지 관리
- 변경 요청 시 코드 전에 스펙부터 수정

#### Memory Bank의 역할

Memory Bank는 **프로젝트 전체에 걸쳐 일관되게 적용되는 맥락 정보**입니다. AI가 새 세션을 시작할 때마다 이 정보를 참조하여 "이 프로젝트의 방식"으로 작업합니다.

Memory Bank의 구성 요소는 다음과 같습니다.

```markdown
# Memory Bank 예시 (constitution.md 또는 GEMINI.md)

## 기술 스택
- Backend: Python 3.11 + FastAPI
- Database: PostgreSQL 15
- ORM: SQLAlchemy
- 인증: JWT (RS256)
- 배포: AWS ECS

## 코딩 컨벤션
- 함수 이름: snake_case
- 클래스 이름: PascalCase
- 주석: 한국어로 작성
- 테스트: pytest, 커버리지 80% 이상

## 아키텍처 결정 사항
- API 설계: RESTful, 버전 관리(/api/v1/)
- 에러 응답: {"code": "ERR_001", "message": "..."}
- 로깅: structlog 사용, JSON 형식

## 보안 원칙
- 비밀번호: bcrypt 해싱
- 개인정보: AES-256 암호화
- SQL Injection: ORM을 통한 파라미터 바인딩만 사용
```

Memory Bank의 특성은 다음과 같습니다.

- 프로젝트 초반에 팀 전체가 합의하여 작성
- 기술적 결정이 내려질 때마다 업데이트
- 모든 AI 세션의 시작점으로 활용

#### 두 컨텍스트가 결합할 때

스펙과 Memory Bank가 함께 제공될 때 AI는 "무엇을"과 "어떻게"를 모두 알게 됩니다.

```
[두 컨텍스트가 없을 때]
AI의 상태: "이 프로젝트가 어떤 프로젝트인지, 어떤 규칙으로 만드는지 모름"
결과:       매번 다른 스타일, 다른 기술 스택, 일관성 없는 코드

[스펙만 있을 때]
AI의 상태: "이 기능이 무엇인지는 알지만, 어떤 방식으로 구현해야 하는지 모름"
결과:       기능은 구현되지만 기존 코드베이스와 스타일 불일치

[Memory Bank만 있을 때]
AI의 상태: "프로젝트 맥락은 알지만, 지금 무엇을 만들어야 하는지 모름"
결과:       프로젝트에 맞는 스타일이지만 엉뚱한 기능 구현

[스펙 + Memory Bank]
AI의 상태: "무엇을 만들어야 하는지, 어떻게 만들어야 하는지 모두 앎"
결과:       의도한 기능을 프로젝트 표준에 맞게 일관성 있게 구현
```

> **🔑 핵심 메시지**
>
> 스펙은 기능을 정의하고, Memory Bank는 맥락을 제공합니다.  
> 두 가지가 결합될 때 AI는 진정한 개발 파트너가 됩니다.
>
> GitHub Spec Kit의 `constitution.md`, Cursor의 `.cursorrules`, Gemini의 `GEMINI.md` 파일이 모두 Memory Bank의 구현 형태입니다.

---

## 4. SDD의 4가지 핵심 원칙

---

### 원칙 1. 기본 아티팩트로서의 사양

사양은 진실의 중심 소스입니다. 코드는 특정 언어와 프레임워크에서 사양을 표현한 결과물이 됩니다. 소프트웨어를 유지보수한다는 것은 단순히 코드를 수정하는 것이 아니라, 사양 자체를 발전시키는 것을 의미합니다.

### 원칙 2. 실행 가능한 사양(Executable Specification)

사양은 작동하는 시스템을 생성할 수 있을 만큼 **정확하고, 완전하며, 명확**해야 합니다. 이 정밀도가 의도와 구현 사이의 간극을 제거합니다.

바이브 코딩의 가장 큰 문제가 바로 이 정밀도의 부재였습니다. "로그인 기능 만들어 줘"라는 지시는 AI에게 너무 많은 것을 가정하게 만듭니다. SDD에서는 이 지시가 사용자 스토리, 수용 기준, 요구 사항, 엣지 케이스로 구체화됩니다.

### 원칙 3. 살아있는 문서(Living Documentation)

- 버그를 수정한다는 것은 잘못된 코드를 만들어 낸 사양을 수정하는 것을 의미합니다.
- 리팩터링한다는 것은 명확성을 위해 사양을 재구성하는 것을 의미합니다.
- 사양은 구현과 항상 동기화된 상태를 유지합니다.

### 원칙 4. AI-인간 협업 구조

AI는 사양을 코드로 변환하는 역할을 수행합니다. 그러나 구조 없이 원시적으로 AI를 활용하면 혼란이 발생합니다. SDD는 잘 정의된 사양과 구현 계획을 통해 AI가 활용할 수 있는 명확한 구조를 제공합니다.

이는 카파시가 말한 에이전틱 엔지니어링의 핵심, 즉 **"개발자가 AI를 조율하고 감독하는 역할"** 을 구체적인 프레임워크로 구현한 것입니다.

```
[기존 바이브 코딩]
  개발자 ──(모호한 지시)──▶ AI ──▶ 예측 불가능한 코드

[SDD + 에이전틱 엔지니어링]
  개발자 ──(명확한 사양)──▶ AI ──▶ 검증된 코드
     ▲          ▲                        │
     │          │                        │
  Memory    Spec 문서              피드백/검증
  Bank      (기능 명세)                  │
     │          │                        │
     └──────────┴────────────────────────┘
```

---

## 5. SDD 4단계 워크플로

```
[1단계: 지정] ──▶ [2단계: 계획] ──▶ [3단계: 작업] ──▶ [4단계: 구현]
  Specify              Plan              Task             Implement
  "무엇을?"           "어떻게?"        "어떤 순서로?"      "코드 생성"
```

각 단계는 다음 단계의 입력물이 되는 아티팩트를 생성하며, 이를 통해 요구 사항에서 최종 코드까지의 완전한 추적성(Traceability)이 보장됩니다.

---

### 1단계: 지정(Specify)

**"소프트웨어가 무엇을 해야 하는지, 그리고 왜 해야 하는지를 정의합니다."**

지정 단계는 SDD의 출발점입니다. 이 단계에서 내린 결정이 이후 모든 단계의 품질을 좌우하기 때문에, 가장 많은 시간과 노력을 투자해야 합니다. 개발자, 기획자, 이해관계자가 함께 참여하여 시스템이 달성해야 할 목표를 명확하게 합의하는 과정입니다.

> **⚠️ 바이브 코딩과의 차이점**
>
> 바이브 코딩에서는 "쇼핑몰 만들어 줘"라고 말합니다. SDD의 지정 단계에서는 쇼핑몰이 어떤 사용자를 위해, 어떤 문제를 해결하며, 어떤 조건을 만족해야 하는지를 체계적으로 문서화합니다.

#### 사용자 스토리(User Story)

사용자 스토리는 최종 사용자의 관점에서 기능을 서술하는 방식입니다. 기술적 구현이 아닌 사용자가 얻게 되는 가치에 초점을 맞춥니다.

작성 형식은 다음과 같습니다.

> "나는 [사용자 유형]으로서, [목표]를 달성하기 위해 [기능]을 원한다."

예시는 다음과 같습니다.

> "나는 쇼핑몰 회원으로서, 주문 내역을 한눈에 확인하기 위해 마이페이지에서 최근 주문 목록을 조회하고 싶다."

사용자 스토리를 잘 작성하면 개발팀이 '무엇을 만들 것인가'보다 '왜 만드는가'에 집중할 수 있어, 불필요한 기능 개발을 방지하고 실제 사용자 가치를 중심으로 개발 방향을 정렬할 수 있습니다.

#### 수용 기준(Acceptance Criteria)

수용 기준은 특정 사용자 스토리가 완료되었다고 판단할 수 있는 조건을 명확하게 정의한 것입니다. "언제 이 기능이 완성되었다고 볼 수 있는가?"에 대한 구체적인 답변입니다.

수용 기준은 주로 Given-When-Then 형식으로 작성합니다.

- **Given(주어진 상황)**: 특정 조건이나 상태를 전제합니다.
- **When(동작)**: 사용자가 어떤 행동을 취합니다.
- **Then(결과)**: 시스템이 어떻게 응답해야 하는지를 기술합니다.

예시는 다음과 같습니다.

> - Given: 로그인한 사용자가 마이페이지에 접속했을 때
> - When: 주문 내역 탭을 클릭하면
> - Then: 최근 3개월 이내 주문 목록이 날짜 내림차순으로 표시된다.

수용 기준이 명확할수록 개발자와 QA 담당자 사이의 해석 차이가 줄어들며, AI가 코드를 생성할 때도 더 정확한 결과물을 만들어 낼 수 있습니다.

#### 요구 사항(Requirements)

요구 사항은 시스템이 반드시 충족해야 하는 조건들을 체계적으로 정리한 것입니다. 크게 두 가지로 구분됩니다.

**기능적 요구 사항(Functional Requirements)** 은 시스템이 수행해야 하는 구체적인 기능을 정의합니다.

- 회원가입 시 이메일 중복 여부를 확인해야 한다.
- 비밀번호는 최소 8자 이상, 영문·숫자·특수문자를 각각 1개 이상 포함해야 한다.
- 주문 완료 후 사용자에게 확인 이메일을 발송해야 한다.

**비기능적 요구 사항(Non-Functional Requirements)** 은 시스템의 품질 속성을 정의합니다.

- 성능: 주문 목록 조회 응답 시간은 2초 이내여야 한다.
- 보안: 개인정보는 AES-256 암호화로 저장해야 한다.
- 가용성: 시스템 가동률은 99.9% 이상을 유지해야 한다.

요구 사항 문서는 사양의 핵심 구성요소로, AI가 코드를 생성할 때 판단 기준이 됩니다. 요구 사항이 불명확하면 AI는 임의로 가정하여 의도와 다른 코드를 생성하게 됩니다.

#### 엣지 케이스(Edge Cases)

엣지 케이스는 일반적이지 않은 입력값이나 예외적인 상황에서 시스템이 어떻게 동작해야 하는지를 정의합니다. 대부분의 버그는 엣지 케이스를 사전에 정의하지 않아 발생합니다.

엣지 케이스의 주요 유형은 다음과 같습니다.

- **경계값**: 주문 수량이 0개일 때, 재고가 정확히 1개 남았을 때
- **빈 값 및 null**: 검색어 없이 검색 버튼을 눌렀을 때
- **권한 예외**: 로그인하지 않은 사용자가 마이페이지에 접근할 때
- **동시성**: 두 사용자가 동시에 마지막 재고 1개를 구매하려 할 때
- **네트워크 오류**: 결제 진행 중 연결이 끊겼을 때

엣지 케이스를 사전에 정의해 두면, AI가 예외 처리 로직을 포함한 완성도 높은 코드를 생성할 수 있습니다.

---

### 2단계: 계획(Plan)

**"어떻게 구축할 것인지를 결정합니다."**

계획 단계는 1단계에서 정의한 '무엇을'을 '어떻게'로 전환하는 과정입니다. 아키텍처, 기술 스택, 구현 전략을 결정하여 개발팀 전체가 동일한 방향으로 나아갈 수 있는 청사진을 만듭니다. 이 단계의 산출물은 이후 AI가 코드를 생성하는 방식에 직접적인 영향을 미칩니다.

#### 아키텍처 설계

아키텍처 설계는 시스템의 전체 구조를 결정하는 작업입니다. 주요 결정 사항은 다음과 같습니다.

**시스템 구조 선택**은 모놀리식(Monolithic), 마이크로서비스(Microservices), 서버리스(Serverless) 등 프로젝트 특성에 맞는 구조를 선택합니다.

**컴포넌트 분리**는 시스템을 어떤 단위로 나눌 것인지 정의합니다. 예를 들어 사용자 인증 서비스, 주문 서비스, 결제 서비스, 알림 서비스 등으로 분리할 수 있습니다.

**데이터 흐름**은 컴포넌트 간 데이터가 어떻게 이동하는지, API 통신 방식(REST, GraphQL, gRPC 등)은 무엇인지를 정의합니다.

**데이터 모델**은 핵심 엔티티와 관계를 설계합니다. 예를 들어 사용자(User), 주문(Order), 상품(Product) 간의 관계를 ERD로 표현합니다.

아키텍처 문서는 사양의 일부로 관리되어야 하며, 구현이 변경될 때마다 함께 업데이트되어야 합니다.

#### 기술 스택 선정

기술 스택 선정은 단순히 익숙한 기술을 고르는 것이 아니라, 요구 사항과 제약 조건을 기반으로 합리적인 근거를 가지고 결정해야 합니다.

선정 기준은 다음과 같습니다.

- **성능 요구 사항**: 높은 트래픽 처리가 필요한 경우, 비동기 처리에 강한 언어나 프레임워크를 선택합니다.
- **팀 역량**: 팀이 보유한 기술과의 일치 여부를 고려합니다.
- **생태계 성숙도**: 라이브러리, 커뮤니티, 장기 지원 여부를 검토합니다.
- **조직 표준**: 엔터프라이즈 환경에서는 조직에서 승인된 기술 목록을 준수해야 합니다.
- **보안 및 규정 준수**: 특정 산업의 규정에 부합하는 기술인지 확인합니다.

선정한 기술 스택과 그 근거를 사양 문서에 명시적으로 기록해 두면, 이후 AI가 코드를 생성할 때 올바른 언어, 프레임워크, 라이브러리를 일관되게 사용할 수 있습니다.

#### 구현 전략 수립

구현 전략은 개발 순서와 방식에 대한 큰 그림을 제공합니다. 주요 결정 사항은 다음과 같습니다.

**개발 우선순위**는 핵심 기능(Core)을 먼저 구현하고 부가 기능(Enhancement)을 이후에 추가하는 순서를 결정합니다.

**통합 전략**은 외부 시스템(결제 게이트웨이, 외부 API 등)과의 연동 방식을 계획합니다.

**테스트 전략**은 단위 테스트(Unit Test), 통합 테스트(Integration Test), E2E 테스트의 범위와 도구를 결정합니다.

**배포 전략**은 CI/CD 파이프라인, 환경 구성(개발, 스테이징, 운영), 릴리즈 방식을 계획합니다.

---

### 3단계: 작업(Task)

**"계획을 구체적이고 실행 가능한 개발 작업 단위로 분해합니다."**

작업 단계는 2단계의 계획을 개발자(또는 AI)가 즉시 실행할 수 있는 수준의 작업 목록으로 세분화하는 과정입니다. 작업이 구체적일수록 AI는 더 정확하고 일관된 코드를 생성하며, 개발자는 진행 상황을 명확하게 추적할 수 있습니다.

> **💡 에이전틱 엔지니어링 관점**
>
> 에이전틱 엔지니어링에서 개발자는 AI 에이전트를 '조율'하는 역할을 합니다. 이 조율의 핵심이 바로 잘 분해된 작업 목록입니다. 명확한 작업 목록이 없으면 에이전트는 방향을 잃고 일관성 없는 결과를 만들어 냅니다.

#### 단계별로 구조화된 개별 작업 목록 생성

좋은 작업 목록의 핵심은 각 작업이 **독립적으로 완료 가능한 수준**으로 분해되어 있어야 한다는 것입니다.

작업 정의 시 포함해야 할 요소는 다음과 같습니다.

- **작업 제목**: 무엇을 만드는지 명확하게 서술합니다. 예: "회원가입 API 엔드포인트 구현"
- **참조 사양**: 이 작업이 어떤 사용자 스토리, 요구 사항, 수용 기준과 연결되는지 명시합니다.
- **구현 범위**: 이 작업에서 다루는 것과 다루지 않는 것을 명확히 합니다.
- **완료 조건**: 이 작업이 완료되었다고 판단하는 기준을 정의합니다.
- **예상 산출물**: 코드 파일, API 문서, 테스트 코드 등 기대되는 결과물을 나열합니다.

작업 분해 예시는 다음과 같습니다.

```
[계획] 회원가입 기능 구현
  │
  ├── [작업 1] User 데이터 모델 및 DB 스키마 생성
  ├── [작업 2] 이메일 중복 확인 API 구현
  ├── [작업 3] 회원가입 API 엔드포인트 구현
  ├── [작업 4] 비밀번호 유효성 검증 로직 구현
  ├── [작업 5] 가입 완료 확인 이메일 발송 기능 구현
  └── [작업 6] 회원가입 관련 단위 테스트 작성
```

작업의 크기는 하루 이내에 완료할 수 있는 수준이 이상적입니다. 너무 큰 작업은 다시 분해하고, 너무 작은 작업은 관련된 것끼리 묶는 것이 좋습니다.

#### 각 작업의 우선순위 및 의존 관계 정의

작업 목록이 완성되면 실행 가능한 순서를 결정해야 합니다.

우선순위 결정 기준은 다음과 같습니다.

- **의존 관계**: 다른 작업의 전제가 되는 작업은 먼저 수행합니다. 위의 예시에서 작업 1(데이터 모델 생성)은 작업 3(API 구현)보다 반드시 먼저 완료되어야 합니다.
- **비즈니스 중요도**: 핵심 기능을 구현하는 작업을 우선합니다.
- **기술적 위험도**: 불확실성이 높은 작업을 초기에 배치하여 위험을 조기에 파악합니다.
- **병렬 가능 여부**: 의존 관계가 없는 작업은 병렬로 진행하여 개발 속도를 높입니다.

---

### 4단계: 구현(Implement)

**"사양, 계획, 작업 목록을 기반으로 코드를 작성합니다."**

구현 단계는 앞선 세 단계의 산출물을 기반으로 실제 코드를 작성하는 단계입니다. SDD에서 구현은 단순한 코딩 작업이 아니라, 사양을 코드로 번역하는 검증된 프로세스입니다. AI는 이 단계에서 가장 큰 역할을 수행하며, 명확한 사양이 있을수록 더 정확하고 완성도 높은 코드를 생성합니다.

#### 사양에 따라 각 작업을 검증(Verify)하며 진행

구현은 작업 목록의 순서에 따라 하나씩 진행하되, 각 작업이 완료될 때마다 반드시 사양에 정의된 수용 기준과 대조하여 검증합니다.

```
[작업 선택] ──▶ [사양 확인] ──▶ [코드 생성/작성] ──▶ [수용 기준 검증] ──▶ [다음 작업]
                                                              │
                                             (실패 시 사양 재검토 또는 코드 수정)
```

AI를 활용한 코드 생성 시, 각 프롬프트에는 다음 정보를 포함하는 것이 좋습니다.

- 현재 작업의 목표와 범위
- 관련 사용자 스토리 및 수용 기준
- 적용할 기술 스택과 아키텍처 컨텍스트 (Memory Bank에서)
- 이미 완료된 이전 작업의 결과물(API 계약, 데이터 모델 등)
- 처리해야 할 엣지 케이스 목록

#### 요구 사항에서 완성 코드까지 추적 가능한 경로 확보

SDD에서 추적성(Traceability)은 핵심 가치 중 하나입니다. 모든 코드 조각은 특정 사용자 스토리, 요구 사항, 작업으로 거슬러 올라갈 수 있어야 합니다.

```python
# Implements: User Story #12 - 주문 목록 조회
# Acceptance Criteria: AC-12-1, AC-12-2
# Task: TASK-045
def get_order_list(user_id: str) -> list:
    ...
```

추적성이 확보되면 이후 요구 사항이 변경될 때 영향 범위를 빠르게 파악할 수 있으며, 감사(Audit)나 규정 준수 검토 시에도 명확한 근거 자료로 활용할 수 있습니다.

---

## 6. 실전 도구 비교

SDD 방법론이 실제 개발 현장에서 어떻게 구현되고 있는지, 주요 도구들을 비교합니다. 각 도구가 SDD의 어떤 레벨을 지원하는지, 어떤 철학을 가지고 있는지 살펴봅니다.

```
실전 도구 비교 개요

도구           │ 만든 곳  │ 접근법          │ SDD 레벨
───────────────┼─────────┼─────────────────┼──────────
AWS Kiro       │ Amazon  │ 통합 AI IDE      │ Level 2~3
GitHub Spec Kit│ GitHub  │ 명령어 기반 확장 │ Level 1~3
Tessl          │ Tessl   │ AI 네이티브 플랫폼│ Level 2~3
```

---

### 6.1 AWS의 Kiro

#### 개요

**Kiro(카이로)** 는 2025년 AWS가 발표한 **명세 주도 AI IDE(Spec-Driven AI IDE)** 입니다. "스펙에서 코드까지(Spec to Code)"를 핵심 슬로건으로 내세우며, SDD를 IDE 수준에서 완전히 통합하려는 시도입니다.

> Kiro는 기존의 VS Code 기반 AI 코딩 도구들이 "코드 자동완성"에 집중했다면, Kiro는 **소프트웨어 개발의 시작점을 스펙으로 옮기는 것**을 목표로 합니다.

#### 핵심 기능

**Spec 자동 생성**은 자연어로 기능 아이디어를 입력하면 Kiro가 요구사항, 설계 문서, 구현 작업 목록을 자동으로 생성합니다. SDD의 1~3단계 워크플로가 자동화됩니다.

```
개발자 입력: "사용자가 소셜 로그인으로 서비스에 가입할 수 있어야 해"
        ↓
Kiro 자동 생성:
  ├── requirements.md (기능 요구사항)
  ├── design.md       (아키텍처 설계)
  └── tasks.md        (구현 작업 목록, 우선순위 포함)
```

**Steering Documents(조율 문서)** 는 GitHub Spec Kit의 `constitution.md`에 해당하는 개념입니다. 프로젝트의 기술 스택, 코딩 컨벤션, 보안 원칙 등을 정의하면 Kiro의 모든 AI 작업에 자동으로 적용됩니다.

**Hook 시스템**은 코드 변경이 발생할 때 자동으로 트리거되는 검증 워크플로입니다. 예를 들어 "API 엔드포인트가 변경되면 자동으로 API 문서를 업데이트한다" 같은 규칙을 설정할 수 있습니다.

#### SDD 관점에서의 평가

| 항목 | 내용 |
|---|---|
| SDD 지원 레벨 | Level 2~3 (Spec-Anchored ~ Spec-as-Source) |
| 강점 | 스펙 → 코드 변환이 워크플로에 완전 통합 |
| 강점 | Steering Documents로 Memory Bank 관리 |
| 제한 | 기존 프로젝트에 도입 시 학습 비용 |
| 적합 대상 | 새 프로젝트, AWS 생태계를 활용하는 팀 |

---

### 6.2 GitHub Spec Kit

#### 개요

**GitHub Spec Kit**은 VS Code와 GitHub Copilot을 연동하여 SDD 워크플로를 구현하는 **명령어 기반 확장 도구**입니다. 2강에서 상세히 다룰 도구이지만, 실전 도구 비교 맥락에서 간략히 위치를 잡겠습니다.

#### 핵심 특징

명령어 기반 워크플로는 VS Code Copilot 채팅창에서 `/speckit.*` 슬래시 명령어로 SDD의 각 단계를 실행합니다.

```
/speckit.specify   → 요구사항 → spec.md 생성 (1단계)
/speckit.clarify   → 명세 보완
/speckit.checklist → 품질 점검
/speckit.plan      → 구현 계획 → plan.md 생성 (2단계)
/speckit.tasks     → 작업 목록 → tasks.md 생성 (3단계)
/speckit.analyze   → 일관성 검사
/speckit.implement → 코드 생성 (4단계)
```

아티팩트 중심 접근은 각 단계의 결과물이 파일(spec.md, plan.md, tasks.md)로 저장되어 Git으로 버전 관리됩니다. 사양이 코드와 함께 살아있는 문서(Living Documentation)로 유지됩니다.

#### SDD 관점에서의 평가

| 항목 | 내용 |
|---|---|
| SDD 지원 레벨 | Level 1~3 (단계적 도입 가능) |
| 강점 | VS Code + Copilot 환경에 자연스럽게 통합 |
| 강점 | 기존 프로젝트에도 점진적으로 도입 가능 |
| 강점 | Git 기반 버전 관리로 사양 이력 추적 |
| 제한 | Copilot 구독 필요, 월 50회 채팅 한도 (무료 계정) |
| 적합 대상 | GitHub 생태계를 사용하는 팀, VS Code 사용자 |

---

### 6.3 Tessl Framework

#### 개요

**Tessl(테슬)**은 "AI Native Software Factory"를 표방하는 플랫폼입니다. GitHub Spec Kit이나 Kiro가 기존 개발 도구에 SDD를 통합하는 방식이라면, Tessl은 **소프트웨어 개발 프로세스 전체를 AI 중심으로 재설계**하는 더 급진적인 접근을 취합니다.

#### 핵심 철학

Tessl의 핵심 개념은 **"의도 주도 개발(Intent-Driven Development)"** 입니다. 개발자가 "무엇을 원하는가(Intent)"를 선언하면, AI가 이를 스펙, 코드, 테스트, 배포 파이프라인으로 변환합니다.

```
[Tessl의 접근법]

개발자의 의도(Intent)
        ↓
AI가 스펙 생성
        ↓
AI가 코드 생성
        ↓
AI가 테스트 생성
        ↓
AI가 배포 실행

← 개발자는 각 단계의 결과를 검토하고 승인
```

이는 SDD Level 3(Spec-as-Source)의 완전한 실현에 가깝습니다.

#### SDD 관점에서의 평가

| 항목 | 내용 |
|---|---|
| SDD 지원 레벨 | Level 3 (Spec-as-Source) 지향 |
| 강점 | 개발 사이클 전체의 AI 자동화 |
| 강점 | 스펙 → 코드 → 테스트 → 배포의 완전 통합 |
| 제한 | 기존 개발 방식과 큰 패러다임 차이 → 높은 학습 비용 |
| 제한 | 아직 성숙도가 낮아 엔터프라이즈 도입에 검증 필요 |
| 적합 대상 | AI 네이티브 신규 프로젝트, 실험적 도입을 원하는 팀 |

---

### 6.4 Böckeler가 던진 핵심 질문들

Birgitta Böckeler는 SDD를 분석하면서 단순히 장점만 나열하지 않았습니다. 그녀는 SDD가 실제로 유효한지에 대한 **날카로운 비판적 질문들**을 함께 제시했습니다. 이 질문들은 SDD를 도입할 때 반드시 고민해야 할 핵심 논점입니다.

---

#### 질문 1: "하나의 워크플로우로 모든 크기의 문제를 커버할 수 있는가?"

**질문의 핵심**

SDD 워크플로(Specify → Plan → Task → Implement)는 중간 규모 이상의 기능 개발에 잘 맞습니다. 그런데 소규모 버그 수정이나 간단한 텍스트 변경에도 같은 워크플로를 적용해야 하는가?

```
[대규모 기능] 로그인 시스템 구현
  → Specify → Plan → Task → Implement (수일 ~ 수주)
  → SDD 워크플로 적합 ✅

[소규모 변경] 버튼 색상을 파란색에서 초록색으로 변경
  → spec.md 작성 → plan.md 작성 → tasks.md 작성 → 구현
  → SDD 워크플로 적용 시 과도한 오버헤드 ❌
```

**Böckeler의 지적**

모든 크기의 작업에 동일한 SDD 워크플로를 적용하면, 작은 변경이 불필요하게 복잡해지고 팀의 생산성이 오히려 낮아질 수 있습니다.

**현실적인 대응**

SDD를 도입할 때는 **작업 규모에 따른 워크플로 분기**가 필요합니다.

```
작업 규모 판단:
  ├── 소규모(< 30분): 자유 형식 지시 → 코드 수정 → 완료
  ├── 중규모(30분~1일): 간략한 spec → 구현
  └── 대규모(1일 이상): 전체 SDD 워크플로 적용
```

---

#### 질문 2: "스펙 리뷰가 코드 리뷰보다 나은가?"

**질문의 핵심**

SDD에서는 코드 리뷰 전에 스펙 리뷰를 먼저 합니다. 하지만 개발자들은 수십 년간 코드 리뷰를 통해 품질을 검증해 왔습니다. 스펙 리뷰가 정말로 코드 리뷰를 대체할 수 있는가?

**Böckeler의 지적**

스펙 리뷰와 코드 리뷰는 서로 다른 것을 검증합니다.

```
스펙 리뷰가 잡아내는 것:
  - 요구사항 누락
  - 설계 방향의 오류
  - 이해관계자 간 의견 불일치
  - 구현 전에 발견할 수 있는 논리 오류

코드 리뷰가 잡아내는 것:
  - 구현 수준의 버그
  - 성능 문제
  - 코딩 컨벤션 위반
  - 실제 동작과 의도의 불일치
```

스펙 리뷰는 코드 리뷰를 대체하는 것이 아닙니다. 오히려 **스펙 리뷰가 코드 리뷰의 품질을 높이는 보완 관계**입니다.

**현실적인 대응**

스펙 리뷰를 도입할 때는 "코드 리뷰를 없애는 것"이 아니라 "코드 리뷰의 범위를 줄이는 것"으로 접근합니다.

```
[SDD 적용 전] 코드 리뷰에서 모든 것을 검토
  → 방향 오류, 설계 문제, 코드 버그 모두 코드 리뷰에서 발견
  → 리뷰 비용 높음, 수정 비용 매우 높음

[SDD 적용 후] 스펙 리뷰와 코드 리뷰가 역할 분담
  → 스펙 리뷰: 방향과 설계 검증 (비용 낮음)
  → 코드 리뷰: 구현 세부사항 검증 (범위 좁아짐)
  → 전체 리뷰 비용 감소, 문제 조기 발견
```

---

#### 질문 3: "통제의 착각은 아닌가?"

**질문의 핵심**

세밀한 스펙 문서를 작성하면 AI가 예측 가능하게 동작할 것이라고 기대합니다. 하지만 실제로는 AI가 여전히 스펙의 일부를 잘못 해석하거나 누락할 수 있습니다. 이때 "스펙을 작성했다"는 사실이 오히려 **잘못된 안도감(False Sense of Control)** 을 주는 것은 아닌가?

**Böckeler의 지적**

스펙이 존재한다고 해서 AI의 출력이 자동으로 올바른 것이 아닙니다. 스펙은 **필요 조건**이지 **충분 조건**이 아닙니다.

```
착각: 스펙 작성 → AI가 스펙대로 코드 생성 → 완성
현실: 스펙 작성 → AI가 스펙을 나름대로 해석 → 검증 필요
                                                    ↓
                        스펙과 코드가 일치하는지 개발자가 반드시 검증해야 함
```

**현실적인 대응**

스펙을 작성하는 것으로 끝나지 않고, 반드시 **검증(Verification) 단계**가 뒤따라야 합니다.

```
SDD의 올바른 이해:
  스펙 작성 → AI 코드 생성 → [검증] → 불일치 발견 → 스펙 보완 → 재생성
                                  ↑
              /speckit.analyze, /speckit.checklist 등이 이 역할 수행
```

수용 기준(Acceptance Criteria)을 테스트 케이스로 변환하고, 테스트가 통과할 때만 "스펙이 충족되었다"고 판단해야 합니다. 스펙을 작성했다고 통제하고 있다는 착각에 빠지면, 버그가 숨겨진 코드를 승인하는 위험이 발생합니다.

---

#### 질문 4: "MDD의 실패를 반복하는 것은 아닌가?"

**질문의 핵심**

이 질문은 가장 역사적이고 철학적인 비판입니다.

**MDD(Model-Driven Development)** 는 1990년대~2000년대에 소프트웨어 개발계를 뜨겁게 달궜던 방법론입니다. MDD의 약속은 이러했습니다.

> "소프트웨어 시스템을 추상적인 모델로 정의하면, 그 모델에서 코드를 자동으로 생성할 수 있다."

MDD를 지원하는 도구들(IBM Rational Rose, Eclipse EMF, UML-to-Code 도구들)이 쏟아졌고, 많은 기업들이 도입했습니다. 하지만 결과는?

```
MDD의 실패 원인:
  ├── 모델 언어가 너무 형식적 → 비개발자가 이해하기 어려움
  ├── 모델 → 코드 변환이 불완전 → 결국 생성된 코드를 수작업 수정
  ├── 모델과 코드가 결국 다시 괴리 → "다시 코드가 진실"
  └── 실제 복잡한 비즈니스 로직을 모델로 표현하는 것의 한계
```

SDD도 "스펙에서 코드를 생성한다"는 유사한 약속을 합니다. **SDD는 MDD의 실수를 반복하는 것이 아닌가?**

**Böckeler의 분석: SDD와 MDD의 결정적 차이**

Böckeler는 이 질문에 대해 중요한 차이점을 지적합니다.

| 비교 항목 | MDD | SDD |
|---|---|---|
| 스펙 언어 | 형식적 모델링 언어(UML, DSL) | 자연어 + 구조화된 마크다운 |
| 코드 생성 방식 | 규칙 기반 컴파일러 | 자연어 이해 기반 LLM |
| 모호함 처리 | 모호함 불허 (컴파일 오류) | 맥락을 추론하여 처리 |
| 사람의 참여 | 모델만 올바르면 사람 개입 최소화 | 사람이 검증 단계에 필수 참여 |
| 실패 방식 | 변환 실패 시 아무것도 생성 못 함 | 불완전하게 생성 후 검증에서 보완 |

**핵심 차이**는 다음과 같습니다.

```
MDD: 스펙(모델) → [형식 변환기] → 코드
     모델이 조금이라도 형식에 맞지 않으면 변환 실패

SDD: 스펙(자연어) → [LLM 해석] → 코드 + [검증]
     LLM이 맥락을 추론하여 불완전한 스펙도 어느 정도 처리
     인간이 검증 단계에서 오류를 보완
```

MDD가 **인간을 완전히 배제하는 완전 자동화**를 목표로 했다면, SDD는 **AI와 인간의 협업**을 목표로 합니다.

**그러나 Böckeler의 경고**

그렇다고 SDD가 MDD의 함정을 완전히 피해가는 것은 아닙니다.

스펙이 코드와 점점 괴리되는 문제는 MDD와 SDD 모두에서 발생할 수 있습니다. AI가 스펙을 "충분히 잘" 해석하지 못하는 경우 개발자가 코드를 직접 수정하기 시작하고, 그 순간부터 스펙과 코드는 다시 분리됩니다.

```
SDD가 MDD처럼 실패하는 패턴:
  1. 스펙 작성
  2. AI가 코드 생성 (스펙과 약간 다름)
  3. 개발자가 코드 직접 수정 (스펙 업데이트 없이)
  4. 스펙과 코드 괴리 발생
  5. 스펙이 다시 무용지물이 됨 ← MDD의 실패 패턴과 동일
```

**이를 방지하는 원칙**: "코드를 수정하기 전에 반드시 스펙을 먼저 수정한다."

---

#### 핵심 질문들의 종합적 함의

Böckeler가 던진 4가지 질문은 SDD를 비판하는 것이 아니라, **SDD를 올바르게 실천하기 위한 경계선을 그어주는 것**입니다.

| 질문 | 핵심 교훈 |
|---|---|
| 워크플로 크기 | SDD는 모든 작업에 동일하게 적용하지 말고, 규모에 맞게 조정하라 |
| 스펙 리뷰 | 스펙 리뷰는 코드 리뷰를 대체하는 것이 아니라 보완하는 것이다 |
| 통제의 착각 | 스펙 작성이 끝이 아니다. 반드시 검증 단계가 뒤따라야 한다 |
| MDD 실패 반복 | "코드 수정 전 스펙 수정"의 원칙을 절대 타협하지 말라 |

> **💡 핵심 메시지**
>
> SDD는 만능이 아닙니다. 올바르게 실천하지 않으면 MDD처럼 실패할 수 있습니다. Böckeler의 질문들을 항상 기억하며 비판적으로 적용하는 것이 SDD를 성공적으로 운용하는 열쇠입니다.

---

## 7. 엔터프라이즈 환경에서 SDD가 중요한 이유

다음 세 가지 흐름이 엔터프라이즈 팀에게 SDD를 필수적인 방법론으로 만들고 있습니다.

### 1. AI 기능의 발전

자연어로 작성된 사양이 이제는 작동하는 코드를 안정적으로 생성할 수 있습니다. 사양에서 구현으로 이어지는 번역 과정을 자동화할 수 있게 된 것입니다. 카파시가 말한 에이전틱 엔지니어링이 현실화될수록, 사양의 품질이 곧 소프트웨어의 품질이 됩니다.

### 2. 소프트웨어 복잡성의 증가

현대의 시스템은 수십 개의 서비스, 프레임워크, 의존성을 통합합니다. SDD는 사양 기반 생성을 통해 이러한 복잡한 환경에서도 체계적인 정합성을 제공합니다.

### 3. 빠른 변화의 속도

요구 사항은 빠르게 변합니다. SDD는 요구 사항 변경을 장애물이 아닌 정상적인 워크플로로 전환시킵니다. 사양을 업데이트하면 영향을 받는 아티팩트가 체계적으로 재생성됩니다.

### SDD가 주는 구체적인 이점

| 이점 | 설명 |
|------|------|
| **일관성** | 조직 표준과의 지속적이고 일관된 정합성 유지 |
| **감사 가능성** | 요구 사항과 설계 결정에 대한 추적 및 감사가 가능한 문서화 |
| **규정 준수** | 사양 프로세스를 통한 보안 정책 및 규정 준수 요건의 체계적 적용 |
| **팀 협업** | 새로운 팀원이 합류했을 때 사양 문서만으로 시스템을 빠르게 파악 가능 |
| **AI 활용 극대화** | 명확한 사양이 AI의 코드 생성 품질을 직접적으로 향상 |

---

## 8. 정리

AI 시대의 소프트웨어 개발은 빠르게 진화하고 있습니다.

```
바이브 코딩          AI 코딩의 현실          SDD           에이전틱 엔지니어링
(2025년 초)    →    (한계 발견)       →   (해결책)   →    (실천 프레임워크)
"일단 만들자"        "왜 안 되지?"          "구조가             "잘 만들자"
                                           필요하다"
```

### 핵심 교훈 요약

**AI 코딩의 현실**에서 배운 것은 다음과 같습니다.

- 자유 형식 지시는 AI를 혼란스럽게 만든다 (문제 1)
- 구조 없는 많은 정보도 AI에게 도움이 되지 않는다 (문제 2)
- 진짜 문제는 커뮤니케이션 구조의 부재다 (근본 원인)

**SDD 이론**에서 배운 것은 다음과 같습니다.

- Böckeler가 정의한 SDD: 사양이 코드의 소스이자 진실의 근원
- 3가지 구현 레벨: Level 1(Spec-First) → Level 2(Spec-Anchored) → Level 3(Spec-as-Source)
- 스펙은 "무엇을", Memory Bank는 "어떻게" — 두 가지가 함께 AI에게 완전한 맥락을 제공

**Böckeler의 경고**에서 배운 것은 다음과 같습니다.

- SDD는 만능이 아니다. 작업 규모에 따라 조정이 필요하다
- 스펙 리뷰와 코드 리뷰는 역할이 다르다. 하나가 다른 것을 대체하지 않는다
- "스펙을 작성했다"는 착각은 금물. 반드시 검증이 뒤따라야 한다
- MDD의 실패 패턴(코드를 수정하면서 스펙을 포기하는 것)을 반복하지 말라

> **핵심 요약**
>
> - **바이브 코딩**: AI 활용의 첫 단계. 빠르지만 품질이 불안정하다.
> - **AI 코딩의 현실**: 자유 형식 지시와 비구조화 정보는 한계가 있다.
> - **SDD**: 커뮤니케이션 구조를 제공하는 사양 중심 개발 프레임워크.
>   - Level 1 (Spec-First): 코딩 전 문서화 → 지금 당장 실천 가능
>   - Level 2 (Spec-Anchored): 사양이 개발의 기준점
>   - Level 3 (Spec-as-Source): 사양에서 코드 생성
> - **스펙 + Memory Bank**: AI에게 "무엇을"과 "어떻게"를 모두 제공
> - **에이전틱 엔지니어링**: AI를 조율하고 감독하는 전문 개발 방식.
> - **GitHub Spec Kit**: SDD를 실제 프로젝트에서 적용하기 위한 도구 모음.

다음 강의에서는 GitHub Spec Kit을 활용하여 이 SDD 워크플로를 실제로 구현하는 방법을 살펴보겠습니다.

---

*※ 본 문서는 강의용으로 작성되었습니다. 내용 관련 문의는 강의 담당자에게 연락하세요.*

*참고 문헌: Birgitta Böckeler, "Specification-Driven Development", martinfowler.com / Andrej Karpathy, "Vibe Coding" and "Agentic Engineering", X(Twitter) / AWS Kiro 공식 문서 / GitHub Spec Kit 공식 문서*
