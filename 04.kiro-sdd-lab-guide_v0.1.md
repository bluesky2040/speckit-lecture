# Kiro CLI SDD 실습 가이드 — 3차시
## 주제: 로그인 화면 만들기 + 명세 수정 + 기능 추가

> **이 실습은 3차시입니다.**  
> 1차시(SDD 방법론) → 2차시(Kiro CLI 가이드) 에서 배운 개념을 직접 실행합니다.  
> 이 버전은 **Kiro CLI (AWS)** 를 단독으로 사용하며, 외부 CLI 도구(specify 등)를 설치하지 않습니다.

**목표**: GitHub Codespace 환경에서 Kiro CLI만으로 SDD 전체 흐름을 체험하고, 로그인 화면을 완성한다  
**사용 도구**: GitHub Codespaces, Kiro CLI  
**결과물**: HTML + CSS + JavaScript 기반 로그인 화면 (명세 변경 + 3가지 기능 포함)  
**무료 여부**: AWS Builder ID 로 Kiro CLI 를 **무료**로 사용 (신용카드 불필요)

---

## Spec Kit 미사용 버전의 핵심 차이

| 항목 | Spec Kit 사용 버전 | **이 실습 (Kiro 단독)** |
|---|---|---|
| 명세 파일 생성 | `specify` CLI 자동 생성 | **직접 작성 (Kiro 가 도와줌)** |
| 슬래시 명령어 | `/speckit-specify` 등 | **Kiro 채팅에서 직접 요청** |
| 파일 구조 | `.specify/specs/` 자동 관리 | **`docs/specs/` 수동 관리** |
| Steering 파일 | 동일 | **동일 (`.kiro/steering/`)** |
| SDD 워크플로 | 동일 | **동일** |

> **핵심 메시지**: 도구가 없어도 SDD 방법론과 워크플로는 완전히 동일하다.  
> Kiro 채팅창에 단계별로 명확하게 요청하면, Kiro 가 명세·계획·코드를 직접 생성해 준다.

---

## 목차

1. [실습 전 준비 사항](#실습-전-준비-사항)
2. [실습 개요 — SDD 전체 흐름](#실습-개요--sdd-전체-흐름)
3. [Part 1. Codespace 생성](#part-1-codespace-생성-약-15분)
4. [Part 2. Kiro CLI 설치 및 인증](#part-2-kiro-cli-설치-및-인증-약-15분)
5. [Part 3. 프로젝트 초기화](#part-3-프로젝트-초기화-약-10분)
6. [Part 4. 기본 로그인 화면 구현](#part-4-sdd-실습-1단계--기본-로그인-화면-약-65분)
7. [Part 5. 결과물 실행 및 테스트](#part-5-결과물-실행-및-테스트-약-15분)
8. [Part 6. 기존 명세(001) 변경 대처](#part-6-sdd--기존-명세001-변경-대처-약-30분)
9. [Part 7. 로그인 횟수 제한 추가](#part-7-sdd-실습-2단계--로그인-횟수-제한-추가-약-30분)
10. [Part 8. 아이디 저장 기능 추가](#part-8-sdd-실습-3단계--아이디-저장-기능-추가-약-30분)
11. [Part 9. 전체 산출물 구조 확인](#part-9-전체-산출물-구조-확인)
12. [트러블슈팅](#트러블슈팅)
13. [전체 명령어 요약표](#전체-명령어-요약표)
14. [실습 진행 시간 배분](#실습-진행-시간-배분)

---

## 실습 전 준비 사항

| 필요 계정 | 가입 주소 | 비고 |
|---|---|---|
| GitHub 계정 | https://github.com/signup | Codespace 실행에 필요 |
| **AWS Builder ID** | **https://profile.aws.amazon.com** | **Kiro CLI 인증에 사용 (무료, 신용카드 불필요)** |

> **AWS Builder ID 란?**  
> AWS 서비스 이용 없이 무료로 만들 수 있는 개인 개발자 계정이다.  
> AWS 계정(유료)과는 다르다. 이메일로만 가입 가능하며, Kiro CLI 를 무료로 사용할 수 있다.

**브라우저**: Chrome 또는 Edge 권장 (팝업 허용 필수)

---

## 실습 개요 — SDD 전체 흐름

SDD(Spec-Driven Development)는 코드를 먼저 짜지 않는다.  
**무엇을, 왜 만드는지** 먼저 정의하고, AI 에이전트가 구현하도록 지시하는 방법론이다.

### SDD 7단계 흐름

```
constitution → specify → clarify → checklist → plan → tasks → analyze → implement
(원칙 설정)  (요구사항) (모호함 제거) (명세점검)  (기술설계) (작업목록) (최종점검)  (코드생성)
   1회만        ①         ②            ③         ④        ⑤          ⑥          ⑦
```

### Kiro CLI 단독 방식에서 각 단계 실행법

| 단계 | Spec Kit 방식 | **Kiro 단독 방식 (이 실습)** |
|---|---|---|
| ① Specify | `/speckit-specify` | Kiro 채팅에 요구사항 입력 → Kiro 가 `spec.md` 직접 작성 |
| ② Clarify | `/speckit-clarify` | Kiro 채팅에 보완 사항 입력 → Kiro 가 `spec.md` 수정 |
| ③ Checklist | `/speckit-checklist` | Kiro 채팅에 점검 요청 → Kiro 가 `checklist.md` 작성 |
| ④ Plan | `/speckit-plan` | Kiro 채팅에 설계 요청 → Kiro 가 `plan.md` 작성 |
| ⑤ Tasks | `/speckit-tasks` | Kiro 채팅에 작업 목록 요청 → Kiro 가 `tasks.md` 작성 |
| ⑥ Analyze | `/speckit-analyze` | Kiro 채팅에 일관성 점검 요청 |
| ⑦ Implement | `/speckit-implement` | Kiro 채팅에 코드 생성 요청 → Kiro 가 `index.html` 작성 |

> **결론**: 명령어 대신 채팅 요청으로 동일한 결과를 얻는다.  
> 파일은 Kiro 가 직접 생성한다.

### 기능을 추가할 때의 흐름

```
[최초 구현]  constitution → specify(001) → clarify → checklist → plan → tasks → analyze → implement
[기능 추가]               → specify(002) → clarify → checklist → plan → tasks → analyze → implement
[기능 추가]               → specify(003) → clarify → checklist → plan → tasks → analyze → implement
```

### 기존 명세를 수정할 때의 흐름

```
[명세 수정]  spec.md 직접 수정 → clarify → checklist → analyze → plan(재실행) → tasks(재실행) → implement
```

### 이번 실습에서 다루는 시나리오

| 파트 | 시나리오 | SDD 번호 |
|---|---|---|
| Part 4 | 기본 로그인 화면 구현 | 001 최초 구현 |
| Part 6 | 001 명세 변경 — 로그인 성공 후 동작 추가 | 001 명세 수정 |
| Part 7 | 로그인 횟수 제한 + 카운트다운 기능 추가 | 002 신규 추가 |
| Part 8 | 아이디 저장 (localStorage) 기능 추가 | 003 신규 추가 |

---

## Part 1. Codespace 생성 (약 15분)

### 1-1. 빈 레포지토리 만들기

1. https://github.com 에 로그인한다.
2. 오른쪽 상단 `+` 버튼 클릭 후 `New repository` 를 선택한다.
3. 아래와 같이 입력한다.

| 항목 | 입력값 |
|---|---|
| Repository name | `login-page-lab` |
| Visibility | Public 또는 Private 선택 |
| Add a README file | **반드시 체크** |

4. `Create repository` 버튼을 클릭한다.

### 1-2. Codespace 실행

1. 생성된 레포지토리 페이지에서 초록색 `Code` 버튼을 클릭한다.
2. `Codespaces` 탭을 선택한다.
3. `Create codespace on main` 을 클릭한다.
4. 브라우저에서 VS Code 화면이 열릴 때까지 1~2분 기다린다.

터미널이 보이지 않으면 상단 메뉴에서 `Terminal → New Terminal` 을 선택한다.

### 1-3. 현재 위치 확인

```bash
pwd
```

예상 출력:

```
/workspaces/login-page-lab
```

> **확인 포인트 ✅**  
> `/workspaces/login-page-lab` 이 출력되면 Part 1 완료.

---

## Part 2. Kiro CLI 설치 및 인증 (약 15분)

> Spec Kit 버전과 달리 이 실습에서는 `uv` 와 `specify-cli` 를 설치하지 않는다.  
> **Kiro CLI 하나만 설치한다.**

### 2-1. Kiro CLI 설치

**설치 방법 1: 공식 설치 스크립트 (권장)**

```bash
curl --proto '=https' --tlsv1.2 -sSf \
  'https://desktop-release.q.us-east-1.amazonaws.com/latest/kirocli-x86_64-linux.zip' \
  -o kirocli.zip
unzip kirocli.zip
sudo ./install.sh
source ~/.bashrc
```

**설치 방법 2: Homebrew (macOS 또는 Linux)**

```bash
brew install kiro-cli
```

설치 확인:

```bash
kiro-cli --version
```

예상 출력:

```
kiro-cli x.x.x
```

> **`kiro-cli: command not found` 오류 시**  
> PATH 가 업데이트되지 않은 것이다.
>
> ```bash
> export PATH="$HOME/.local/bin:$PATH"
> source ~/.bashrc
> kiro-cli --version
> ```

> **확인 포인트 ✅**  
> `kiro-cli --version` 이 출력되면 Part 2 완료.

---

## Part 3. 프로젝트 초기화 (약 10분)

### 3-1. 프로젝트 폴더 및 명세 디렉터리 생성

Spec Kit 없이 SDD 폴더 구조를 직접 만든다.

```bash
cd /workspaces/login-page-lab
mkdir -p docs/specs/001-login-page/checklists
mkdir -p .kiro/steering
```

폴더 구조 확인:

```bash
ls -la
ls docs/specs/
ls .kiro/
```

예상 출력:

```
docs/
.kiro/
README.md
```

### 3-2. AWS Builder ID 로 Kiro CLI 인증

반드시 프로젝트 루트 폴더에서 실행한다.

```bash
kiro-cli
```

처음 실행 시 인증 요청 메시지가 나타난다.

```
To use Kiro CLI, you need to authenticate.
Press Enter to open the browser for authentication...
```

Enter 를 누르면 브라우저가 열리며 AWS Builder ID 로그인 페이지가 표시된다.

```
[Chrome 팝업이 차단된 경우]
주소창 오른쪽 팝업 차단 아이콘 클릭 → 팝업 허용
```

**AWS Builder ID 최초 가입 시**:

```
1. https://profile.aws.amazon.com 접속
2. Create an AWS Builder ID 클릭
3. 이메일 주소 입력
4. 이메일로 받은 인증 코드 입력
5. 이름, 비밀번호 설정
6. 가입 완료 후 Kiro CLI 인증 페이지에서 로그인
```

로그인이 완료되면 터미널에 채팅창이 열린다.

```
Kiro CLI  vX.X.X
Type /help for help, /exit to quit

>
```

> **두 개의 창 구분하기**  
> - 프롬프트가 `>` → Kiro CLI 채팅창 (AI 에게 요청)  
> - 프롬프트가 `$` → 터미널 (bash 명령어 실행)  
>
> 이후 실습에서 **Kiro 채팅창** 과 **터미널** 을 자주 전환한다.

> **확인 포인트 ✅**  
> Kiro 채팅창(`>` 프롬프트)이 열리면 Part 3 완료. 실습 시작 가능.

---

## Part 4. SDD 실습 1단계 — 기본 로그인 화면 (약 65분)

### 실습 목표

HTML + CSS + JavaScript 로 구성된 로그인 화면을 SDD 7단계를 거쳐 만든다.

---

### Step 1. Constitution — 프로젝트 원칙 설정

> **[SDD 기반 원칙]** — `.kiro/steering/constitution.md` 에 Steering 파일로 저장한다.  
> `inclusion: always` 설정으로 Kiro 가 **모든 채팅에서 자동으로 참조**한다.

**Kiro 채팅창** (`>` 프롬프트) 에 아래를 입력한다.

```
아래 내용으로 .kiro/steering/constitution.md 파일을 만들어줘.

---
inclusion: always
---

# 프로젝트 헌법 (Project Constitution)

## 프로젝트 설명
교육용 웹 로그인 화면 실습 프로젝트

## 개발 원칙
- HTML, CSS, JavaScript 만 사용한다 (외부 프레임워크 사용 금지)
- 하나의 index.html 파일로 구성한다
- CSS 는 <style> 태그 안에 작성한다
- JavaScript 는 <script> 태그 안에 작성한다
- CDN 링크나 외부 라이브러리를 사용하지 않는다
- 코드에 한국어 주석을 달아 초심자가 이해할 수 있도록 한다
- 모바일과 데스크탑 모두에서 보기 좋게 만든다 (반응형)
- Flexbox 를 사용하여 중앙 정렬한다
- 불필요하게 복잡한 구조는 피하고 단순하게 작성한다
- 모든 로그 및 AI 와의 대화는 한국어로 한다

## 명세 파일 관리 원칙
- 기능별 명세는 docs/specs/ 폴더에 번호 순서대로 저장한다
  예: docs/specs/001-login-page/spec.md
- 각 명세 폴더는 spec.md, plan.md, tasks.md, checklists/ 로 구성한다
- 기존 명세를 수정할 때는 spec.md 에 변경 이력을 기록한다

## 테스트 원칙
- 코드 구현 완료 후 반드시 테스트 체크리스트를 작성하고 검증한다
- 테스트는 브라우저에서 직접 시나리오를 실행하는 방식으로 진행한다 (자동화 테스트 도구 미사용)
- 각 수용 기준(Given-When-Then)에 대응하는 테스트 시나리오를 1:1 로 검증한다
- 정상 흐름(Happy Path)과 오류 흐름(Error Path)을 모두 테스트한다
- 모바일 화면(375px 기준)에서도 동작을 확인한다
- 개발 원칙 준수 여부(단일 파일, 한국어 주석, 외부 라이브러리 미사용)도 테스트 항목에 포함한다
```

Kiro 가 파일을 생성한 후 **터미널 새 탭**에서 확인한다.

```bash
cat .kiro/steering/constitution.md
```

예상 출력 (파일 첫 부분):

```markdown
---
inclusion: always
---

# 프로젝트 헌법 (Project Constitution)
...
```

> **`inclusion: always` 의 의미**  
> Kiro CLI 가 모든 채팅 세션을 시작할 때 이 파일을 자동으로 컨텍스트에 포함한다.  
> 따라서 개발 원칙과 테스트 원칙이 이후 모든 요청에 항상 반영된다.

> **확인 포인트 ✅**  
> `.kiro/steering/constitution.md` 에 `inclusion: always` 와 `[테스트 원칙]` 섹션 확인.

---

### Step 2. Specify 001 — 요구사항 정의 및 spec.md 작성

> **[SDD ①단계: Specify]**  
> Kiro 에게 요구사항을 설명하고 `spec.md` 를 작성하게 한다.

**Kiro 채팅창** 에 아래를 입력한다.

```
아래 요구사항을 분석하여 docs/specs/001-login-page/spec.md 파일을 작성해줘.
유저 스토리, 수용 기준(Given-When-Then), 엣지 케이스를 포함해서 작성해줘.

[기능 요구사항]
- 아이디 입력 필드가 있다
- 비밀번호 입력 필드가 있다 (입력 내용은 별표로 가려진다)
- 로그인 버튼이 있다
- 아이디 또는 비밀번호가 비어 있으면 "아이디와 비밀번호를 입력하세요" 메시지를 표시한다
- 아이디가 'admin', 비밀번호가 '1234' 이면 "로그인 성공!" 메시지를 표시한다
- 아이디나 비밀번호가 틀리면 "아이디 또는 비밀번호가 올바르지 않습니다" 메시지를 표시한다
- 비밀번호 보이기/숨기기 버튼이 있다
- Enter 키를 눌러도 로그인이 시도된다
```

**터미널** 에서 생성 여부를 확인한다.

```bash
cat docs/specs/001-login-page/spec.md
```

> spec.md 에 포함되어야 할 항목 예시:
> - 기능 개요
> - 유저 스토리 (사용자로서 나는 ~~를 원한다)
> - 수용 기준 (Given / When / Then)
> - 엣지 케이스
> - 비기능 요구사항 (반응형, 단일 파일 등)

---

### Step 3. Clarify — 모호한 부분 명확하게

> **[SDD ①단계: Specify 보완]**

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 를 아래 내용으로 보완해줘.
모호하거나 빠진 부분을 spec.md 에 추가하고, 변경한 내용을 알려줘.

[보완 사항]
- 화면 배경은 밝은 회색 계열로 한다
- 로그인 폼은 화면 정중앙에 카드 형태로 배치한다
- 오류 메시지는 빨간색으로 표시한다
- 성공 메시지는 초록색으로 표시한다
- 로그인 버튼은 파란색으로 한다
- 페이지 제목(h1)은 "로그인" 으로 한다
- Enter 키 입력 시 keydown 이벤트를 사용한다
```

---

### Step 4. Checklist — 명세 완성도 점검 (specify 후)

> **[SDD ①단계: Specify 최종 검증]** — `plan` 으로 넘어가기 전에 spec.md 를 점검한다.

#### 4-1. 기본 요구사항 체크리스트

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 를 아래 기준으로 점검하고,
결과를 docs/specs/001-login-page/checklists/requirements.md 에 저장해줘.

[점검 기준]
- 모든 기능 요구사항이 유저 스토리로 표현되어 있는가?
- 각 유저 스토리에 Given-When-Then 수용 기준이 있는가?
- 정상 흐름(Happy Path)과 오류 흐름(Error Path)이 모두 정의되어 있는가?
- 엣지 케이스(빈 입력, 잘못된 값 등)가 명시되어 있는가?
- 비기능 요구사항(반응형, 단일 파일 등)이 포함되어 있는가?

미통과 항목이 있으면 spec.md 를 어떻게 수정해야 하는지도 알려줘.
```

**터미널** 에서 저장 여부를 확인한다.

```bash
cat docs/specs/001-login-page/checklists/requirements.md
```

#### 4-2. UX 관점 체크리스트

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 를 UX 관점에서 점검하고,
결과를 docs/specs/001-login-page/checklists/ux.md 에 저장해줘.

[UX 점검 기준]
- 오류 메시지가 사용자가 이해하기 쉬운 언어로 작성되어 있는가?
- 포커스 이동 순서(아이디 → 비밀번호 → 버튼)가 정의되어 있는가?
- 버튼 비활성화 상태에 대한 시각적 피드백이 명시되어 있는가?
- 모바일 환경에서의 동작이 고려되어 있는가?
- 접근성(label, placeholder 등) 요구사항이 있는가?

빠진 UX 요구사항이 있으면 spec.md 에 추가할 내용도 알려줘.
```

```bash
cat docs/specs/001-login-page/checklists/ux.md
```

#### 4-3. 체크리스트 결과 처리

`[ ]` (미통과) 항목 발견 시:

```
1. Kiro 채팅창에서 수정 방법을 확인한다
   예) "CHK003 항목이 미통과야. spec.md 에 어떤 내용을 추가하면 돼?"

2. Kiro 에게 spec.md 보완을 요청한다
   예) "위 내용을 docs/specs/001-login-page/spec.md 에 추가해줘."

3. 체크리스트를 재실행한다
   예) "docs/specs/001-login-page/spec.md 를 UX 기준으로 다시 점검해줘."

4. 모든 항목 통과 → Step 5 로 진행
```

> **확인 포인트 ✅**  
> `checklists/requirements.md`, `checklists/ux.md` 생성 확인 → Step 5 로 진행.

---

### Step 5. Plan — 기술 설계

> **[SDD ②단계: Plan]**

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 를 바탕으로 기술 설계를 하고,
결과를 docs/specs/001-login-page/plan.md 에 저장해줘.

[설계 조건]
- 단일 HTML 파일(index.html) 하나로 모든 것을 구현한다
- CSS 는 <style> 태그 안에 작성한다
- JavaScript 는 <script> 태그 안에 작성한다
- 외부 라이브러리나 CDN 을 사용하지 않는다
- Flexbox 를 사용하여 중앙 정렬한다
- 비밀번호 보이기/숨기기는 input type 전환으로 구현한다
- Enter 키 처리는 keydown 이벤트로 구현한다

plan.md 에는 컴포넌트 구조, 주요 함수 목록, CSS 설계 전략을 포함해줘.
```

```bash
cat docs/specs/001-login-page/plan.md
```

---

### Step 6. Tasks — 작업 목록 생성

> **[SDD ③단계: Task]**

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 와 docs/specs/001-login-page/plan.md 를 바탕으로
구현 작업 목록을 만들고 docs/specs/001-login-page/tasks.md 에 저장해줘.

각 작업 항목은 아래 형식으로 작성해줘:
- [ ] TASK-001: (작업 설명) — 예상 소요 시간

HTML 구조 작성, CSS 스타일링, JavaScript 기능 구현 순서로 나눠줘.
```

```bash
cat docs/specs/001-login-page/tasks.md
```

---

### Step 7. Analyze — 최종 점검

> **[SDD ②단계: Plan 검증]**

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/ 폴더의 spec.md, plan.md, tasks.md 를 비교해서
아래를 확인해줘:

1. spec.md 의 모든 수용 기준이 plan.md 의 설계에 반영되어 있는가?
2. plan.md 의 모든 설계 항목이 tasks.md 의 작업 목록에 포함되어 있는가?
3. 누락되거나 충돌하는 항목이 있으면 알려줘.
4. 문제가 없으면 "일관성 점검 통과" 라고 알려줘.
```

"일관성 점검 통과" 메시지가 나오면 다음 단계로 진행한다.

---

### Step 8. Implement — 코드 생성

> **[SDD ④단계: Implement]**

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md, plan.md, tasks.md 와
.kiro/steering/constitution.md 의 개발 원칙을 모두 반영하여
index.html 파일을 현재 폴더에 생성해줘.

[중요 요구사항]
- 단일 index.html 파일로 HTML + CSS + JavaScript 를 모두 작성한다
- 모든 주요 함수와 CSS 섹션에 한국어 주석을 달아준다
- spec.md 의 모든 수용 기준이 구현되어야 한다
- 외부 라이브러리나 CDN 을 사용하지 않는다
```

생성 완료 후 **터미널** 에서 확인한다.

```bash
ls -la
# index.html 이 생성되어 있어야 한다

head -30 index.html
```

---

### Step 9. Checklist — 구현 품질 검증 (implement 후)

> **[품질 게이트]** — `.kiro/steering/constitution.md` 의 `[테스트 원칙]` 이  
> Kiro 에 의해 자동으로 로드되어, 체크리스트 생성 시 자동 반영된다.

**Kiro 채팅창** 에 아래를 입력한다.

```
생성된 index.html 이 아래 기준을 충족하는지 점검하고,
결과를 docs/specs/001-login-page/checklists/testing.md 에 저장해줘.

[점검 기준 1: spec.md 수용 기준 충족 여부]
- 빈 칸 제출 시 "아이디와 비밀번호를 입력하세요" 메시지가 표시되는가?
- admin/1234 로그인 시 "로그인 성공!" 초록색 메시지가 표시되는가?
- 틀린 정보 입력 시 "아이디 또는 비밀번호가 올바르지 않습니다" 빨간색 메시지가 표시되는가?
- 비밀번호 보이기/숨기기 버튼이 동작하는가?
- Enter 키로 로그인이 시도되는가?

[점검 기준 2: constitution.md 개발 원칙 준수 여부]
- CSS 가 <style> 태그 안에 작성되어 있는가?
- JavaScript 가 <script> 태그 안에 작성되어 있는가?
- CDN 링크나 외부 라이브러리가 없는가?
- 주요 함수에 한국어 주석이 달려 있는가?

[점검 기준 3: 반응형 검증]
- 브라우저 폭 375px 에서 로그인 카드가 잘리지 않는가?

누락되거나 구현되지 않은 기능이 있으면 알려줘.
```

```bash
cat docs/specs/001-login-page/checklists/testing.md
```

예상 출력 (일부):

```markdown
# 테스트 체크리스트: 기본 로그인 화면

## 수용 기준 검증 (spec.md 기반)

### 정상 흐름 (Happy Path)
- [ ] CHK001 - admin/1234 입력 → "로그인 성공!" 초록색 메시지가 표시되는가?

### 오류 흐름 (Error Path)
- [ ] CHK002 - 빈 칸 제출 → "아이디와 비밀번호를 입력하세요" 메시지가 표시되는가?
- [ ] CHK003 - 틀린 정보 → "아이디 또는 비밀번호가 올바르지 않습니다" 메시지가 표시되는가?

## 개발 원칙 준수 검증 (constitution.md 기반)
- [ ] CHK008 - CSS 가 <style> 태그 안에 작성되어 있는가?
- [ ] CHK009 - JavaScript 가 <script> 태그 안에 작성되어 있는가?
- [ ] CHK010 - CDN 링크나 외부 라이브러리가 없는가?
- [ ] CHK011 - 주요 함수에 한국어 주석이 달려 있는가?

## 반응형 검증
- [ ] CHK013 - 브라우저 폭 375px 에서 로그인 카드가 잘리지 않는가?
```

미통과 항목 발견 시 Kiro 에게 수정을 요청한다.

```
예) "index.html 에서 CHK011 이 미통과야. constitution.md 원칙에 맞게 한국어 주석을 추가해줘."
```

> **확인 포인트 ✅**  
> `index.html` 생성 확인, `checklists/testing.md` 생성 확인, 모든 항목 확인 완료 → Part 4 완료.

---

## Part 5. 결과물 실행 및 테스트 (약 15분)

### 5-1. 브라우저에서 열기

**방법 1: Live Server 확장 프로그램 (권장)**

1. VS Code 왼쪽 사이드바에서 확장 프로그램 아이콘을 클릭한다.
2. 검색창에 `Live Server` 를 입력한다.
3. `Live Server` (작성자: Ritwick Dey) 를 설치한다.
4. 파일 탐색기에서 `index.html` 을 우클릭한다.
5. `Open with Live Server` 를 선택한다.

**방법 2: Python 내장 서버**

```bash
python3 -m http.server 8080
```

VS Code 하단 `포트(PORTS)` 탭에서 8080 포트의 지구본 아이콘을 클릭한다.

### 5-2. 기본 동작 테스트

| 시나리오 | 아이디 | 비밀번호 | 예상 결과 |
|---|---|---|---|
| 빈 칸 제출 | (비워둠) | (비워둠) | "아이디와 비밀번호를 입력하세요" |
| 로그인 성공 | admin | 1234 | "로그인 성공!" (초록색) |
| 로그인 실패 | admin | 0000 | "아이디 또는 비밀번호가 올바르지 않습니다" (빨간색) |
| 비밀번호 보이기 | 아무 값 | 아무 값 | 비밀번호 텍스트가 보임 |
| Enter 키 로그인 | admin | 1234 | Enter 키로도 로그인 성공 |

### 5-3. 예상 결과물 화면 구성

```
+------------------------------------------+
|                                          |
|   +----------------------------------+   |
|   |          로그인                  |   |
|   |  아이디                          |   |
|   |  [                          ]    |   |
|   |  비밀번호                        |   |
|   |  [                        👁]    |   |
|   |  [        로그인 버튼        ]   |   |
|   |  ← 메시지 표시 영역              |   |
|   +----------------------------------+   |
|                                          |
+------------------------------------------+
  배경: 밝은 회색     카드: 흰색 + 그림자
```

---

## Part 6. SDD — 기존 명세(001) 변경 대처 (약 30분)

### 변경 시나리오

> **기획자 요청**: "로그인 성공 후 '로그인 성공!' 메시지를 2초 동안 보여준 뒤,  
> '환영합니다, admin 님!' 으로 교체하고 비밀번호 입력 필드를 초기화해 주세요."

### 나쁜 방법 vs 올바른 SDD 방법

```
[나쁜 방법 — 코드만 바꾸는 경우]
  index.html 수정 → 배포
  → spec.md 는 여전히 기존 내용 → 명세와 코드 불일치 → 기술 부채

[올바른 SDD 방법 — 명세부터 수정]
  spec.md 수정 → clarify → checklist → analyze → plan/tasks 수정 → implement
```

---

### Step 1. spec.md 직접 수정

**터미널** 에서 spec.md 를 연다.

```bash
nano docs/specs/001-login-page/spec.md
```

**수정할 부분 — 수용 기준 변경:**

```markdown
## 수용 기준 (변경됨)

Given: 사용자가 올바른 아이디(admin)와 비밀번호(1234)를 입력했을 때
When:  로그인 버튼을 클릭하면
Then:  "로그인 성공!" 초록색 메시지가 2초 동안 표시된다
Then:  2초 후 메시지가 "환영합니다, admin 님!" 으로 교체된다
Then:  비밀번호 입력 필드가 초기화된다 (아이디 필드는 유지)

## 변경 이력
- 2026-05-25: 로그인 성공 후 환영 메시지 및 필드 초기화 요구사항 추가 (기획팀 요청)
```

---

### Step 2. Clarify — 변경된 명세 보완

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 가 수정되었어.
로그인 성공 후 동작 변경에 관해 아래 내용을 spec.md 에 보완해줘.
변경한 내용을 알려줘.

[보완 사항]
- 2초 타이머는 setTimeout 을 사용한다
- 메시지 교체 시 애니메이션(페이드) 효과는 사용하지 않는다
- 비밀번호 필드만 초기화하고 아이디 필드는 그대로 둔다
- 타이머 중복 실행 방지: 이미 타이머가 동작 중이면 새 타이머를 무시한다
```

---

### Step 3. Checklist — 변경된 명세 점검

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 가 수정되었어.
변경된 "로그인 성공 후 동작" 부분을 중심으로 누락된 요구사항이 없는지 점검해줘.
결과를 docs/specs/001-login-page/checklists/requirements.md 에 업데이트해줘.
```

---

### Step 4. Analyze — 변경 영향 범위 탐지

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 가 수정되었어.
변경된 내용이 plan.md 및 tasks.md 와 일치하는지 분석해줘.
불일치하는 항목이 있으면 어떤 부분을 수정해야 하는지 알려줘.
```

---

### Step 5. Plan 재실행

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 가 수정되었어.
기존 index.html 에 다음 변경 사항을 추가하는 계획을 세우고
docs/specs/001-login-page/plan.md 를 업데이트해줘.

[변경 설계 조건]
- 로그인 성공 시 setTimeout(2000) 으로 2초 대기
- 2초 후 메시지를 "환영합니다, admin 님!" 으로 교체
- 비밀번호 입력 필드 값을 빈 문자열로 초기화
- 타이머 중복 실행 방지 (타이머 ID 변수 사용)
- 기존 코드 구조를 최대한 유지한다
```

---

### Step 6. Tasks 재실행

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/plan.md 가 업데이트되었어.
변경된 내용을 반영하여 docs/specs/001-login-page/tasks.md 를 업데이트해줘.
기존 Task 는 유지하고 변경에 필요한 Task 만 추가해줘.
```

---

### Step 7. Implement — 변경 코드 반영

**Kiro 채팅창** 에 아래를 입력한다.

```
docs/specs/001-login-page/spec.md 가 수정되었어.
기존 index.html 에 로그인 성공 후 2초 대기 + 메시지 교체 + 비밀번호 초기화 기능을 추가해줘.

[구현 조건]
- 기존 코드 구조는 최대한 유지한다
- 변경된 부분에는 한국어 주석으로 변경 이유를 달아준다
- constitution.md 의 개발 원칙을 준수한다
```

---

### 6-2. 변경 결과 확인 및 재검증

`Ctrl+Shift+R` 로 브라우저를 새로고침한다.

| 시나리오 | 예상 결과 |
|---|---|
| admin/1234 로 로그인 | "로그인 성공!" 표시 |
| 2초 대기 | "환영합니다, admin 님!" 으로 교체 |
| 비밀번호 필드 | 자동으로 비워짐 |
| 버튼 재클릭 (2초 내) | 타이머 중복 실행 없음 |

**Kiro 채팅창** 에서 변경분 재검증을 실행한다.

```
docs/specs/001-login-page/spec.md 가 수정되었어.
변경된 "로그인 성공 후 동작" 부분을 중심으로
constitution.md 의 [테스트 원칙] 을 반영하여 testing.md 를 업데이트해줘.
기존 항목은 유지하고 새 항목만 추가해줘.
결과를 docs/specs/001-login-page/checklists/testing.md 에 저장해줘.
```

### 6-3. Git 커밋으로 변경 이력 남기기

```bash
git add docs/specs/001-login-page/spec.md
git add docs/specs/001-login-page/plan.md
git add docs/specs/001-login-page/tasks.md
git add docs/specs/001-login-page/checklists/
git add index.html
git commit -m "feat(001): 로그인 성공 후 환영 메시지 + 필드 초기화 추가

- spec.md: 2초 후 메시지 교체 수용 기준 추가
- plan.md: setTimeout 로직 구현 계획 추가
- tasks.md: 관련 Task 항목 추가
- index.html: setTimeout, 메시지 교체, 비밀번호 초기화 구현
변경 요청: 기획팀 (2026-05-25)"
```

> **확인 포인트 ✅**  
> 변경된 동작 테스트 통과, Git 커밋 완료 → Part 6 완료.

---

## Part 7. SDD 실습 2단계 — 로그인 횟수 제한 추가 (약 30분)

```
지금부터: specify(002) → clarify → checklist(ux+security) → plan → tasks → analyze → implement
```

---

### Step 1. Specify 002 — 요구사항 정의 및 spec.md 작성

**먼저 터미널에서 폴더를 생성한다.**

```bash
mkdir -p docs/specs/002-login-attempts/checklists
```

**Kiro 채팅창** 에 아래를 입력한다.

```
아래 요구사항을 분석하여 docs/specs/002-login-attempts/spec.md 파일을 작성해줘.
유저 스토리, 수용 기준(Given-When-Then), 엣지 케이스를 포함해서 작성해줘.

[기능 요구사항]
- 로그인 실패 횟수를 화면에 표시한다 (예: "로그인 시도: 1/3")
- 로그인 실패를 3회 반복하면 로그인 버튼을 비활성화한다
- 버튼이 비활성화되면 "5초 후 다시 시도하세요" 메시지를 표시한다
- 5초가 지나면 실패 횟수가 초기화되고 버튼이 다시 활성화된다
- 카운트다운 숫자가 화면에 표시된다 (5, 4, 3, 2, 1)
- 이 기능은 기존 index.html 에 추가된다
```

```bash
ls docs/specs/
# 예상 출력: 001-login-page/   002-login-attempts/

cat docs/specs/002-login-attempts/spec.md
```

---

### Step 2. Clarify

```
docs/specs/002-login-attempts/spec.md 를 아래 내용으로 보완해줘.

[보완 사항]
- 실패 횟수 표시는 로그인 버튼 위에 작게 표시한다
- 버튼 비활성화 상태에서는 버튼 색상이 회색으로 바뀐다
- 카운트다운 메시지는 빨간색으로 표시한다
- 로그인 성공 시에는 실패 횟수가 초기화된다
- 페이지를 새로고침하면 횟수가 초기화된다 (세션 유지 불필요)
```

---

### Step 3. Checklist (UX + 보안)

**UX 체크리스트:**

```
docs/specs/002-login-attempts/spec.md 를 UX 관점에서 점검하고,
결과를 docs/specs/002-login-attempts/checklists/ux.md 에 저장해줘.

로그인 횟수 제한 기능에서 특히 아래를 확인해줘:
- 실패 횟수 표시 위치와 방식이 명확한가?
- 버튼 비활성화 상태의 시각적 피드백이 정의되어 있는가?
- 카운트다운 표시 방식이 사용자가 이해하기 쉬운가?
```

```bash
cat docs/specs/002-login-attempts/checklists/ux.md
```

**보안 체크리스트:**

```
docs/specs/002-login-attempts/spec.md 를 보안 관점에서 점검하고,
결과를 docs/specs/002-login-attempts/checklists/security.md 에 저장해줘.

아래를 중심으로 확인해줘:
- 횟수 초기화 조건이 명확하게 정의되어 있는가?
- 타이머 우회(새로고침 등)에 대한 방침이 명시되어 있는가?
- 클라이언트 측 검증의 한계가 문서화되어 있는가?
```

```bash
cat docs/specs/002-login-attempts/checklists/security.md
```

---

### Step 4. Plan

```
docs/specs/002-login-attempts/spec.md 를 바탕으로 기술 설계를 하고,
결과를 docs/specs/002-login-attempts/plan.md 에 저장해줘.

[설계 조건]
- 기존 index.html 파일을 수정하여 기능을 추가한다
- JavaScript 의 setInterval 을 사용하여 카운트다운을 구현한다
- 실패 횟수는 JavaScript 변수로 관리한다 (localStorage 미사용)
- 별도 파일을 만들지 않고 index.html 안에서 모두 처리한다
- 기존 코드 구조를 최대한 유지한다
```

---

### Step 5. Tasks

```
docs/specs/002-login-attempts/spec.md 와 plan.md 를 바탕으로
구현 작업 목록을 만들고 docs/specs/002-login-attempts/tasks.md 에 저장해줘.
```

---

### Step 6. Analyze

```
docs/specs/002-login-attempts/ 폴더의 spec.md, plan.md, tasks.md 를 비교해서
일관성을 점검해줘.
누락되거나 충돌하는 항목이 있으면 알려줘.
문제가 없으면 "일관성 점검 통과" 라고 알려줘.
```

---

### Step 7. Implement

```
docs/specs/002-login-attempts/spec.md, plan.md, tasks.md 와
.kiro/steering/constitution.md 의 개발 원칙을 반영하여
기존 index.html 에 로그인 횟수 제한 기능을 추가해줘.

[구현 조건]
- 기존 코드 구조를 최대한 유지한다
- 새로 추가된 코드에는 한국어 주석을 달아준다
- constitution.md 의 개발 원칙을 준수한다
```

---

### 7-2. 동작 테스트 + 재검증

`Ctrl+Shift+R` 로 브라우저를 새로고침한다.

| 시나리오 | 예상 결과 |
|---|---|
| 틀린 비밀번호 1회 | "로그인 시도: 1/3" 표시 |
| 틀린 비밀번호 3회 | 버튼 비활성화(회색), 카운트다운 시작 |
| 5초 대기 | 버튼 재활성화(파란색), 횟수 초기화 |
| 올바른 정보 | "로그인 성공!" → 2초 후 환영 메시지 (Part 6 유지) |

**Kiro 채팅창** 에서 검증을 실행한다.

```
횟수 제한 기능(002)이 구현된 index.html 을
constitution.md 의 [테스트 원칙] 과 docs/specs/002-login-attempts/spec.md 의 수용 기준에 맞게 검증해줘.
결과를 docs/specs/002-login-attempts/checklists/testing.md 에 저장해줘.
```

```bash
cat docs/specs/002-login-attempts/checklists/testing.md
```

> **확인 포인트 ✅** `checklists/testing.md` 생성 확인, 모든 항목 확인 완료 → Part 7 완료.

---

## Part 8. SDD 실습 3단계 — 아이디 저장 기능 추가 (약 30분)

### Step 1. Specify 003 — 요구사항 정의 및 spec.md 작성

**먼저 터미널에서 폴더를 생성한다.**

```bash
mkdir -p docs/specs/003-save-id/checklists
```

**Kiro 채팅창** 에 아래를 입력한다.

```
아래 요구사항을 분석하여 docs/specs/003-save-id/spec.md 파일을 작성해줘.
유저 스토리, 수용 기준(Given-When-Then), 엣지 케이스를 포함해서 작성해줘.

[기능 요구사항]
- 아이디 입력 필드 아래에 "아이디 저장" 체크박스가 있다
- 체크박스를 체크하고 로그인 성공하면 아이디가 브라우저에 저장된다
- 다음에 페이지를 열면 저장된 아이디가 자동으로 입력 필드에 채워진다
- 저장된 아이디가 있으면 체크박스도 자동으로 체크된 상태로 표시된다
- 체크박스를 해제하고 로그인하면 저장된 아이디가 삭제된다
- 이 기능은 기존 index.html 에 추가된다
```

```bash
ls docs/specs/
# 예상 출력: 001-login-page/   002-login-attempts/   003-save-id/
```

---

### Step 2. Clarify

```
docs/specs/003-save-id/spec.md 를 아래 내용으로 보완해줘.

[보완 사항]
- 아이디 저장에는 localStorage 를 사용한다
- 저장 키 이름은 'saved_username' 으로 한다
- 비밀번호는 저장하지 않는다
- 체크박스는 아이디 입력 필드와 비밀번호 입력 필드 사이에 배치한다
- 저장된 아이디가 있을 때는 비밀번호 입력 필드에 자동으로 포커스가 이동한다
```

---

### Step 3. Checklist (보안 + UX)

**보안 체크리스트:**

```
docs/specs/003-save-id/spec.md 를 보안 관점에서 점검하고,
결과를 docs/specs/003-save-id/checklists/security.md 에 저장해줘.

아래를 중심으로 확인해줘:
- localStorage 사용에 대한 보안 고려사항이 명시되어 있는가?
- 비밀번호 미저장 원칙이 명확하게 정의되어 있는가?
- 저장 데이터 삭제 조건이 모두 명시되어 있는가?
```

```bash
cat docs/specs/003-save-id/checklists/security.md
```

**UX 체크리스트:**

```
docs/specs/003-save-id/spec.md 를 UX 관점에서 점검하고,
결과를 docs/specs/003-save-id/checklists/ux.md 에 저장해줘.

아래를 중심으로 확인해줘:
- 체크박스 위치와 레이블이 명확하게 정의되어 있는가?
- 자동 입력 동작(포커스 이동 등)이 상세하게 정의되어 있는가?
- 저장된 아이디가 없을 때의 동작이 정의되어 있는가?
```

```bash
cat docs/specs/003-save-id/checklists/ux.md
```

---

### Step 4. Plan

```
docs/specs/003-save-id/spec.md 를 바탕으로 기술 설계를 하고,
결과를 docs/specs/003-save-id/plan.md 에 저장해줘.

[설계 조건]
- localStorage 를 사용하여 아이디를 브라우저에 저장한다
- 저장 키 이름은 'saved_username' 으로 한다
- 페이지 로드 시 localStorage 를 확인하여 저장된 아이디를 불러온다
- 별도 파일 없이 index.html 안에서 처리한다
- 기존 코드 구조를 최대한 유지한다
```

---

### Step 5. Tasks

```
docs/specs/003-save-id/spec.md 와 plan.md 를 바탕으로
구현 작업 목록을 만들고 docs/specs/003-save-id/tasks.md 에 저장해줘.
```

---

### Step 6. Analyze

```
docs/specs/003-save-id/ 폴더의 spec.md, plan.md, tasks.md 를 비교해서
일관성을 점검해줘.
누락되거나 충돌하는 항목이 있으면 알려줘.
문제가 없으면 "일관성 점검 통과" 라고 알려줘.
```

---

### Step 7. Implement

```
docs/specs/003-save-id/spec.md, plan.md, tasks.md 와
.kiro/steering/constitution.md 의 개발 원칙을 반영하여
기존 index.html 에 아이디 저장 기능을 추가해줘.

[구현 조건]
- 기존 코드 구조를 최대한 유지한다
- 새로 추가된 코드에는 한국어 주석을 달아준다
- constitution.md 의 개발 원칙을 준수한다
- 비밀번호는 절대 저장하지 않는다
```

---

### 8-2. 동작 테스트 + 재검증

`Ctrl+Shift+R` 로 브라우저를 새로고침한다.

| 시나리오 | 예상 결과 |
|---|---|
| 체크박스 체크 후 로그인 성공 | localStorage 에 아이디 저장 |
| 페이지 새로고침 | 아이디 자동 입력, 체크박스 자동 체크 |
| 저장된 상태에서 비밀번호 필드 | 자동으로 포커스 이동 |
| 체크박스 해제 후 로그인 | localStorage 에서 아이디 삭제 |

**localStorage 저장 확인** (브라우저 개발자 도구):

```
F12 → Application 탭 → Local Storage → 'saved_username' 키 확인
```

**Kiro 채팅창** 에서 검증을 실행한다.

```
아이디 저장 기능(003)이 구현된 index.html 을
constitution.md 의 [테스트 원칙] 과 docs/specs/003-save-id/spec.md 의 수용 기준에 맞게 검증해줘.
결과를 docs/specs/003-save-id/checklists/testing.md 에 저장해줘.
```

> **확인 포인트 ✅** `checklists/testing.md` 생성 확인, 모든 항목 확인 완료 → Part 8 완료.

---

## Part 9. 전체 산출물 구조 확인

실습이 모두 끝나면 **터미널** 에서 전체 구조를 확인한다.

```bash
find . -not -path '*/.git/*' | sort
```

예상 디렉터리 구조:

```
./
├── .kiro/
│   └── steering/
│       └── constitution.md          ← inclusion: always (항상 자동 로드)
├── docs/
│   └── specs/
│       ├── 001-login-page/
│       │   ├── spec.md
│       │   ├── plan.md
│       │   ├── tasks.md
│       │   └── checklists/
│       │       ├── requirements.md
│       │       ├── ux.md
│       │       └── testing.md
│       ├── 002-login-attempts/
│       │   ├── spec.md
│       │   ├── plan.md
│       │   ├── tasks.md
│       │   └── checklists/
│       │       ├── ux.md
│       │       ├── security.md
│       │       └── testing.md
│       └── 003-save-id/
│           ├── spec.md
│           ├── plan.md
│           ├── tasks.md
│           └── checklists/
│               ├── security.md
│               ├── ux.md
│               └── testing.md
├── index.html
└── README.md
```

### .kiro/steering/constitution.md 최종 확인

```bash
cat .kiro/steering/constitution.md
```

반드시 아래 내용이 포함되어 있어야 한다.

```
✅ frontmatter: inclusion: always
✅ [개발 원칙] 섹션
✅ [테스트 원칙] 섹션
✅ [명세 파일 관리 원칙] 섹션
```

### 최종 완성 기능 전체 테스트

```
기본 로그인 (001 — Part 6 수정 포함)
[ ] 빈 칸 제출 → 안내 메시지
[ ] admin/1234 → "로그인 성공!" → 2초 후 환영 메시지
[ ] 비밀번호 필드 자동 초기화 (아이디 유지)
[ ] 틀린 정보 → 오류 메시지
[ ] 비밀번호 보이기/숨기기
[ ] Enter 키 로그인

로그인 횟수 제한 (002)
[ ] 실패 시 "로그인 시도: N/3" 표시
[ ] 3회 실패 → 버튼 비활성화 (회색)
[ ] 카운트다운 5→1
[ ] 5초 후 재활성화

아이디 저장 (003)
[ ] 체크박스 체크 + 로그인 성공 → 아이디 저장
[ ] 새로고침 → 아이디 자동 입력, 체크박스 자동 체크
[ ] 체크박스 해제 + 로그인 → 아이디 삭제
```

> **확인 포인트 ✅ — 실습 완주 기준**
> 1. 각 기능 폴더에 `checklists/testing.md` 파일이 존재한다
> 2. `.kiro/steering/constitution.md` 에 `inclusion: always` 와 `[테스트 원칙]` 이 있다
> 3. `001/spec.md` 에 Part 6 의 변경 이력이 기록되어 있다
> 4. `docs/specs/` 에 001, 002, 003 세 폴더가 모두 있다

---

## 실습 종료 후 Codespace 정지 방법

**방법 1 (VS Code 화면에서)**

화면 왼쪽 하단의 `>< Codespaces` → `Stop Current Codespace`.

**방법 2 (GitHub 웹에서)**

https://github.com/codespaces → 본인 Codespace 오른쪽 `...` → `Stop codespace`.

---

## 트러블슈팅

### kiro-cli: command not found

PATH 가 업데이트되지 않은 것이다.

```bash
export PATH="$HOME/.local/bin:$PATH"
source ~/.bashrc
kiro-cli --version
```

그래도 안 되면 재설치한다.

```bash
curl --proto '=https' --tlsv1.2 -sSf \
  'https://desktop-release.q.us-east-1.amazonaws.com/latest/kirocli-x86_64-linux.zip' \
  -o kirocli.zip
unzip -o kirocli.zip
sudo ./install.sh
source ~/.bashrc
```

---

### Kiro CLI 인증 팝업이 뜨지 않는다

Chrome 팝업 차단 아이콘 → **팝업 허용** 으로 변경한다.  
이후 `kiro-cli` 를 다시 실행한다.

---

### AWS Builder ID 가입이 안 된다

https://profile.aws.amazon.com 에서 직접 가입한다.  
이메일 인증이 필요하다. 스팸 폴더도 확인한다.

---

### Kiro 가 파일을 생성하지 않고 채팅으로만 답한다

파일 생성을 명시적으로 요청한다.

```
예) "위 내용을 docs/specs/001-login-page/spec.md 파일로 저장해줘.
    파일이 없으면 생성하고, 있으면 덮어써줘."
```

---

### Kiro 가 기존 코드를 무시하고 처음부터 다시 작성한다

기존 파일을 유지하라고 명시적으로 요청한다.

```
예) "기존 index.html 의 코드 구조를 최대한 유지하면서
    로그인 횟수 제한 기능만 추가해줘.
    기존 기능(로그인 성공 후 환영 메시지 등)은 그대로 남겨둬."
```

---

### constitution.md 의 원칙이 반영되지 않는 것 같다

`inclusion: always` 설정이 있는지 확인한다.

```bash
head -5 .kiro/steering/constitution.md
```

`inclusion: always` 가 없으면 추가한다.

```bash
nano .kiro/steering/constitution.md
```

첫 3줄이 다음과 같아야 한다.

```
---
inclusion: always
---
```

이후 Kiro CLI 를 재시작한다.

```bash
/exit
kiro-cli
```

---

### spec.md 수정 후 plan.md 가 자동으로 바뀌지 않는다

Kiro 는 파일을 자동으로 수정하지 않는다.  
반드시 채팅에서 직접 업데이트를 요청해야 한다.

```
[올바른 순서]
spec.md 수정 → Kiro 에게 analyze 요청 → Kiro 에게 plan 업데이트 요청 → Kiro 에게 tasks 업데이트 요청
```

```
예) "docs/specs/001-login-page/spec.md 가 수정되었어.
    변경된 내용을 반영하여 plan.md 를 업데이트해줘."
```

---

### 브라우저에서 화면이 열리지 않는다 (Codespace 포트 포워딩)

VS Code 하단 `포트(PORTS)` 탭 → 8080 포트 지구본 클릭.  
목록에 없으면 `포트 추가` → `8080` 입력.  
가시성이 `Private` 이면 `Public` 으로 변경.

---

### index.html 이 생성되지 않았다

Kiro 채팅창에서 직접 명시적으로 요청한다.

```
docs/specs/001-login-page/ 폴더의 spec.md, plan.md, tasks.md 를 참고해서
index.html 을 현재 프로젝트 루트 폴더(/workspaces/login-page-lab/)에 생성해줘.
constitution.md 의 개발 원칙을 반드시 지켜줘.
```

---

## 전체 명령어 요약표

### 터미널 명령어

| 단계 | 명령어 | 설명 |
|---|---|---|
| Kiro CLI 설치 | `curl ... \| sh && sudo ./install.sh` | (위 2-1절 참조) |
| 인증 및 실행 | `kiro-cli` | AWS Builder ID 로 인증 후 채팅창 실행 |
| 폴더 생성 | `mkdir -p docs/specs/001-login-page/checklists` | 명세 디렉터리 생성 |
| 파일 확인 | `cat docs/specs/001-login-page/spec.md` | 명세 파일 내용 확인 |
| 구조 확인 | `find . -not -path '*/.git/*' \| sort` | 전체 산출물 구조 확인 |
| 서버 실행 | `python3 -m http.server 8080` | 브라우저 미리보기 서버 |
| 강제 새로고침 | `Ctrl+Shift+R` (브라우저) | 변경된 파일 반영 |
| Git 커밋 | `git add . && git commit -m "..."` | 변경 이력 저장 |

### Kiro 채팅 요청 패턴

| 단계 | 요청 패턴 |
|---|---|
| Constitution (1회) | "아래 내용으로 .kiro/steering/constitution.md 를 만들어줘." |
| Specify | "아래 요구사항으로 docs/specs/001-login-page/spec.md 를 작성해줘. 유저 스토리와 수용 기준 포함." |
| Clarify | "spec.md 를 아래 내용으로 보완해줘. 변경한 내용을 알려줘." |
| Checklist | "spec.md 를 [기준] 관점으로 점검하고 결과를 checklists/requirements.md 에 저장해줘." |
| Plan | "spec.md 를 바탕으로 기술 설계를 하고 plan.md 에 저장해줘." |
| Tasks | "spec.md 와 plan.md 를 바탕으로 작업 목록을 만들고 tasks.md 에 저장해줘." |
| Analyze | "spec.md, plan.md, tasks.md 를 비교해서 일관성을 점검해줘." |
| Implement | "spec.md, plan.md, tasks.md 와 constitution.md 를 반영하여 index.html 을 생성해줘." |
| 재검증 | "index.html 이 spec.md 의 수용 기준을 충족하는지 검증하고 testing.md 에 저장해줘." |

### 명세 변경 시 Kiro 채팅 순서

```
spec.md 직접 수정 (터미널 또는 에디터)
    → "spec.md 가 수정되었어. 변경 내용을 보완해줘." (clarify)
    → "변경된 spec.md 의 누락 항목을 점검해줘." (checklist)
    → "spec.md 와 plan.md 의 일관성을 점검해줘." (analyze)
    → "변경된 spec.md 를 반영하여 plan.md 를 업데이트해줘." (plan)
    → "변경된 plan.md 를 반영하여 tasks.md 를 업데이트해줘." (tasks)
    → "변경 사항을 index.html 에 반영해줘." (implement)
    → git commit (명세+코드 함께 커밋)
```

---

## 실습 진행 시간 배분

| 파트 | 내용 | 시간 |
|---|---|---|
| Part 1 | Codespace 생성 | 15분 |
| Part 2 | Kiro CLI 설치 및 인증 | 15분 |
| Part 3 | 프로젝트 초기화 (폴더 생성 + AWS Builder ID 인증) | 10분 |
| Part 4 | 기본 로그인 화면 구현 (SDD 7단계) | 65분 |
| Part 5 | 결과물 실행 및 테스트 | 15분 |
| Part 6 | 기존 명세 변경 대처 | 30분 |
| Part 7 | 로그인 횟수 제한 추가 | 30분 |
| Part 8 | 아이디 저장 기능 추가 | 30분 |
| Part 9 | 전체 구조 확인 및 완료 테스트 | 10분 |
| **합계** | | **약 3시간 40분** |

> **강사 운영 팁**  
> **AWS Builder ID 가입은 실습 전날** 학생들에게 미리 안내한다.  
> 이메일 인증이 필요하므로 당일 처음 가입하면 시간이 지체될 수 있다.
>
> Part 4 Step 2(Specify) 에서 Kiro 가 spec.md 를 처음 생성할 때  
> 학생들이 "어디에 파일이 생겼지?" 하고 헤매는 경우가 많다.  
> 강사가 터미널에서 `cat docs/specs/001-login-page/spec.md` 를 시범 실행하면 도움이 된다.
>
> Part 6·7·8 은 시간이 부족하면 과제로 전환 가능하다.

---

*본 실습 자료는 Kiro CLI 공식 문서를 기반으로 작성되었습니다.*  
*참고: https://kiro.dev/docs/cli/*  
*Kiro Steering 파일 규격: https://kiro.dev/docs/steering/*  
*명령어 기준: Kiro CLI 최신 버전 / Kiro Steering 파일 형식 (`.kiro/steering/*.md`)*
