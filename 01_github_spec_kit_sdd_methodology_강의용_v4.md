# SDD(Specification-Driven Development) 방법론

## 목차

1. [시대적 배경: AI가 코딩 방식을 바꾸다](#1-시대적-배경-ai가-코딩-방식을-바꾸다)
2. [AI 코딩의 현실](#2-ai-코딩의-현실)
   - 2.1 ["그냥 시키면 되지 않나?" — 자유 형식 지시의 한계](#21-그냥-시키면-되지-않나--자유-형식-지시의-한계)
   - 2.2 ["더 많이 알려주면 되지 않나?" — 정보 과부하의 역설](#22-더-많이-알려주면-되지-않나--정보-과부하의-역설)
   - 2.3 [진짜 문제: 커뮤니케이션 구조의 부재](#23-진짜-문제-커뮤니케이션-구조의-부재)
3. [SDD란 무엇인가?](#3-sdd란-무엇인가)
   - 3.1 [SDD의 정의와 등장 배경](#31-sdd의-정의와-등장-배경)
   - 3.2 [기존 개발 방식과 SDD의 차이](#32-기존-개발-방식과-sdd의-차이)
   - 3.3 [SDD의 3가지 구현 레벨](#33-sdd의-3가지-구현-레벨)
   - 3.4 [스펙과 Memory Bank: 두 가지 컨텍스트의 역할](#34-스펙과-memory-bank-두-가지-컨텍스트의-역할)
4. [SDD의 4가지 핵심 원칙](#4-sdd의-4가지-핵심-원칙)
5. [Less is More: 스펙 설계 전략](#5-less-is-more-스펙-설계-전략)
   - 5.1 [Less is More 원칙이란?](#51-less-is-more-원칙이란)
   - 5.2 [계층적 스펙 구조](#52-계층적-스펙-구조)
   - 5.3 [스펙 작성 실전 가이드: 좋은 스펙과 나쁜 스펙](#53-스펙-작성-실전-가이드-좋은-스펙과-나쁜-스펙)
6. [SDD 4단계 워크플로](#6-sdd-4단계-워크플로)
7. [실전 도구 비교](#7-실전-도구-비교)
   - 7.1 [AWS의 Kiro](#71-aws의-kiro)
   - 7.2 [GitHub Spec Kit](#72-github-spec-kit)
   - 7.3 [Tessl Framework](#73-tessl-framework)
   - 7.4 [Böckeler가 던진 핵심 질문들](#74-böckeler가-던진-핵심-질문들)
8. [엔터프라이즈 레거시 전환에서의 SDD 적용](#8-엔터프라이즈-레거시-전환에서의-sdd-적용)
   - 8.1 [엔터프라이즈 환경에서 SDD가 중요한 이유](#81-엔터프라이즈-환경에서-sdd가-중요한-이유)
   - 8.2 [왜 레거시 전환에 SDD가 필요한가?](#82-왜-레거시-전환에-sdd가-필요한가)
   - 8.3 [기존 산출물에서 스펙으로: 핵심 정보 압축의 비밀](#83-기존-산출물에서-스펙으로-핵심-정보-압축의-비밀)
   - 8.4 [수백 명 규모 프로젝트에서의 SDD 운영](#84-수백-명-규모-프로젝트에서의-sdd-운영)
   - 8.5 [점진적 전환 전략: Big Bang은 실패한다](#85-점진적-전환-전략-big-bang은-실패한다)
9. [SDD와 AI-Native 설계 패러다임의 연결](#9-sdd와-ai-native-설계-패러다임의-연결)
   - 9.1 [AI-Native 설계 패러다임이란?](#91-ai-native-설계-패러다임이란)
   - 9.2 [SDD가 AI-Native를 가능하게 하는 방법](#92-sdd가-ai-native를-가능하게-하는-방법)
   - 9.3 [AI 능력과 스펙의 가치: 비례 관계](#93-ai-능력과-스펙의-가치-비례-관계)
   - 9.4 [SDD의 위치: 현재와 미래 사이의 다리](#94-sdd의-위치-현재와-미래-사이의-다리)
10. [정리](#10-정리)

---

## 1. 시대적 배경: AI가 코딩 방식을 바꾸다

SDD를 이해하려면 먼저 AI 시대에 개발 방식이 어떻게 변화해 왔는지를 살펴볼 필요가 있습니다.

---

### 바이브 코딩(Vibe Coding)의 등장

2025년 2월, OpenAI 공동 창업자이자 전 Tesla AI 총괄 책임자인 **안드레이 카파시(Andrej Karpathy)** 는 소셜미디어(X)에 짧은 글을 올렸습니다.

> *"There's a new kind of coding I call 'vibe coding', where you fully give in to the vibes, embrace exponentials, and forget that the code even exists."*
>
> "나는 이것을 '바이브 코딩'이라고 부른다. 분위기에 완전히 몸을 맡기고, 지수적 성장을 받아들이며, 코드가 존재한다는 사실조차 잊어버리는 것이다."

이 글은 전 세계 개발자 커뮤니티에 즉각적인 반향을 일으켰습니다. 바이브 코딩이란, 개발자가 AI에게 자연어로 원하는 것을 말하면 AI가 코드를 생성해 주는 방식입니다. 개발자는 코드를 한 줄씩 작성하는 대신, **무엇을 만들지(What)** 를 설명하고 AI가 **어떻게 만들지(How)** 를 결정하는 구조입니다.

```
[기존 방식]   개발자 → 직접 코드 작성 → 실행
[바이브 코딩] 개발자 → 자연어 지시 → AI가 코드 생성 → 실행
```

초기에는 주로 개인 프로젝트나 프로토타이핑에 활용되었습니다. 복잡한 기술 지식 없이도 동작하는 소프트웨어를 빠르게 만들 수 있다는 점에서 큰 인기를 끌었습니다. 실제로 Y Combinator의 2025년 동계 배치(Winter 2025)에 참여한 스타트업 중 21%가 전체 코드베이스의 91% 이상을 AI로 생성했다고 알려져 있습니다.

> **💡 핵심 개념**
>
> 바이브 코딩은 AI가 코딩의 진입 장벽을 획기적으로 낮춰준 혁신이었습니다. 비개발자도 소프트웨어를 만들 수 있게 되었고, 개발자들은 반복적인 코드 작성에서 해방되기 시작했습니다.

---

### 바이브 코딩의 한계

그러나 바이브 코딩이 빠르게 확산되면서 한계도 분명하게 드러났습니다.

**문제 1: 품질과 일관성 부족**
AI가 생성한 코드가 당장 동작하더라도, 전체 시스템과의 정합성이나 장기적인 유지보수를 고려하지 않는 경우가 많았습니다.

**문제 2: 의도와 구현 사이의 간극**
개발자가 AI에게 내린 지시가 모호하면, AI는 합리적이지만 의도와 전혀 다른 코드를 생성합니다.

**문제 3: 추적 불가능한 코드베이스**
바이브 코딩으로 빠르게 생성된 코드는 왜 이런 구조로 만들어졌는지 근거가 없었습니다.

**문제 4: 프로덕션 수준에 미치지 못하는 품질**
보안, 성능, 규정 준수 등 엔터프라이즈 환경의 요구 사항을 만족시키기 위해서는 단순히 '동작하는 코드' 이상이 필요했습니다.

---

### 에이전틱 엔지니어링(Agentic Engineering)으로의 진화

2026년 초, 카파시는 바이브 코딩의 후계 개념으로 **에이전틱 엔지니어링(Agentic Engineering)** 을 제시했습니다.

> *"'Agentic' because the new default is that you are not writing the code directly 99% of the time, you are orchestrating agents who do and acting as oversight."*

| 구분 | 바이브 코딩 (2025) | 에이전틱 엔지니어링 (2026~) |
|------|-------------------|--------------------------|
| 주요 사용자 | 비개발자, 초보자, 빠른 프로토타이핑 | 전문 개발자, 엔터프라이즈 팀 |
| AI의 역할 | 코드 생성기 | 자율 에이전트 |
| 개발자의 역할 | 지시자 | 조율자(Orchestrator) + 감독자(Oversight) |
| 품질 기준 | 일단 동작하면 성공 | 프로덕션 품질 유지 |
| 핵심 스킬 | 자연어 지시 | 구조 설계 + 에이전트 관리 |

---

### Software 3.0과 SDD의 연결고리

카파시는 이러한 흐름을 **Software 3.0** 시대로 설명합니다.

```
Software 1.0  →  개발자가 직접 명시적인 코드 작성
Software 2.0  →  데이터와 학습을 통해 모델이 동작 결정 (머신러닝)
Software 3.0  →  자연어 지시로 AI가 소프트웨어 생성 및 운영
```

> **🔑 핵심 메시지**
>
> 바이브 코딩이 AI 시대의 가능성을 열었다면, 에이전틱 엔지니어링은 그 가능성을 프로덕션 수준의 품질로 끌어올리는 방법론입니다. 그리고 SDD는 에이전틱 엔지니어링을 실천하기 위한 구체적인 프레임워크입니다.

---

## 2. AI 코딩의 현실

AI 코딩의 진짜 문제가 무엇인지를 살펴봅니다.

---

### 2.1 "그냥 시키면 되지 않나?" — 자유 형식 지시의 한계

> **"그냥 말로 시키면 되는 거 아닌가?"**

이 직관은 절반은 맞고 절반은 틀립니다. **자유 형식(Free-form) 지시만으로는 일관된 결과를 보장할 수 없습니다.**

```
개발자: "로그인 기능 만들어 줘."

AI가 가정해야 하는 것들:
├── 어떤 인증 방식? (JWT? 세션? OAuth?)
├── 어떤 데이터베이스? (MySQL? PostgreSQL? MongoDB?)
├── 비밀번호 해싱 방식? (bcrypt? SHA-256?)
├── 실패 시 처리? (잠금? 재시도 제한?)
├── 어떤 프레임워크? (FastAPI? Django? Express?)
└── 어떤 보안 수준? (금융급? 일반 웹?)
```

AI는 이 모든 것을 **스스로 가정(Assumption)** 합니다.

#### 반복적인 수정 루프

```
[1회] "로그인 기능 만들어 줘"
      → AI가 세션 기반으로 구현 (JWT를 원했는데)

[2회] "JWT로 바꿔줘"
      → AI가 refresh token 없이 구현

[3회] "refresh token도 추가해줘"
      → AI가 이전 코드와 충돌하는 새 코드 생성

[4회] "이전 코드와 합쳐줘"
      → AI가 컨텍스트를 잃고 다시 처음부터 시작...
```

이 루프에서 빠져나오려면 평균 5~15회의 추가 대화가 필요합니다.

> **💡 핵심 교훈**: AI는 마법이 아닙니다. 입력이 모호하면 출력도 모호해집니다.

---

### 2.2 "더 많이 알려주면 되지 않나?" — 정보 과부하의 역설

> **"그럼 더 자세하게, 더 많은 정보를 주면 되겠네?"**

**정보의 양이 많다고 해서 AI가 더 잘 이해하는 것은 아닙니다.**

#### "Lost in the Middle" 현상

AI는 긴 텍스트를 처리할 때 처음과 끝 부분을 잘 처리하지만, **중간 부분의 중요한 제약사항과 비즈니스 규칙은 상대적으로 누락**하는 경향이 있습니다.

```
긴 프롬프트:
┌──────────────────────────────────────────┐
│ [앞부분] 잘 처리됨 ✅                      │
│ [중간부분] 중요한 제약사항, 비즈니스 규칙 ⚠️ │
│ [끝부분] 잘 처리됨 ✅                      │
└──────────────────────────────────────────┘
```

| 상황 | 기대 | 실제 결과 |
|---|---|---|
| 짧은 지시 | 빠를 것 같다 | AI가 스스로 가정하여 일관되지 않은 결과 |
| 긴 비구조화 정보 | 더 잘 알겠지 | AI가 중요도 판단을 못해 핵심 요구사항 누락 |
| 구조화된 명세 | 번거롭다 | AI가 정확하게 이해하고 예측 가능한 결과 생성 |

> **💡 핵심 교훈**: AI에게 필요한 것은 **정보의 양**이 아니라 **정보의 구조**입니다.

---

### 2.3 진짜 문제: 커뮤니케이션 구조의 부재

> **문제의 본질은 커뮤니케이션 구조의 부재입니다.**

```
[인간 간 커뮤니케이션]
시니어 → 주니어: 조직 문화, 코딩 컨벤션, 도메인 지식을 공유
                  암묵적 지식 + 명시적 지시의 혼합

[AI와의 커뮤니케이션]
개발자 → AI: 공유하는 맥락이 없음. 모든 것을 명시적으로 구조화해야 함
```

```
커뮤니케이션 구조 부재
        │
        ├── AI가 임의로 기술 스택 선택 → 조직 표준과 불일치
        ├── AI가 임의로 보안 수준 결정 → 규정 준수 실패
        ├── AI가 임의로 아키텍처 패턴 선택 → 기존 시스템과 불일치
        └── 세션마다 다른 결과 → 일관성 없는 코드베이스
```

SDD가 제공하는 커뮤니케이션 구조는 다음과 같습니다.

```
constitution.md → "우리 팀은 이렇게 개발합니다" (공통 규칙)
spec.md         → "이 기능은 이렇게 동작해야 합니다" (명세)
plan.md         → "이렇게 구현할 것입니다" (설계)
tasks.md        → "이 순서로 작업합니다" (실행 계획)
```

> **🔑 핵심 메시지**: "좋은 명세는 좋은 코드보다 먼저 존재한다."

---

## 3. SDD란 무엇인가?

---

### 3.1 SDD의 정의와 등장 배경

#### Birgitta Böckeler의 분석

사양 기반 개발(Specification-Driven Development, SDD)을 가장 체계적으로 정리한 인물 중 한 명은 **Birgitta Böckeler** 입니다. 그녀는 전 세계 45개국 1만 명 이상의 기술 전문가 조직인 **Thoughtworks의 Distinguished Engineer(수석 기술자)** 입니다.

Böckeler는 소프트웨어 아키텍처의 세계적 권위자인 **Martin Fowler**가 운영하는 **martinfowler.com**에 SDD에 관한 분석 글을 게재했습니다.

> **martinfowler.com이란?**
> Martin Fowler는 『Refactoring』 등의 저서로 유명한 소프트웨어 개발의 선구자입니다. 그의 웹사이트는 소프트웨어 개발 방법론에 관한 가장 권위 있는 블로그 중 하나입니다.

Böckeler의 분석은 SDD를 "AI 에이전트가 단순한 코드 생성기를 넘어 실제 개발 파트너로 기능하게 만드는 방법론"으로 정의합니다.

#### SDD의 핵심 정의

> **"코드보다 사양(Specification)을 먼저 정의하라. 사양이 명확하면, AI는 올바른 코드를 만들 수 있다."**

```
[기존 패러다임]
요구사항(말로) → 설계(머릿속) → 코드(실체) ← 진실의 근원
                                    ↑
                     문서는 코드를 따라가며 구식이 됨

[SDD 패러다임]
사양(문서) ← 진실의 근원 → AI가 코드로 변환
    ↑                              ↓
코드 변경 시 사양 먼저 수정      코드는 사양의 표현
```

---

### 3.2 기존 개발 방식과 SDD의 차이

| 구분 | 기존 방식 | SDD |
|------|-----------|-----|
| 진실의 근원 | 코드 | 사양 |
| 사양의 역할 | 코드를 만들기 위한 수단 | 핵심 아티팩트 |
| 코드의 역할 | 최종 결과물 | 사양의 구현체 |
| 유지보수 대상 | 코드 | 사양 + 코드 |
| AI 활용 방식 | 코드 생성 보조 도구 | 사양을 코드로 번역하는 실행자 |
| 변경 시작점 | 코드 수정 | 사양 수정 → 코드 재생성 |
| 문서 상태 | 코드와 점점 괴리됨 | 코드와 항상 동기화 |

---

### 3.3 SDD의 3가지 구현 레벨

Böckeler는 SDD를 성숙도에 따른 **3가지 레벨**로 구분했습니다.

---

#### Level 1: Spec-First (명세 우선)

```
수준: 입문 단계
핵심 원칙: "코딩 전에 먼저 문서로 정의한다"
```

코드를 작성하기 전에 무엇을 만들 것인지를 먼저 문서로 정의합니다. 사양은 **참조 문서(Reference Document)** 의 역할입니다.

- 기능 개발 전에 spec.md를 먼저 작성
- 사양은 AI 대화에서 맥락 제공 용도로 활용
- 코드 완성 후 사양은 보관 또는 일부 업데이트

> **Level 1은 지금 당장 실천할 수 있는 가장 기본적인 SDD 형태입니다.**

---

#### Level 2: Spec-Anchored (명세 고정)

```
수준: 중급 단계
핵심 원칙: "사양이 개발의 기준점이다. 코드는 사양을 증명해야 한다"
```

사양이 개발의 기준점(Anchor)이 됩니다. 코드가 변경될 때마다 사양과의 일치 여부를 검증합니다.

```markdown
코드 리뷰 체크리스트:
  □ 모든 수용 기준(AC)에 대응하는 코드가 있는가?
  □ 엣지 케이스가 처리되었는가?
  □ 사양에 없는 기능이 무단으로 추가되지 않았는가?
```

---

#### Level 3: Spec-as-Source (명세가 소스)

```
수준: 고급 단계
핵심 원칙: "사양이 코드의 소스다. 코드는 사양에서 생성된다"
```

사양 자체가 코드를 생성하는 소스입니다. AI는 사양을 입력받아 코드, 테스트, 문서를 모두 생성합니다.

```
사양 작성/수정 → AI가 해석 → 구현 코드, 단위 테스트, API 문서 자동 생성
                              → 개발자가 결과 검증 → 사양 정제
```

---

#### 3가지 레벨 비교

| 구분 | Level 1: Spec-First | Level 2: Spec-Anchored | Level 3: Spec-as-Source |
|---|---|---|---|
| 핵심 변화 | 코딩 전 문서화 | 코드가 사양을 증명 | 사양이 코드를 생성 |
| 사양의 역할 | 참조 문서 | 기준점 | 소스 |
| 도입 난이도 | 낮음 | 중간 | 높음 |
| 적용 대상 | 모든 팀 | 체계적인 팀 | 성숙한 팀 |

> **💡 실용적 조언**: Level 1부터 시작해서 팀의 역량과 함께 점진적으로 이동하는 것이 현실적입니다.

---

### 3.4 스펙과 Memory Bank: 두 가지 컨텍스트의 역할

AI와 효과적으로 협업하려면 두 가지 컨텍스트를 모두 제공해야 합니다.

```
AI가 필요로 하는 정보
  ├── [1] 스펙(Spec): "무엇을 만들어야 하는가?"
  │         → 기능별, 작업별로 달라지는 정보
  └── [2] Memory Bank: "어떤 맥락에서 만들어야 하는가?"
            → 프로젝트 전체에 걸쳐 일관되게 유지되는 정보
```

**스펙(Specification)**은 특정 기능에 대한 구체적인 요구사항으로, 새 기능 개발 시마다 작성됩니다.

**Memory Bank**는 프로젝트 전체에 적용되는 맥락 정보입니다. GitHub Spec Kit의 `constitution.md`, Cursor의 `.cursorrules`, Gemini의 `GEMINI.md`가 모두 Memory Bank의 구현 형태입니다.

```
[두 컨텍스트가 없을 때] → 매번 다른 스타일, 일관성 없는 코드
[스펙만 있을 때]        → 기능 구현되지만 기존 코드베이스와 스타일 불일치
[Memory Bank만 있을 때] → 프로젝트에 맞는 스타일이지만 엉뚱한 기능 구현
[스펙 + Memory Bank]    → 의도한 기능을 프로젝트 표준에 맞게 일관성 있게 구현
```

> **🔑 핵심**: 스펙은 기능을 정의하고, Memory Bank는 맥락을 제공합니다. 두 가지가 결합될 때 AI는 진정한 개발 파트너가 됩니다.

---

## 4. SDD의 4가지 핵심 원칙

### 원칙 1. 기본 아티팩트로서의 사양

사양은 진실의 중심 소스입니다. 코드는 사양을 표현한 결과물이며, 소프트웨어를 유지보수한다는 것은 사양 자체를 발전시키는 것을 의미합니다.

### 원칙 2. 실행 가능한 사양(Executable Specification)

사양은 작동하는 시스템을 생성할 수 있을 만큼 **정확하고, 완전하며, 명확**해야 합니다. "로그인 기능 만들어 줘"라는 지시는 SDD에서 사용자 스토리, 수용 기준, 요구 사항, 엣지 케이스로 구체화됩니다.

### 원칙 3. 살아있는 문서(Living Documentation)

- 버그를 수정한다는 것은 잘못된 코드를 만들어 낸 **사양을 수정**하는 것입니다.
- 리팩터링한다는 것은 명확성을 위해 **사양을 재구성**하는 것입니다.
- 사양은 구현과 항상 동기화된 상태를 유지합니다.

### 원칙 4. AI-인간 협업 구조

```
[기존 바이브 코딩]
  개발자 ──(모호한 지시)──▶ AI ──▶ 예측 불가능한 코드

[SDD + 에이전틱 엔지니어링]
  개발자 ──(명확한 사양)──▶ AI ──▶ 검증된 코드
     ▲          ▲                        │
  Memory    Spec 문서              피드백/검증
  Bank      (기능 명세)                  │
     └──────────┴────────────────────────┘
```

---

## 5. Less is More: 스펙 설계 전략

2절에서 "정보 과부하의 역설"을 배웠습니다. 그렇다면 **얼마나, 어떻게** 써야 할까요? 이 장에서는 효과적인 스펙을 설계하는 전략을 다룹니다.

---

### 5.1 Less is More 원칙이란?

소프트웨어 설계에서 "Less is More"는 단순성의 철학입니다. 스펙 설계에 적용하면 이런 의미가 됩니다.

> **"스펙의 가치는 길이가 아니라 밀도에 있다. 필요한 것만, 정확하게, 구조적으로."**

이 원칙의 배경에는 AI의 작동 방식이 있습니다.

```
스펙 길이 vs AI 이해도:

짧고 핵심적인 스펙 → AI의 주의가 집중 → 높은 정확도
긴 비구조화 스펙   → AI의 주의가 분산 → 중요한 부분 누락

최적의 스펙: 충분히 구체적이되, 불필요한 내용 없이
```

Less is More 원칙의 실천 방향은 다음 세 가지입니다.

**첫째, JES(Just Enough Specification)**: 구현하기에 충분한 정보만. 구현 방법의 세부사항은 AI에게 위임합니다.

**둘째, 단일 책임 원칙**: 하나의 스펙 파일이 하나의 기능에만 집중합니다. 관련 없는 요구사항을 하나의 파일에 섞지 않습니다.

**셋째, 계층적 구조**: 범위에 따라 스펙을 계층으로 분리합니다. 프로젝트 전체 원칙과 개별 기능 명세를 같은 파일에 섞지 않습니다.

---

### 5.2 계층적 스펙 구조

효과적인 스펙 시스템은 범위(Scope)에 따라 **4개 레이어**로 구성됩니다. 각 레이어는 다른 레이어를 참조하되, 직접 포함하지 않습니다.

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 0: 프로젝트 헌법 (constitution.md)                    │
│  "이 프로젝트는 이렇게 만든다"                               │
│  → 기술 스택, 보안 원칙, 코딩 컨벤션, 팀 규칙               │
│  → 변경 빈도: 매우 낮음 (분기 1회 이하)                     │
├─────────────────────────────────────────────────────────────┤
│  Layer 1: 도메인/시스템 명세 (domain.md)                    │
│  "이 시스템은 어떤 도메인으로 구성되는가"                   │
│  → 주요 도메인, 서비스 간 관계, API 계약 개요               │
│  → 변경 빈도: 낮음 (새 도메인 추가 시)                      │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: 기능 명세 (features/NNN-기능명/spec.md)           │
│  "이 기능은 이렇게 동작한다"                                │
│  → 사용자 스토리, 수용 기준, 엣지 케이스                   │
│  → 변경 빈도: 중간 (기능 변경 시)                           │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: 구현 작업 명세 (features/NNN-기능명/tasks.md)     │
│  "이 기능을 어떤 순서로 구현한다"                           │
│  → 구체적 작업, 의존 관계, 완료 기준                        │
│  → 변경 빈도: 높음 (구현 진행에 따라)                       │
└─────────────────────────────────────────────────────────────┘
```

#### Layer 0: 프로젝트 헌법 (constitution.md)

프로젝트 전체에 적용되는 **변하지 않는 원칙들**입니다. 팀 전체가 합의한 규칙이므로, 변경 시 모든 팀원의 동의가 필요합니다.

포함해야 할 내용과 포함하지 말아야 할 내용은 다음과 같습니다.

| 포함해야 할 것 (✅) | 포함하지 말아야 할 것 (❌) |
|---|---|
| 승인된 기술 스택과 버전 | 특정 기능의 요구사항 |
| 보안 원칙 (암호화 알고리즘 등) | 구현 세부사항 |
| 코딩 컨벤션 | 변경될 가능성이 있는 비즈니스 규칙 |
| 테스트 커버리지 기준 | 임시 결정사항 |
| 배포 환경 정보 | 특정 팀원에게만 해당되는 규칙 |

헌법의 규모는 **한 화면에 보일 정도**(약 50~80줄)가 적절합니다. 그 이상이 되면 AI가 중요한 원칙을 놓칩니다.

#### Layer 1: 도메인/시스템 명세 (domain.md)

시스템의 **전체 지도(Map)** 입니다. 어떤 도메인이 있고, 도메인 간 경계가 어디인지, 주요 API 계약이 무엇인지를 정의합니다.

```markdown
# 시스템 도메인 명세 예시

## 주요 도메인
- 인증(Auth): 로그인, 회원가입, 세션 관리
- 주문(Order): 주문 생성, 조회, 취소
- 결제(Payment): 결제 처리, 환불 (외부 연동)

## 서비스 간 통신 원칙
- Auth → 다른 서비스: JWT 토큰 검증
- Order → Payment: REST API (동기)
- Payment → Order: 이벤트 메시지 (비동기)

## 외부 의존성
- 결제 게이트웨이: TossPayments API v2
- SMS: NCP SMS API
- 이메일: AWS SES
```

Layer 1의 목적은 "전체 그림 없이 부분만 본다"는 함정을 방지하는 것입니다. AI가 한 기능을 구현할 때 전체 시스템 구조를 알고 있으면, 다른 서비스와 충돌하는 코드를 생성하는 실수를 줄일 수 있습니다.

#### Layer 2: 기능 명세 (spec.md)

특정 기능이 **무엇을, 왜** 해야 하는지를 정의합니다. AI가 코드를 생성할 때 가장 직접적으로 참조하는 레이어입니다.

기능 명세의 표준 구조는 다음과 같습니다.

```markdown
# 기능 명세: [기능 이름]

## 사용자 스토리
[역할]로서, [목표]를 위해 [기능]을 원한다.

## 수용 기준 (Acceptance Criteria)
- Given: [전제 조건]
  When: [사용자 행동]
  Then: [예상 결과]

## 기능 요구사항
- MUST: [반드시 구현해야 함]
- SHOULD: [가능하면 구현]
- MAY: [선택적 구현]

## 비기능 요구사항
- 성능: [측정 가능한 기준]
- 보안: [보안 제약사항]

## 엣지 케이스
- [예외 상황 1]: [처리 방법]
- [예외 상황 2]: [처리 방법]
```

#### Layer 3: 구현 작업 명세 (tasks.md)

Layer 2의 기능 명세를 **어떤 순서로 구현할 것인가**를 정의합니다.

```markdown
## 작업 목록: 로그인 기능

[ ] TASK-001: User 데이터 모델 및 DB 스키마 생성
    → spec.md AC-login-1, AC-login-2 참조
    → 완료 기준: 마이그레이션 스크립트 실행 가능

[ ] TASK-002: 로그인 API 엔드포인트 구현 (TASK-001 완료 후)
    → spec.md AC-login-1, AC-login-3 참조
    → 완료 기준: 단위 테스트 통과

[ ] TASK-003: 프론트엔드 로그인 폼 구현 (TASK-002 완료 후)
    → 완료 기준: E2E 테스트 시나리오 통과
```

#### 계층 간 참조 원칙

```
올바른 참조:
  Layer 2(기능 명세) → "constitution.md의 보안 원칙을 따른다" (Layer 0 참조)
  Layer 3(작업 명세) → "spec.md AC-003을 구현한다" (Layer 2 참조)

잘못된 참조:
  Layer 0(헌법) → 특정 기능 요구사항을 포함 (Layer 2 내용이 Layer 0에 섞임)
  Layer 2(기능) → 다른 기능의 상세 요구사항을 포함 (기능 간 결합)
```

---

### 5.3 스펙 작성 실전 가이드: 좋은 스펙과 나쁜 스펙

이론보다 예시가 더 명확합니다. 실제 스펙 작성에서 자주 발생하는 나쁜 패턴과 그것을 좋은 스펙으로 고치는 방법을 살펴봅니다.

---

#### 나쁜 패턴 1: 모호한 형용사 사용

```markdown
❌ 나쁜 스펙:
"검색 결과는 빠르게 반환되어야 한다."
"사용자 인터페이스가 직관적이어야 한다."
"시스템은 안정적으로 동작해야 한다."

→ 문제: "빠르게", "직관적으로", "안정적으로"는 AI가 수치화할 수 없다.
  AI는 자신이 판단하는 "빠름"의 기준으로 코드를 작성한다.
```

```markdown
✅ 좋은 스펙:
"검색 결과는 500ms 이내에 반환되어야 한다. (p95 기준)"
"모바일 화면(375px 이상)에서 버튼의 터치 영역이 44px 이상이어야 한다."
"가용성 99.9% (월간 다운타임 43분 이하)"

→ 측정 가능한 수치로 정의하면 AI가 구현 기준을 명확히 알 수 있다.
```

---

#### 나쁜 패턴 2: 구현 방법을 지시하는 스펙

```markdown
❌ 나쁜 스펙:
"React의 useState 훅을 사용하여 로그인 상태를 관리한다."
"MySQL의 InnoDB 엔진을 사용하고 인덱스를 username 컬럼에 추가한다."
"bcrypt의 saltRounds를 10으로 설정한다."

→ 문제: 이것은 "어떻게(How)"에 해당한다.
  스펙은 "무엇을(What)"과 "왜(Why)"에 집중해야 한다.
  구현 세부사항은 AI에게 위임하거나, plan.md에 작성한다.
```

```markdown
✅ 좋은 스펙:
"사용자의 로그인 상태는 페이지 이동 시에도 유지되어야 한다."
"사용자 검색 성능: 100만 건 데이터에서 1초 이내 결과 반환"
"비밀번호는 해킹 시도로부터 보호될 수 있는 수준으로 저장되어야 한다.
  (constitution.md의 보안 원칙 참조)"

→ "무엇을"과 "왜"를 정의하고, "어떻게"는 constitution.md 원칙을 참조한다.
```

---

#### 나쁜 패턴 3: 검증 불가능한 요구사항

```markdown
❌ 나쁜 스펙:
"좋은 사용자 경험을 제공해야 한다."
"확장 가능한 아키텍처로 구현해야 한다."
"유지보수하기 쉬운 코드를 작성해야 한다."

→ 문제: "테스트가 통과하면 이 요구사항이 충족되었는가?"를
  판단할 방법이 없다.
  AI도, 개발자도, QA도 "좋은 UX"의 기준을 모른다.
```

```markdown
✅ 좋은 스펙:
Given: 사용자가 잘못된 비밀번호를 입력했을 때
When:  로그인 버튼을 클릭하면
Then:  "이메일 또는 비밀번호가 올바르지 않습니다" 메시지가 input 필드 아래에
       빨간색으로 표시되고, 비밀번호 필드가 초기화된다.

→ Given-When-Then 형식으로 테스트 가능한 기준을 정의한다.
```

---

#### 나쁜 패턴 4: 맥락 없는 요구사항

```markdown
❌ 나쁜 스펙:
"에러 처리를 해야 한다."
"로깅을 추가한다."
"보안을 강화한다."

→ 문제: "어떤" 에러? "어떤 수준의" 로깅? "무엇에 대한" 보안?
  AI는 자신이 판단하기에 적절한 수준으로 임의로 구현한다.
```

```markdown
✅ 좋은 스펙:
"에러 처리:
  - 네트워크 오류: "서버에 연결할 수 없습니다. 잠시 후 다시 시도해 주세요." 표시
  - 세션 만료: 로그인 페이지로 리다이렉트
  - 잘못된 입력: 해당 필드 아래에 인라인 에러 메시지 표시

로깅:
  - 모든 로그인 시도(성공/실패)를 기록 (사용자 ID, IP, 타임스탬프)
  - 5회 연속 실패 시 보안 알림 이벤트 발행"

→ 구체적인 시나리오와 처리 방법을 명시한다.
```

---

#### 나쁜 패턴 5: 너무 긴 스펙 (모든 것을 한 파일에)

```markdown
❌ 나쁜 스펙 구조:
# 전체 시스템 요구사항 명세서 (500줄)

## 기술 스택
## 로그인 기능
## 회원가입 기능
## 주문 기능
## 결제 기능
## 배송 기능
...

→ 문제: AI는 500줄 문서에서 지금 필요한 로그인 기능과
  관련 없는 배송 기능의 노이즈를 구분하지 못한다.
```

```markdown
✅ 좋은 스펙 구조:
constitution.md (50줄: 프로젝트 전체 원칙)
features/001-login/spec.md (50줄: 로그인 기능만)
features/002-signup/spec.md (40줄: 회원가입 기능만)
features/003-order/spec.md (60줄: 주문 기능만)
...

→ 기능별로 분리하여 AI가 현재 작업에만 집중할 수 있게 한다.
```

---

#### 스펙 품질 자가 체크리스트

스펙을 작성한 후 아래 질문에 "예"라고 답할 수 있으면 좋은 스펙입니다.

```
[ ] 이 스펙만 보고 AI가 구현할 수 있는가?
[ ] 모든 수용 기준이 테스트 가능한가?
[ ] 모호한 형용사("빠르게", "쉽게")가 없는가?
[ ] 구현 방법(How)이 아닌 요구사항(What)에 집중했는가?
[ ] 엣지 케이스(예외 상황)를 정의했는가?
[ ] constitution.md의 원칙과 충돌하지 않는가?
[ ] 다른 기능의 요구사항이 섞여 있지 않은가?
[ ] 한 화면에 볼 수 있는 길이인가? (50~100줄 권장)
```

> **🔑 핵심 메시지**
>
> 좋은 스펙은 개발자가 작성하기 쉬운 스펙이 아니라, **AI가 올바른 코드를 생성하기 쉬운 스펙**입니다.
> 스펙 작성에 30분을 투자하면 구현과 수정에서 3~5시간을 절약합니다.

---

## 6. SDD 4단계 워크플로

```
[1단계: 지정] ──▶ [2단계: 계획] ──▶ [3단계: 작업] ──▶ [4단계: 구현]
  Specify              Plan              Task             Implement
  "무엇을?"           "어떻게?"        "어떤 순서로?"      "코드 생성"
```

각 단계는 다음 단계의 입력물이 되는 아티팩트를 생성하며, 요구 사항에서 최종 코드까지의 완전한 추적성(Traceability)이 보장됩니다.

---

### 1단계: 지정(Specify)

**"소프트웨어가 무엇을 해야 하는지, 그리고 왜 해야 하는지를 정의합니다."**

개발자, 기획자, 이해관계자가 함께 참여하여 시스템이 달성해야 할 목표를 명확하게 합의합니다.

#### 사용자 스토리(User Story)

```
형식: "나는 [사용자 유형]으로서, [목표]를 달성하기 위해 [기능]을 원한다."
예시: "나는 쇼핑몰 회원으로서, 주문 내역을 한눈에 확인하기 위해
       마이페이지에서 최근 주문 목록을 조회하고 싶다."
```

#### 수용 기준(Acceptance Criteria)

```
Given: 로그인한 사용자가 마이페이지에 접속했을 때
When:  주문 내역 탭을 클릭하면
Then:  최근 3개월 이내 주문 목록이 날짜 내림차순으로 표시된다.
```

#### 요구 사항(Requirements)

**기능적 요구 사항(Functional Requirements)**

- MUST: 회원가입 시 이메일 중복 여부를 확인해야 한다.
- MUST: 비밀번호는 최소 8자 이상, 영문·숫자·특수문자를 각각 1개 이상 포함해야 한다.
- SHOULD: 주문 완료 후 사용자에게 확인 이메일을 발송해야 한다.

**비기능적 요구 사항(Non-Functional Requirements)**

- 성능: 주문 목록 조회 응답 시간은 2초 이내여야 한다.
- 보안: 개인정보는 AES-256 암호화로 저장해야 한다.
- 가용성: 시스템 가동률은 99.9% 이상을 유지해야 한다.

#### 엣지 케이스(Edge Cases)

- **경계값**: 주문 수량이 0개일 때, 재고가 정확히 1개 남았을 때
- **빈 값 및 null**: 검색어 없이 검색 버튼을 눌렀을 때
- **권한 예외**: 로그인하지 않은 사용자가 마이페이지에 접근할 때
- **동시성**: 두 사용자가 동시에 마지막 재고 1개를 구매하려 할 때
- **네트워크 오류**: 결제 진행 중 연결이 끊겼을 때

---

### 2단계: 계획(Plan)

**"어떻게 구축할 것인지를 결정합니다."**

아키텍처, 기술 스택, 구현 전략을 결정합니다.

- **아키텍처 설계**: 시스템 구조 선택, 컴포넌트 분리, 데이터 흐름 정의
- **기술 스택 선정**: 성능, 팀 역량, 생태계 성숙도, 조직 표준 기반 결정
- **구현 전략**: 개발 우선순위, 통합 전략, 테스트 전략, 배포 전략

---

### 3단계: 작업(Task)

**"계획을 구체적이고 실행 가능한 개발 작업 단위로 분해합니다."**

```
[계획] 회원가입 기능 구현
  ├── [작업 1] User 데이터 모델 및 DB 스키마 생성
  ├── [작업 2] 이메일 중복 확인 API 구현
  ├── [작업 3] 회원가입 API 엔드포인트 구현
  ├── [작업 4] 비밀번호 유효성 검증 로직 구현
  ├── [작업 5] 가입 완료 확인 이메일 발송 기능 구현
  └── [작업 6] 회원가입 관련 단위 테스트 작성
```

좋은 작업의 크기는 하루 이내에 완료할 수 있는 수준이 이상적입니다.

---

### 4단계: 구현(Implement)

**"사양, 계획, 작업 목록을 기반으로 코드를 작성합니다."**

```
[작업 선택] ──▶ [사양 확인] ──▶ [코드 생성] ──▶ [수용 기준 검증] ──▶ [다음 작업]
                                                         │
                                        (실패 시 사양 재검토 또는 코드 수정)
```

추적성(Traceability) 확보 예시입니다.

```python
# Implements: User Story #12 - 주문 목록 조회
# Acceptance Criteria: AC-12-1, AC-12-2
# Task: TASK-045
def get_order_list(user_id: str) -> list:
    ...
```

---

## 7. 실전 도구 비교

```
도구           │ 만든 곳  │ 접근법           │ SDD 레벨
───────────────┼─────────┼──────────────────┼──────────
AWS Kiro       │ Amazon  │ 통합 AI IDE       │ Level 2~3
GitHub Spec Kit│ GitHub  │ 명령어 기반 확장  │ Level 1~3
Tessl          │ Tessl   │ AI 네이티브 플랫폼│ Level 2~3
```

---

### 7.1 AWS의 Kiro

**Kiro(카이로)** 는 2025년 AWS가 발표한 **명세 주도 AI IDE**입니다. "스펙에서 코드까지(Spec to Code)"를 핵심 슬로건으로 내세우며, SDD를 IDE 수준에서 완전히 통합합니다.

**핵심 기능**

자연어로 기능 아이디어를 입력하면 requirements.md, design.md, tasks.md를 자동으로 생성합니다. **Steering Documents**는 GitHub Spec Kit의 `constitution.md`에 해당하며, 프로젝트 전체 AI 작업에 자동 적용됩니다. **Hook 시스템**은 코드 변경 시 자동으로 트리거되는 검증 워크플로입니다.

| 항목 | 내용 |
|---|---|
| SDD 지원 레벨 | Level 2~3 |
| 강점 | 스펙 → 코드 변환이 워크플로에 완전 통합 |
| 제한 | 기존 프로젝트에 도입 시 학습 비용 |
| 적합 대상 | 새 프로젝트, AWS 생태계를 활용하는 팀 |

---

### 7.2 GitHub Spec Kit

VS Code와 GitHub Copilot을 연동하는 **명령어 기반 SDD 확장 도구**입니다.

```
/speckit.specify   → spec.md 생성 (1단계)
/speckit.clarify   → 명세 보완
/speckit.checklist → 품질 점검
/speckit.plan      → plan.md 생성 (2단계)
/speckit.tasks     → tasks.md 생성 (3단계)
/speckit.analyze   → 일관성 검사
/speckit.implement → 코드 생성 (4단계)
```

| 항목 | 내용 |
|---|---|
| SDD 지원 레벨 | Level 1~3 (단계적 도입 가능) |
| 강점 | VS Code + Copilot 환경에 자연스럽게 통합 |
| 강점 | Git 기반 버전 관리로 사양 이력 추적 |
| 제한 | Copilot 구독 필요 |
| 적합 대상 | GitHub 생태계를 사용하는 팀 |

---

### 7.3 Tessl Framework

**"AI Native Software Factory"** 를 표방하는 플랫폼입니다. 핵심 개념은 **"의도 주도 개발(Intent-Driven Development)"** 입니다.

```
개발자의 의도(Intent)
        ↓
AI가 스펙 생성 → AI가 코드 생성 → AI가 테스트 생성 → AI가 배포 실행
                  ← 개발자는 각 단계의 결과를 검토하고 승인
```

| 항목 | 내용 |
|---|---|
| SDD 지원 레벨 | Level 3 (Spec-as-Source) 지향 |
| 강점 | 개발 사이클 전체의 AI 자동화 |
| 제한 | 높은 학습 비용, 성숙도 검증 필요 |
| 적합 대상 | AI 네이티브 신규 프로젝트 |

---

### 7.4 Böckeler가 던진 핵심 질문들

Böckeler는 SDD를 분석하면서 **날카로운 비판적 질문들**을 함께 제시했습니다.

---

#### 질문 1: "하나의 워크플로우로 모든 크기의 문제를 커버할 수 있는가?"

```
[대규모 기능] 로그인 시스템 구현
  → Specify → Plan → Task → Implement (수일 ~ 수주) ✅ 적합

[소규모 변경] 버튼 색상을 파란색에서 초록색으로 변경
  → 전체 SDD 워크플로 적용 시 과도한 오버헤드 ❌
```

**현실적 대응**: 작업 규모에 따른 워크플로 분기가 필요합니다.

```
소규모(< 30분): 자유 형식 지시 → 코드 수정 → 완료
중규모(30분~1일): 간략한 spec → 구현
대규모(1일 이상): 전체 SDD 워크플로 적용
```

---

#### 질문 2: "스펙 리뷰가 코드 리뷰보다 나은가?"

스펙 리뷰는 코드 리뷰를 **대체**하는 것이 아닙니다. **보완** 관계입니다.

```
스펙 리뷰가 잡아내는 것:   요구사항 누락, 설계 방향 오류, 논리 오류
코드 리뷰가 잡아내는 것:   구현 수준의 버그, 성능 문제, 컨벤션 위반
```

SDD 적용 후 스펙 리뷰와 코드 리뷰가 역할을 분담하면 전체 리뷰 비용이 감소하고 문제를 조기에 발견할 수 있습니다.

---

#### 질문 3: "통제의 착각은 아닌가?"

스펙은 **필요 조건**이지 **충분 조건**이 아닙니다.

```
착각: 스펙 작성 → AI가 스펙대로 코드 생성 → 완성
현실: 스펙 작성 → AI가 스펙을 나름대로 해석 → 반드시 검증 필요
```

수용 기준을 테스트 케이스로 변환하고, 테스트가 통과할 때만 "스펙이 충족되었다"고 판단해야 합니다.

---

#### 질문 4: "MDD의 실패를 반복하는 것은 아닌가?"

| 비교 항목 | MDD | SDD |
|---|---|---|
| 스펙 언어 | 형식적 모델링 언어(UML, DSL) | 자연어 + 구조화된 마크다운 |
| 코드 생성 | 규칙 기반 컴파일러 | 자연어 이해 기반 LLM |
| 모호함 처리 | 모호함 불허 (컴파일 오류) | 맥락을 추론하여 처리 |
| 사람의 참여 | 최소화 지향 | 검증 단계에 필수 참여 |

MDD가 인간을 완전히 배제하는 자동화를 목표로 했다면, SDD는 **AI와 인간의 협업**을 목표로 합니다.

**SDD가 MDD처럼 실패하는 패턴**:

```
1. 스펙 작성
2. AI가 코드 생성 (스펙과 약간 다름)
3. 개발자가 코드 직접 수정 (스펙 업데이트 없이) ← 이 순간부터 실패
4. 스펙과 코드 괴리 발생
5. 스펙이 다시 무용지물이 됨
```

**방지 원칙**: "코드를 수정하기 전에 반드시 스펙을 먼저 수정한다."

---

#### 핵심 질문들의 종합적 함의

| 질문 | 핵심 교훈 |
|---|---|
| 워크플로 크기 | 규모에 따라 워크플로를 조정하라 |
| 스펙 리뷰 | 스펙 리뷰는 코드 리뷰를 보완하는 것이다 |
| 통제의 착각 | 검증 단계가 반드시 뒤따라야 한다 |
| MDD 실패 반복 | "코드 수정 전 스펙 수정" 원칙을 타협하지 말라 |

---

## 8. 엔터프라이즈 레거시 전환에서의 SDD 적용

---

### 8.1 엔터프라이즈 환경에서 SDD가 중요한 이유

세 가지 흐름이 엔터프라이즈 팀에게 SDD를 필수 방법론으로 만들고 있습니다.

**AI 기능의 발전**: 사양의 품질이 곧 소프트웨어의 품질이 됩니다. 에이전틱 엔지니어링이 현실화될수록 명확한 사양 없이는 AI를 제대로 활용할 수 없습니다.

**소프트웨어 복잡성의 증가**: 현대 시스템은 수십 개의 서비스와 의존성을 통합합니다. SDD는 이 복잡한 환경에서도 체계적인 정합성을 제공합니다.

**빠른 변화의 속도**: 요구 사항은 빠르게 변합니다. SDD는 변경을 장애물이 아닌 정상적인 워크플로로 전환시킵니다.

| 이점 | 설명 |
|------|------|
| **일관성** | 조직 표준과의 지속적이고 일관된 정합성 유지 |
| **감사 가능성** | 요구 사항과 설계 결정에 대한 추적 및 감사 가능한 문서화 |
| **규정 준수** | 보안 정책 및 규정 준수 요건의 체계적 적용 |
| **팀 협업** | 새 팀원이 사양 문서만으로 시스템을 빠르게 파악 가능 |
| **AI 활용 극대화** | 명확한 사양이 AI 코드 생성 품질을 직접적으로 향상 |

---

### 8.2 왜 레거시 전환에 SDD가 필요한가?

#### 레거시 시스템의 현실

대부분의 기업은 5~20년 된 레거시 시스템을 운영하고 있습니다. 이 시스템들의 공통적인 특징은 다음과 같습니다.

```
레거시 시스템의 전형적인 상태:
  ├── 문서 없음 또는 현행화 되지 않은 문서
  ├── 원래 개발자가 이직하여 맥락 상실
  ├── 코드만이 "진실"이나 코드도 이해하기 어려움
  ├── 변경 시마다 예상치 못한 버그 발생
  └── 새 기능 추가가 두려운 상태 ("무너질까봐")
```

이 상태에서 AI를 활용하여 레거시를 전환하려면 어떤 일이 발생할까요?

```
AI에게 레거시 코드를 주고 "현대화해줘"라고 하면:

→ AI가 코드를 분석하고 자신의 해석으로 새 코드 생성
→ 비즈니스 규칙이 원래 코드에 숨어 있어 AI가 놓침
→ 새 코드가 기존 코드의 동작을 완전히 재현하지 못함
→ 프로덕션 배포 후 예상치 못한 장애 발생
```

#### SDD가 레거시 전환의 리스크를 줄이는 방법

SDD는 레거시 전환의 순서를 바꿉니다.

```
[기존 접근법 (코드 → 코드)]
레거시 코드 → AI가 분석 → 새 코드 생성
                ↑
        비즈니스 규칙 누락 위험이 매우 높음

[SDD 접근법 (코드 → 스펙 → 코드)]
레거시 코드 → 역분석하여 스펙 도출 → 스펙 검증 → 새 코드 생성
                ↑                          ↑
        비즈니스 규칙을 먼저       개발자가 스펙을 검토하여
        명시적으로 문서화          누락 여부 확인 가능
```

SDD 접근법의 핵심은 **"코드를 전환하기 전에 동작을 먼저 명세로 만든다"** 는 것입니다. 새 코드는 레거시 코드를 보지 않고, 스펙을 보고 생성합니다.

#### 레거시 전환 전과 후의 비용 비교

```
[SDD 없이 레거시 전환]
전환 기간: 예상 6개월 → 실제 18~24개월
주요 원인: 예상치 못한 비즈니스 규칙 발견
          → 전환 중 기존 기능 회귀(Regression) 발생
          → 검증 반복 → 일정 지연

[SDD로 레거시 전환]
스펙 도출 기간: 2~4주 추가
전환 기간: 예상 대비 30~40% 단축 (스펙이 검증 기준 역할)
주요 이점: 비즈니스 규칙을 전환 전에 모두 파악
           수용 기준 기반 자동화 테스트로 회귀 조기 발견
```

---

### 8.3 기존 산출물에서 스펙으로: 핵심 정보 압축의 비밀

레거시 전환 프로젝트에는 보통 방대한 기존 문서가 존재합니다. 1,000페이지짜리 요구사항 정의서, 2008년에 작성된 설계 문서, 수백 개의 Use Case 문서… 이것들을 AI에게 그대로 주면 효과가 없습니다. 핵심 정보를 **압축**하는 작업이 필요합니다.

#### 기존 문서에서 AI가 실제로 사용하는 정보 vs 사용하지 않는 정보

```
기존 1,000페이지 요구사항 정의서의 실제 구성:

AI가 사용할 수 있는 정보 (약 5~10%):
  ├── 실제 비즈니스 규칙과 로직
  ├── 인터페이스 정의 (입력/출력)
  ├── 예외 처리 조건
  └── 비기능 요구사항 수치

AI가 사용할 수 없는 정보 (약 90~95%):
  ├── 회의록과 결정 배경 (맥락적 히스토리)
  ├── 이해관계자 간 정치적 타협의 흔적
  ├── 현재 구현과 달라진 구식 설계 내용
  ├── 반복된 서술 (같은 규칙이 5번 등장)
  ├── UML 다이어그램 (텍스트로 변환 불가)
  └── 미래 계획 (현재 구현과 무관)
```

이것이 기존 문서를 SDD 스펙으로 변환할 때 대규모 압축이 이루어지는 이유입니다. 1,000페이지 문서에서 핵심 비즈니스 규칙만 추출하면 50~80페이지의 명세서로 정리됩니다.

#### 기존 산출물에서 스펙 도출하는 5단계 프로세스

**1단계: 기존 문서 분류 및 신뢰도 평가**

```
문서 유형별 신뢰도:
  ├── 코드 자체: 가장 신뢰도 높음 (현재 동작의 정확한 반영)
  ├── 테스트 코드: 높음 (실제 검증된 동작 기록)
  ├── 최근 릴리즈 노트: 중간 (변경사항의 일부만 기록)
  ├── 2~3년 이내 설계 문서: 중간 (일부 구식 내용 포함 가능)
  └── 5년 이상 된 요구사항 문서: 낮음 (대부분 구식)
```

**2단계: 코드 역분석(Reverse Engineering)으로 비즈니스 규칙 추출**

AI를 활용하여 기존 코드에서 비즈니스 규칙을 추출합니다.

```
AI 프롬프트 예시:
"아래 코드에서 비즈니스 규칙만 추출하여 Given-When-Then 형식으로
 정리해줘. 기술적 구현 방법(라이브러리, 알고리즘)은 제외하고,
 '사용자 관점에서 어떤 일이 발생하는가'에만 집중해줘."
```

**3단계: 도메인 전문가 검증**

AI가 추출한 비즈니스 규칙을 도메인 전문가(현업 담당자)가 검토합니다. 이 단계에서 흔히 발견되는 것은 다음과 같습니다.

```
발견 유형:
  ├── 코드에는 있지만 문서에 없는 암묵적 비즈니스 규칙
  ├── 문서에는 있지만 코드에서 다르게 구현된 규칙
  ├── 개발자도 현업도 왜 그런지 모르는 "마법 같은 동작"
  └── 실제로는 사용되지 않아 제거 가능한 기능
```

**4단계: 스펙 구조화 및 우선순위 분류**

추출한 규칙을 Layer 0~3 구조에 맞게 배치합니다.

```
분류 기준:
  ├── "전 기능에 공통으로 적용" → constitution.md (Layer 0)
  ├── "이 서비스 전체에 적용" → domain.md (Layer 1)
  ├── "이 기능에만 적용" → feature/spec.md (Layer 2)
  └── "이 기능을 어떻게 구현하나" → feature/plan.md + tasks.md
```

**5단계: 수용 기준으로 기존 동작 검증**

도출된 스펙을 기반으로 테스트 케이스를 작성하고, 기존 레거시 시스템에서 통과하는지 확인합니다. 이것이 스펙이 실제 동작을 올바르게 반영하는지 검증하는 "황금 테스트(Golden Test)"가 됩니다.

```
황금 테스트 원칙:
  "레거시 시스템에서 통과하는 테스트가
   새 시스템에서도 반드시 통과해야 한다."
```

---

### 8.4 수백 명 규모 프로젝트에서의 SDD 운영

소규모 팀의 SDD와 수백 명이 참여하는 대규모 프로젝트의 SDD는 다른 도전 과제가 있습니다.

---

#### Multi-Project API 참조 문제

대규모 프로젝트에서는 수십 개의 마이크로서비스가 서로를 참조합니다. 한 서비스의 스펙이 다른 서비스의 API를 참조할 때, 그 API가 변경되면 어떻게 될까요?

**문제 시나리오**:

```
주문 서비스(Order) spec.md:
  "결제 완료 시 Payment 서비스의 /api/v1/payments/confirm를 호출한다"
      ↓
결제 서비스가 API를 /api/v2/payments/confirm으로 변경
      ↓
주문 서비스 스펙은 여전히 v1을 참조 → 스펙-코드 불일치 발생
```

**해결 방법**:

```
1. 공유 API 계약 저장소 (Shared Spec Repository):
   specs/
   ├── shared/
   │   ├── payment-api-contract.md    ← 결제 API 공식 계약
   │   ├── auth-api-contract.md       ← 인증 API 공식 계약
   │   └── notification-api-contract.md
   ├── order-service/
   │   └── spec.md (→ shared/payment-api-contract.md 참조)
   └── payment-service/
       └── spec.md (→ shared/payment-api-contract.md 소유)

2. API 변경 시 프로세스:
   결제 팀이 API 변경 → shared/payment-api-contract.md 업데이트
   → 자동화 도구가 이 파일을 참조하는 모든 서비스에 변경 알림
   → 각 팀이 자신의 스펙과 코드를 업데이트

3. /speckit.analyze를 CI/CD에 통합:
   PR 제출 시 자동으로 참조 API 계약과의 불일치 탐지
```

---

#### 스펙 품질의 균일성 문제

50개 팀이 각자 스펙을 작성하면, 팀마다 스펙의 품질과 형식이 달라집니다. 어떤 팀은 Given-When-Then을 정확하게 쓰고, 어떤 팀은 "~해야 한다"는 단순 나열만 합니다.

**문제**:

```
팀 A의 spec.md (고품질):
  Given: 로그인한 사용자가 주문 목록을 조회할 때
  When:  날짜 필터를 "최근 3개월"로 설정하면
  Then:  오늘 날짜 기준 90일 이내 주문만 표시된다

팀 B의 spec.md (저품질):
  - 주문 목록 조회 기능 구현 필요
  - 날짜 필터 있음
  - 성능 좋아야 함
```

팀 B의 스펙으로 AI가 생성한 코드는 팀 A보다 훨씬 예측 불가능합니다.

**해결 방법**:

```
1. 스펙 템플릿 표준화:
   모든 팀이 동일한 템플릿을 사용하도록 constitution.md에 명시
   + spec 템플릿 파일을 프로젝트 저장소에 포함

2. 자동화된 품질 검사:
   /speckit.checklist를 CI/CD에 통합
   → PR 제출 시 자동으로 스펙 완성도 체크
   → "모든 수용 기준에 Given-When-Then이 있는가?" 자동 검증

3. 크로스 팀 스펙 리뷰:
   분기별로 팀 A가 팀 B의 스펙을 리뷰하고 피드백
   → 팀 간 스펙 작성 노하우 공유

4. 스펙 품질 지표 측정:
   - 수용 기준 완성도 (%)
   - 엣지 케이스 정의 개수
   - 스펙-코드 불일치 발생 빈도
```

---

#### 작업 크기별 워크플로우 유연성

6절에서 소개한 Böckeler의 질문("하나의 워크플로로 모든 크기를 커버할 수 있는가?")은 대규모 프로젝트에서 더욱 중요합니다. 수백 명의 개발자가 하루에도 수십 개의 변경을 만들어 내는 환경에서 모든 변경에 동일한 SDD 프로세스를 적용하면 생산성이 무너집니다.

대규모 프로젝트를 위한 **3단계 워크플로 분기**는 다음과 같습니다.

```
[Tier 1: 소규모 변경] ≤ 4시간
  대상: 버그 수정, UI 텍스트 변경, 설정 값 조정
  스펙: 없음 (커밋 메시지에 "Fixes AC-123" 참조)
  리뷰: 코드 리뷰만

[Tier 2: 중규모 기능] 1~5일
  대상: 기존 기능 개선, 새 필드 추가
  스펙: 간략한 spec.md (사용자 스토리 + 핵심 AC만)
  리뷰: 스펙 리뷰 + 코드 리뷰

[Tier 3: 대규모 기능] 1주 이상
  대상: 신규 기능, 아키텍처 변경, 서비스 신설
  스펙: 전체 SDD 워크플로 (spec + plan + tasks)
  리뷰: 스펙 리뷰 → 아키텍처 리뷰 → 코드 리뷰
```

---

### 8.5 점진적 전환 전략: Big Bang은 실패한다

#### Big Bang 전환이 실패하는 이유

"내일부터 모든 팀이 SDD를 사용합니다"라고 선언하는 Big Bang 전환이 실패하는 이유는 다음과 같습니다.

```
Big Bang 전환의 실패 패턴:
  1. 전체 조직이 동시에 새 방법론 학습 시작
  2. 생산성이 즉시 30~50% 하락 (학습 곡선)
  3. 일정 압박으로 "이번 건 그냥 예전 방식으로"
  4. 일부 팀은 SDD, 일부는 기존 방식 혼용
  5. 혼용 상태가 고착화 → 사실상 실패

교훈: 방법론 전환은 기술 도입이 아니라 문화 변화입니다.
      문화 변화는 강제할 수 없고, 설득해야 합니다.
```

---

#### 성공하는 전환 전략

**20/80 규칙 적용**

파레토 법칙(20/80)을 SDD 도입에 적용합니다.

```
소프트웨어 프로젝트에서 일반적인 패턴:
  - 전체 기능의 20%가 전체 버그의 80%를 발생시킨다
  - 전체 코드의 20%가 전체 수정의 80%를 차지한다

SDD 적용 전략:
  1. 버그 발생 빈도, 수정 빈도, 복잡도 기준으로 기능을 분류
  2. 상위 20%의 고위험 기능에 먼저 SDD 적용
  3. 이 20%에서 얻은 효과가 전체의 80%의 가치를 만듦
  4. 성공 사례를 기반으로 나머지 80%로 점진적 확장
```

---

**파일럿으로 시작**

전체 조직에 도입하기 전에 1~2개 팀을 파일럿 팀으로 선정합니다.

```
파일럿 팀 선정 기준:
  ✅ 새로운 시도에 열린 자세를 가진 팀 리더
  ✅ 신규 기능 개발이 예정된 팀 (레거시 코드만 다루는 팀 제외)
  ✅ 3~6명의 소규모 팀 (너무 크면 조율 비용이 큼)
  ✅ 결과를 측정하고 공유할 의지가 있는 팀

파일럿 기간: 8~12주

파일럿 목표:
  - SDD 프로세스를 실제 프로젝트에 적용
  - 문제점 발견 및 조직에 맞게 커스터마이징
  - "우리 조직에서 SDD가 효과가 있는가?"에 대한 데이터 수집
  - 성공 사례 문서화 (다른 팀 설득용)
```

---

**병행운영**

기존 방식과 SDD를 동시에 운영하는 기간을 두어 리스크를 분산합니다.

```
병행운영 단계:
  Phase 1 (1~3개월): 파일럿 팀만 SDD, 나머지는 기존 방식
  Phase 2 (3~6개월): 파일럿 성공 시 자원하는 팀에게 확대
  Phase 3 (6~12개월): 조직 전체 SDD 적용 (또는 조직 맞춤 변형)

병행운영의 핵심 규칙:
  - 강제하지 않는다. 파일럿 팀의 성공이 설득한다.
  - 각 팀의 속도에 맞게 적용 레벨(Level 1~3)을 유연하게 선택
  - constitution.md는 전사 공통으로 시작, 기능 스펙은 팀별 자율
```

---

**측정**

SDD 도입 효과를 데이터로 측정하지 않으면 성공 여부를 알 수 없고, 다른 팀을 설득하기도 어렵습니다.

```
측정해야 할 지표:

개발 품질 지표:
  ├── 프로덕션 버그 발생률 (SDD 도입 전/후 비교)
  ├── 코드 리뷰 1회당 평균 피드백 수
  ├── 스펙-코드 불일치 발생 건수
  └── 회귀(Regression) 버그 발생률

개발 속도 지표:
  ├── 기능 개발 Lead Time (요구사항 확정 → 배포)
  ├── 스펙 작성 → 코드 생성 → 검증 각 단계별 소요 시간
  └── 수정 루프(AI와 반복 대화) 평균 횟수

팀 만족도 지표:
  ├── 개발자 스펙 작성 만족도 설문
  ├── "이 기능을 다시 하고 싶은가?" (자신감 지표)
  └── 신규 팀원 온보딩 시간 (스펙 문서 활용)
```

측정 결과를 월간으로 공유하면 다음 효과가 있습니다.

```
측정 결과 공유의 효과:
  → 파일럿 팀이 구체적인 수치로 성공을 증명
  → 다른 팀 리더가 자발적으로 도입을 요청
  → 강제하지 않아도 조직 전체에 자연스럽게 확산
```

---

## 9. SDD와 AI-Native 설계 패러다임의 연결

SDD는 현재 AI와 협업하기 위한 실용적인 방법론입니다. 하지만 더 크게 보면, SDD는 소프트웨어 개발이 **AI-Native 패러다임**으로 전환되는 과정에서의 중간 다리 역할을 합니다.

---

### 9.1 AI-Native 설계 패러다임이란?

"AI를 도구로 사용하는 개발"과 "AI-Native 개발"은 다릅니다.

```
[AI를 도구로 사용하는 개발]
  기존 프로세스: 기획 → 설계 → 코딩 → 테스트 → 배포
                                 ↑
                      AI가 이 단계에서 보조
  결과: 개발 속도 향상. 프로세스 자체는 변화 없음.

[AI-Native 개발]
  AI가 프로세스 자체를 재구성:
  의도(Intent) → 스펙 → [AI가 코드/테스트/문서 생성] → 검증 → 배포
  결과: 개발 패러다임 자체가 변화.
        개발자의 역할이 "코더"에서 "명세 설계자"로 전환.
```

AI-Native 설계의 핵심 특징은 다음과 같습니다.

**첫째, 스펙이 실행 가능한 인터페이스다.** AI-Native 환경에서 스펙은 단순한 문서가 아닙니다. AI 에이전트가 직접 읽고 실행할 수 있는 인터페이스입니다. 잘 작성된 스펙은 AI에게 API 호출과 같습니다.

**둘째, 인간의 역할이 "코더"에서 "명세 설계자"로 전환된다.** 카파시가 말한 에이전틱 엔지니어링의 핵심이 바로 이것입니다. 개발자가 AI를 조율하고 감독하는 역할로 전환됩니다.

**셋째, AI가 검증(Verification)의 주체가 된다.** 현재는 인간이 AI가 생성한 코드를 검증합니다. AI-Native 환경에서는 AI가 스스로 스펙을 기준으로 생성된 코드를 검증합니다.

---

### 9.2 SDD가 AI-Native를 가능하게 하는 방법

SDD는 AI-Native 패러다임으로의 전환을 구조적으로 지원합니다.

#### 스펙이 AI에게 실행 가능한 인터페이스가 되는 방법

```
[일반 자연어 요청]
"로그인 기능을 만들어줘."
→ AI가 해석의 자유도가 너무 높아 예측 불가능한 결과

[SDD 스펙 기반 요청]
constitution.md: 기술 스택, 보안 원칙 (Memory Bank)
spec.md:         사용자 스토리, 수용 기준, 엣지 케이스 (What)
plan.md:         아키텍처, API 설계 (How)
tasks.md:        구현 순서, 의존 관계 (When/Who)

→ AI가 명확한 기준으로 코드 생성, 예측 가능한 결과
```

SDD가 제공하는 구조화된 스펙은 AI가 자유도 높은 "바이브 코딩"을 하지 않고, 제약된 범위 내에서 정확하게 동작하도록 만드는 **가이드레일(Guardrail)** 역할을 합니다.

#### 멀티 에이전트 협업에서의 스펙

AI 에이전트가 한 개가 아니라 여러 개가 협업하는 멀티 에이전트 시스템에서 스펙은 더욱 중요합니다.

```
[멀티 에이전트 시스템 예시]

스펙 → 에이전트 A: 백엔드 코드 생성
      ↓ (공유 스펙 참조)
      에이전트 B: 프론트엔드 코드 생성
      ↓ (동일 수용 기준 참조)
      에이전트 C: 테스트 코드 생성
      ↓ (동일 API 계약 참조)
      에이전트 D: API 문서 생성

→ 여러 에이전트가 동일한 스펙을 참조하기 때문에
  각자 생성한 결과물이 서로 호환됨
```

스펙이 없는 멀티 에이전트 시스템은 에이전트마다 다른 해석으로 서로 충돌하는 결과물을 만들어 냅니다.

---

### 9.3 AI 능력과 스펙의 가치: 비례 관계

흥미로운 역설이 있습니다. AI가 더 강력해질수록 스펙의 중요성은 줄어드는 것이 아니라 **더 커집니다.**

```
AI 능력 발전에 따른 스펙의 가치 변화:

2023년 AI (GPT-3.5 수준):
  → 스펙 없이도 어느 정도 코드 생성 가능
  → 하지만 품질이 낮아 결국 인간이 대부분 수정
  → 스펙의 가치: 보통

2024~2025년 AI (GPT-4, Claude 3 수준):
  → 복잡한 기능도 구현 가능
  → 하지만 스펙이 없으면 예측 불가능한 결과
  → AI 실수를 발견하고 수정하는 비용 증가
  → 스펙의 가치: 중요

2026년 이후 AI (에이전틱 수준):
  → 독립적으로 전체 시스템을 구축 가능
  → 스펙이 없으면 완전히 예측 불가능
  → AI가 잘못 판단한 것을 수정하는 비용이 매우 높음
  → 스펙의 가치: 결정적
```

이 역설의 이유는 다음과 같습니다. AI가 강력해질수록 **AI가 잘못된 방향으로 달려가는 속도**도 빨라집니다. 스펙이 없다면 AI가 더 빠르게, 더 많은 코드를 생성하지만, 그 방향이 틀렸을 때 돌이키는 비용도 비례하여 커집니다.

> **비유**  
> 자전거는 느리지만 방향이 틀려도 쉽게 돌아올 수 있습니다.  
> 고속열차는 빠르지만, 방향이 틀리면 돌이키기 어렵습니다.  
> AI가 강력해질수록 고속열차와 같아집니다. 스펙은 열차의 레일(Rail)입니다.

---

### 9.4 SDD의 위치: 현재와 미래 사이의 다리

SDD는 현재의 실용적 방법론이면서 동시에 미래 AI-Native 개발의 기반을 만드는 작업입니다.

```
현재 (2026): Human → Spec → AI → Code → Human Verification
가까운 미래: Human → Spec → AI → Code → AI Verification → Human Approval
먼 미래:     Human → Intent → AI → Spec → AI → Code → AI Verification
```

각 단계에서 변하는 것과 변하지 않는 것이 있습니다.

```
AI 발전에 따라 변하는 것:
  ├── AI가 담당하는 작업의 범위 (점점 확대)
  ├── 스펙의 자동화 수준 (AI가 스펙 초안을 생성)
  └── 검증의 주체 (인간 → AI 보조 → AI 주도)

변하지 않는 것:
  ├── 스펙이 AI 협업의 핵심 인터페이스라는 사실
  ├── 인간이 의도(Intent)와 가치(Value)를 정의해야 한다는 사실
  └── 검증과 최종 승인에 인간의 판단이 필요하다는 사실
```

SDD를 지금 도입하는 것은 단순히 현재 AI 도구를 더 잘 쓰는 것이 아닙니다. **미래의 AI-Native 개발 환경에 조직이 적응할 수 있는 역량을 미리 구축**하는 것입니다.

> **🔑 최종 메시지**
>
> AI가 코드를 생성하는 시대에서, 개발자의 가장 중요한 역량은 **"AI가 이해할 수 있는 언어로 의도를 정확하게 표현하는 능력"** 입니다.
>
> SDD는 그 언어를 배우고 실천하는 방법론입니다.

---

## 10. 정리

AI 시대의 소프트웨어 개발은 빠르게 진화하고 있습니다.

```
바이브 코딩        AI 코딩의 현실        SDD           에이전틱 엔지니어링   AI-Native
(2025년 초)  →   (한계 발견)      →  (해결책)  →   (실천 프레임워크)  →  (미래)
"일단 만들자"     "왜 안 되지?"        "구조가            "잘 만들자"        "스펙이
                                       필요하다"                             소프트웨어다"
```

---

### 핵심 교훈 요약

**AI 코딩의 현실**에서 배운 것:

- 자유 형식 지시는 AI를 혼란스럽게 만든다
- 구조 없는 많은 정보도 AI에게 도움이 되지 않는다
- 진짜 문제는 커뮤니케이션 구조의 부재다

**SDD 이론**에서 배운 것:

- Böckeler가 정의한 SDD: 사양이 코드의 소스이자 진실의 근원
- 3가지 구현 레벨: Level 1(Spec-First) → Level 2(Spec-Anchored) → Level 3(Spec-as-Source)
- 스펙은 "무엇을", Memory Bank는 "어떻게" — 두 가지가 함께 완전한 맥락 제공

**Less is More 스펙 설계**에서 배운 것:

- 계층적 스펙 구조: Layer 0(헌법) → Layer 1(도메인) → Layer 2(기능) → Layer 3(작업)
- 좋은 스펙: 측정 가능하고, 테스트 가능하고, 결과 중심적이다
- 나쁜 스펙: 모호하고, 구현을 지시하고, 검증 불가능하다

**엔터프라이즈 레거시 전환**에서 배운 것:

- 레거시 전환에서 SDD는 "코드를 전환하기 전에 동작을 먼저 명세로 만든다"
- 기존 문서의 5~10%만이 AI가 실제 사용할 수 있는 핵심 정보다
- 대규모 프로젝트: Multi-Project API 참조 문제, 스펙 품질 균일성, 작업 크기별 유연성
- Big Bang 전환은 실패한다 → 20/80 규칙, 파일럿, 병행운영, 측정의 점진적 전략 필요

**AI-Native 패러다임**에서 배운 것:

- AI가 강력해질수록 스펙의 가치는 더 커진다
- 스펙은 멀티 에이전트 시스템의 공통 인터페이스다
- SDD는 현재 방법론이자 AI-Native 미래를 준비하는 투자다

---

> **핵심 요약**
>
> - **바이브 코딩**: AI 활용의 첫 단계. 빠르지만 품질이 불안정하다.
> - **AI 코딩의 현실**: 자유 형식 지시와 비구조화 정보는 한계가 있다.
> - **SDD**: 커뮤니케이션 구조를 제공하는 사양 중심 개발 프레임워크.
>   - Level 1 (Spec-First): 코딩 전 문서화 → 지금 당장 실천 가능
>   - Level 2 (Spec-Anchored): 사양이 개발의 기준점
>   - Level 3 (Spec-as-Source): 사양에서 코드 생성
> - **Less is More**: 계층적 스펙 구조로 AI에게 필요한 정보만 정확하게 제공.
> - **레거시 전환**: 코드 전환 전에 스펙을 먼저 도출. 점진적 전략으로 조직 변화.
> - **AI-Native 연결**: SDD는 AI가 강력해질수록 더 중요해지는 미래 지향 방법론.
> - **에이전틱 엔지니어링**: AI를 조율하고 감독하는 전문 개발 방식.
> - **GitHub Spec Kit**: SDD를 실제 프로젝트에서 적용하기 위한 도구 모음.

다음 강의에서는 GitHub Spec Kit을 활용하여 이 SDD 워크플로를 실제로 구현하는 방법을 살펴보겠습니다.

---

*※ 본 문서는 강의용으로 작성되었습니다. 내용 관련 문의는 강의 담당자에게 연락하세요.*

*참고 문헌: Birgitta Böckeler, "Specification-Driven Development", martinfowler.com / Andrej Karpathy, "Vibe Coding" and "Agentic Engineering", X(Twitter) / AWS Kiro 공식 문서 / GitHub Spec Kit 공식 문서 / Tessl.io 공식 문서*
