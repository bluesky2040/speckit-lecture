# GitHub Spec Kit 실습 가이드 — 3차시
## 주제: 로그인 화면 만들기 + 명세 수정 + 기능 추가
## 환경: Codespace 우측 채팅창 (GitHub Copilot · Auto 모델)

> **이 실습은 3차시입니다.**  
> 1차시(SDD 방법론) → 2차시(Spec Kit 가이드) 에서 배운 개념을 직접 실행합니다.  
> 명령어의 의미를 먼저 이해한 상태에서 실습을 진행하면 훨씬 효과적입니다.

**목표**: GitHub Codespace 환경에서 SDD 전체 흐름을 체험하고, 로그인 화면을 완성한다  
**사용 도구**: GitHub Codespaces + 우측 채팅창 (GitHub Copilot · Auto 모델) + specify CLI  
**결과물**: HTML + CSS + JavaScript 기반 로그인 화면 (명세 변경 + 3가지 기능 포함)

---

## 목차

1. [이 실습의 두 가지 창 구분](#이-실습의-두-가지-창-구분)
2. [실습 전 준비 사항](#실습-전-준비-사항)
3. [실습 개요](#실습-개요)
4. [Part 1. Codespace 생성](#part-1-codespace-생성-약-15분)
5. [Part 2. 도구 설치](#part-2-도구-설치-약-10분)
6. [Part 3. 프로젝트 초기화](#part-3-프로젝트-초기화-약-10분)
7. [Part 4. 기본 로그인 화면 구현](#part-4-sdd-실습-1단계--기본-로그인-화면-약-65분)
   - Step 1. Constitution (개발 원칙 + 테스트 원칙)
   - Step 4. Checklist (specify 후 — UX·요구사항 점검)
   - Step 9. Checklist (implement 후 — constitution 기반 품질 검증)
8. [Part 5. 결과물 실행 및 테스트](#part-5-결과물-실행-및-테스트-약-15분)
9. [Part 6. 기존 명세(001) 변경 대처](#part-6-sdd--기존-명세001-변경-대처-약-30분)
10. [Part 7. 로그인 횟수 제한 추가](#part-7-sdd-실습-2단계--로그인-횟수-제한-추가-약-30분)
11. [Part 8. 아이디 저장 기능 추가](#part-8-sdd-실습-3단계--아이디-저장-기능-추가-약-30분)
12. [Part 9. 전체 산출물 구조 확인](#part-9-전체-산출물-구조-확인)
13. [트러블슈팅](#트러블슈팅)
14. [전체 명령어 요약표](#전체-명령어-요약표)
15. [실습 진행 시간 배분](#실습-진행-시간-배분)

---

## 이 실습의 두 가지 창 구분

이 실습에서는 Codespace 안의 **두 가지 창**을 번갈아 사용한다.  
헷갈리지 않도록 아래 구분을 먼저 확인한다.

```
┌─────────────────────────────────────┬──────────────────────────────┐
│         왼쪽: 터미널                │     오른쪽: 채팅창           │
│  (하단 패널, 프롬프트: $)           │  (GitHub Copilot · Auto)     │
│                                     │                              │
│  $ pwd                              │  💬 여기에 프롬프트 입력     │
│  $ ls .specify/specs/               │                              │
│  $ cat spec.md                      │  /speckit.constitution ...   │
│  $ python3 -m http.server 8080      │  @workspace 아래 파일을...   │
│                                     │                              │
│  bash 명령어 실행 전용              │  AI 와의 대화 전용           │
└─────────────────────────────────────┴──────────────────────────────┘
```

> **채팅창 여는 방법**  
> Codespace VS Code 화면에서 우측 상단의 채팅 아이콘(말풍선 모양)을 클릭한다.  
> 또는 단축키 `Ctrl+Alt+I` (Windows/Linux) / `Cmd+Ctrl+I` (Mac) 를 누른다.  
> 채팅창 상단에 모델이 **"Auto"** 로 표시되어 있는지 확인한다.

### 채팅창과 specify CLI 의 역할 분담

| 역할 | 사용 도구 | 설명 |
|---|---|---|
| SDD 명령 실행 | **채팅창 (Copilot · Auto)** | `/speckit.*` 명령어를 프롬프트로 직접 입력 |
| 파일 생성·저장 확인 | **터미널** | `ls`, `cat` 으로 생성 결과 확인 |
| specify CLI | **터미널** | 프로젝트 초기화(`specify init`)에만 사용 |

> **중요**: 이 실습에서 `specify CLI` 는 프로젝트 구조를 만드는 **초기화 도구**로만 사용한다.  
> 이후 모든 SDD 명령(`/speckit.constitution`, `/speckit.specify` 등)은  
> **채팅창에 텍스트로 직접 입력**하여 Copilot(Auto 모델)이 처리하도록 한다.

---

## 실습 전 준비 사항

| 필요 계정 | 가입 주소 | 비고 |
|---|---|---|
| GitHub 계정 | https://github.com/signup | Codespace + Copilot 모두 GitHub 계정으로 인증 |

> **Google 계정 불필요**: 이전 Gemini CLI 방식과 달리 Google 계정 인증이 필요 없다.  
> GitHub 계정 하나로 Codespace 와 우측 채팅창(Copilot)을 모두 사용할 수 있다.

**브라우저**: Chrome 또는 Edge 권장

---

## 실습 개요

SDD(Spec-Driven Development)는 코드를 먼저 짜지 않는다.  
**무엇을, 왜 만드는지** 먼저 정의하고, AI 에이전트가 구현하도록 지시하는 방법론이다.

### SDD 전체 흐름

```
constitution → specify → clarify → checklist → plan → tasks → analyze → implement
(원칙 설정)  (요구사항) (모호함 제거) (명세점검)  (기술설계) (작업목록) (최종점검)  (코드생성)
   1회만        ①         ②            ③         ④        ⑤          ⑥          ⑦
```

> **checklist 위치가 중요한 이유**  
> `clarify` 로 모호함을 제거한 직후, `plan` 으로 넘어가기 전에 실행해야  
> 명세의 구멍을 설계 단계에서 발견할 수 있다.  
> implement 이후에 발견하면 코드까지 다시 작성해야 하는 상황이 발생한다.

> **constitution.md 가 테스트와 연결되는 원리**  
> `constitution.md` 에 테스트 원칙을 명시하면, `checklist testing` 실행 시  
> Copilot(Auto) 이 constitution.md 를 참조하여 **테스트 기준을 자동으로 반영**한다.
>
> ```
> constitution.md 에 [테스트 원칙] 명시  (Step 1 에서 1회 작성)
>       ↓
> implement 완료 후 채팅창에서 checklist testing 요청
>       ↓
> Copilot(Auto) 이 constitution.md + spec.md 를 동시에 참조
>       ↓
> 프로젝트 원칙(단일 파일, 한국어 주석, 모바일 지원 등)에 맞는 테스트 항목 자동 생성
> ```

### 기능을 추가할 때의 흐름

`constitution` 은 프로젝트 전체에 적용되는 원칙이므로 **최초 1회만** 작성하면 된다.  
기능이 추가될 때마다 `specify` 부터 `implement` 까지 새 번호로 다시 진행한다.

```
[최초 구현]  constitution → specify(001) → clarify → checklist → plan → tasks → analyze → implement
[기능 추가]               → specify(002) → clarify → checklist → plan → tasks → analyze → implement
[기능 추가]               → specify(003) → clarify → checklist → plan → tasks → analyze → implement
```

### 기존 명세를 수정할 때의 흐름

이미 구현이 완료된 기능의 **요구사항이 변경**되는 경우, SDD 에서는 아래 순서를 따른다.

```
[명세 수정]  spec.md 직접 수정 → clarify → checklist → analyze → plan(재실행) → tasks(재실행) → implement
                  ↑
          코드가 아닌 명세부터 수정하는 것이 SDD 의 핵심 원칙이다
```

### 이번 실습에서 다루는 시나리오

| 파트 | 시나리오 | SDD 번호 |
|---|---|---|
| Part 4 | 기본 로그인 화면 구현 (아이디/비밀번호 검증) | 001 최초 구현 |
| Part 6 | 001 명세 변경 — 로그인 성공 후 동작 추가 | 001 명세 수정 |
| Part 7 | 로그인 횟수 제한 + 카운트다운 기능 추가 | 002 신규 추가 |
| Part 8 | 아이디 저장 (localStorage) 기능 추가 | 003 신규 추가 |

---

## Part 1. Codespace 생성 (약 15분)

### 1-1. 빈 레포지토리 만들기

1. https://github.com 에 로그인한다.
2. 오른쪽 상단 `+` 버튼 클릭 후 `New repository` 를 선택한다.
3. 아래와 같이 입력한다.

| 항목 | 입력값 |
|---|---|
| Repository name | `login-page-lab` |
| Visibility | Public 또는 Private 선택 |
| Add a README file | **반드시 체크** (체크 없으면 Codespace 생성 불가) |

4. `Create repository` 버튼을 클릭한다.

### 1-2. Codespace 실행

1. 생성된 레포지토리 페이지에서 초록색 `Code` 버튼을 클릭한다.
2. `Codespaces` 탭을 선택한다.
3. `Create codespace on main` 을 클릭한다.
4. 브라우저에서 VS Code 화면이 열릴 때까지 1~2분 기다린다.

터미널이 보이지 않으면 상단 메뉴에서 `Terminal → New Terminal` 을 선택한다.

### 1-3. 채팅창 열기 및 모델 확인

1. VS Code 우측 상단의 채팅 아이콘(💬 말풍선)을 클릭한다.
2. 채팅창 상단에 **"Auto"** 모델이 선택되어 있는지 확인한다.
3. "Auto" 가 아니면 드롭다운을 클릭하여 **Auto** 로 변경한다.

```
채팅창 상단 확인:
┌─────────────────────────────────┐
│  GitHub Copilot Chat    [Auto▼] │  ← "Auto" 가 보이면 정상
│─────────────────────────────────│
│                                 │
│  Ask Copilot a question...      │  ← 여기에 프롬프트 입력
└─────────────────────────────────┘
```

### 1-4. 현재 위치 확인

**터미널** 에서 실행한다.

```bash
pwd
```

예상 출력:

```
/workspaces/login-page-lab
```

> **확인 포인트 ✅**  
> `/workspaces/login-page-lab` 이 출력되고, 채팅창에 **Auto** 모델이 표시되면 Part 1 완료.

---

## Part 2. 도구 설치 (약 10분)

> **이 파트는 터미널에서만 실행한다.** 채팅창은 사용하지 않는다.

### 2-1. uv 설치

`uv` 는 Python 패키지를 빠르게 설치하는 도구다. `specify CLI` 설치에 필요하다.

**터미널** 에서 실행한다.

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc
```

설치 확인:

```bash
uv --version
```

예상 출력:

```
uv 0.x.x
```

### 2-2. specify CLI 설치

`specify CLI` 는 `.specify/` 디렉터리 구조와 `.github/prompts/` 명령 파일을 자동 생성해 주는 도구다.

**터미널** 에서 실행한다.

```bash
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git
```

확인:

```bash
specify --version
```

예상 출력:

```
specify 0.x.x
```

> **확인 포인트 ✅**  
> `uv`, `specify` 두 가지 모두 버전이 출력되면 Part 2 완료.  
> (Gemini CLI 는 이 실습에서 사용하지 않으므로 설치하지 않는다.)

---

## Part 3. 프로젝트 초기화 (약 10분)

> **이 파트는 터미널에서만 실행한다.**

### 3-1. specify 프로젝트 생성

현재 위치(`/workspaces/login-page-lab`) 에서 실행한다.

```bash
specify init login-project --integration copilot
```

> **`--integration copilot`** 을 사용하면 채팅창(Copilot)이 인식하는  
> `.github/prompts/` 폴더에 `/speckit.*` 명령 파일이 생성된다.

### 3-2. 프로젝트 폴더로 이동

```bash
cd login-project
```

이동 후 현재 위치 확인:

```bash
pwd
```

예상 출력:

```
/workspaces/login-page-lab/login-project
```

> **중요**: 이후 모든 터미널 명령어는 반드시 이 폴더(`login-project`) 안에서 실행한다.

### 3-3. 생성된 구조 확인

```bash
ls -la
```

예상 출력:

```
.github/
.specify/
README.md
```

명령 파일 확인:

```bash
ls .github/prompts/
```

아래 파일들이 모두 보이면 정상이다.

```
speckit.analyze.prompt.md
speckit.checklist.prompt.md
speckit.clarify.prompt.md
speckit.constitution.prompt.md
speckit.implement.prompt.md
speckit.plan.prompt.md
speckit.specify.prompt.md
speckit.tasks.prompt.md
```

> **파일 형식이 `.prompt.md`** 인 것을 확인한다.  
> Copilot 통합은 `.github/prompts/` 폴더의 `.prompt.md` 형식을 사용한다.  
> (Gemini CLI 는 `.gemini/commands/*.toml` 형식을 사용하므로 다르다.)

`.specify` 내부도 확인한다.

```bash
ls .specify/
```

예상 출력:

```
memory/   specs/   templates/
```

### 3-4. 채팅창에서 워크스페이스 인식 확인

**채팅창** 에 아래를 입력하여 Copilot(Auto) 이 프로젝트를 인식하는지 확인한다.

```
@workspace 현재 프로젝트 폴더 구조를 간략히 설명해줘.
.github/prompts/ 폴더에 어떤 파일들이 있는지 알려줘.
```

Copilot 이 `.github/prompts/` 폴더의 `speckit.*.prompt.md` 파일 목록을 포함하여  
응답하면 정상이다.

> **`@workspace` 사용 이유**  
> `@workspace` 를 붙이면 Copilot(Auto) 이 현재 열려 있는 Codespace 의  
> 파일 전체를 컨텍스트로 참조한다.  
> SDD 명령 실행 시 spec.md, plan.md 등 생성된 파일을 자동으로 읽을 수 있다.

> **확인 포인트 ✅**  
> `.github/prompts/` 폴더와 `speckit.*.prompt.md` 파일 목록이 보이면 Part 3 완료.

---

## Part 4. SDD 실습 1단계 — 기본 로그인 화면 (약 65분)

### 실습 목표

HTML + CSS + JavaScript 로 구성된 로그인 화면을 SDD 9단계를 거쳐 만든다.  
각 단계가 1차시·2차시에서 배운 SDD 어느 단계인지 확인하면서 진행한다.

> **채팅창 입력 방식 안내**  
> 이 파트부터 모든 SDD 명령은 **채팅창** 에 입력한다.  
> 아래 `채팅창 입력` 블록의 내용을 **그대로 복사하여 채팅창에 붙여넣기** 하면 된다.

---

### Step 1. Constitution — 프로젝트 원칙 설정

> **[SDD 기반 원칙]** — `constitution.md` 는 프로젝트 전체에 적용되는 규칙이다.  
>
> **테스트 원칙도 여기에 함께 작성하는 이유**  
> constitution.md 는 Copilot(Auto) 이 모든 명령에서 **항상 참조**하는 배경 지식이다.  
> 테스트 기준을 여기에 명시하면, 나중에 `checklist testing` 을 요청할 때  
> "이 프로젝트의 테스트 방식은 이렇다"를 이미 알고 있는 상태에서 체크리스트를 만든다.  
> 즉, 테스트 원칙을 매번 반복해서 입력할 필요 없이 constitution.md 에 한 번만 적으면 된다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.constitution
이 프로젝트는 교육용 웹 로그인 화면입니다.

[개발 원칙]
- HTML, CSS, JavaScript 만 사용한다 (외부 프레임워크 사용 금지)
- 하나의 index.html 파일로 구성한다
- 코드에 한국어 주석을 달아 초심자가 이해할 수 있도록 한다
- 모바일과 데스크탑 모두에서 보기 좋게 만든다
- 불필요하게 복잡한 구조는 피하고 단순하게 작성한다
- 모든 응답과 파일 내용은 한국어로 작성한다

[테스트 원칙]
- implement 완료 후 반드시 checklist testing 으로 품질을 검증한다
- 테스트는 브라우저에서 직접 시나리오를 실행하는 방식으로 진행한다 (자동화 테스트 도구 미사용)
- 각 수용 기준(Given-When-Then)에 대응하는 테스트 시나리오를 1:1 로 검증한다
- 정상 흐름(Happy Path)과 오류 흐름(Error Path)을 모두 테스트한다
- 모바일 화면(375px 기준)에서도 동작을 확인한다
- 개발 원칙 준수 여부(단일 파일, 한국어 주석, 외부 라이브러리 미사용)도 테스트 항목에 포함한다

위 내용을 .specify/memory/constitution.md 파일로 저장해줘.
```

Copilot(Auto) 이 파일 생성을 제안하면 **"Accept"** 를 클릭하거나,  
파일 내용을 직접 알려주면 **터미널** 에서 저장한다.

**터미널** 에서 저장 여부를 확인한다.

```bash
cat .specify/memory/constitution.md
```

`[개발 원칙]` 과 `[테스트 원칙]` 두 섹션이 모두 포함되어 있으면 정상이다.

> **Copilot 이 파일을 직접 저장하지 않는 경우**  
> Copilot(Auto) 이 파일 내용을 채팅창에 출력하기만 하는 경우,  
> 아래 터미널 명령으로 직접 파일을 만든다.
>
> ```bash
> mkdir -p .specify/memory
> nano .specify/memory/constitution.md
> ```
>
> 편집기가 열리면 Copilot 이 출력한 내용을 붙여넣고 `Ctrl+X → Y → Enter` 로 저장한다.

> **확인 포인트 ✅**  
> `constitution.md` 에 `[개발 원칙]` + `[테스트 원칙]` 섹션이 포함되어 있으면 Step 1 완료.

---

### Step 2. Specify 001 — 요구사항 정의

> **[SDD 1단계: Specify]** — "무엇을, 왜 만드는가"를 정의하는 단계다.  
> 1차시에서 배운 사용자 스토리·수용 기준·엣지 케이스가 이 단계에서 생성된다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.specify
사용자가 아이디와 비밀번호를 입력하여 로그인하는 화면을 만들어줘.

[기능 요구사항]
- 아이디 입력 필드가 있다
- 비밀번호 입력 필드가 있다 (입력 내용은 별표로 가려진다)
- 로그인 버튼이 있다
- 아이디 또는 비밀번호가 비어 있으면 "아이디와 비밀번호를 입력하세요" 메시지를 표시한다
- 아이디가 'admin', 비밀번호가 '1234' 이면 "로그인 성공!" 메시지를 표시한다
- 아이디나 비밀번호가 틀리면 "아이디 또는 비밀번호가 올바르지 않습니다" 메시지를 표시한다
- 비밀번호 보이기/숨기기 버튼이 있다

결과를 .specify/specs/001-login-page/spec.md 파일로 저장해줘.
사용자 스토리, 수용 기준(Given-When-Then), 엣지 케이스를 포함해줘.
```

**터미널** 에서 생성 여부를 확인한다.

```bash
ls .specify/specs/
# 예상 출력: 001-login-page/

cat .specify/specs/001-login-page/spec.md
```

---

### Step 3. Clarify — 모호한 부분 명확하게

> **[SDD 1단계: Specify 보완]** — AI 가 추가로 물어볼 수 있는 모호한 부분을 미리 명확히 한다.  
> 2차시에서 "구현 중에 발견하면 수정 비용이 10배"라고 배운 바로 그 단계다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.clarify
.specify/specs/001-login-page/spec.md 를 참고해서,
다음 내용을 명확히 반영하여 spec.md 를 보완해줘.

- 화면 배경은 밝은 회색 계열로 한다
- 로그인 폼은 화면 정중앙에 카드 형태로 배치한다
- 오류 메시지는 빨간색으로 표시한다
- 성공 메시지는 초록색으로 표시한다
- 로그인 버튼은 파란색으로 한다
- 페이지 제목은 "로그인" 으로 한다
- Enter 키를 눌러도 로그인이 시도되도록 한다

변경된 내용을 .specify/specs/001-login-page/spec.md 에 반영해줘.
```

Copilot 이 파일 수정을 제안하면 **"Accept"** 를 클릭한다.

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-login-page/spec.md
```

---

### Step 4. Checklist — 명세 완성도 점검 (specify 후)

> **[SDD 1단계: Specify 최종 검증]** — `clarify` 로 모호함을 제거한 직후, `plan` 으로 넘어가기 전에  
> spec.md 에 빠진 항목이 없는지 도메인별로 점검한다.  
> **이 단계에서 발견된 구멍은 수정 비용이 가장 작다.**

#### 4-1. 기본 요구사항 체크리스트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist
.specify/specs/001-login-page/spec.md 를 읽고,
요구사항 완성도 관점에서 정밀 분석하여 체크리스트를 만들어줘.

- 모든 사용자 스토리에 수용 기준이 있는가?
- 모호한 표현이 없는가?
- constitution.md 원칙을 위반하는 항목이 없는가?
- 이 명세만 보고 plan.md 를 작성할 수 있는가?

누락된 항목이 있으면 어떻게 수정해야 하는지도 알려줘.
결과를 .specify/specs/001-login-page/checklists/requirements.md 로 저장해줘.
```

**터미널** 에서 저장 여부를 확인한다.

```bash
cat .specify/specs/001-login-page/checklists/requirements.md
```

#### 4-2. UX 관점 체크리스트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist ux
.specify/specs/001-login-page/spec.md 를 읽고,
UX 관점에서 빠진 요구사항이 없는지 점검해줘.

다음 항목을 중심으로 확인해줘:
- 사용자 흐름: 로그인 성공/실패 후 어떻게 되는가?
- 오류 메시지: 모든 오류 상황에 구체적인 메시지가 있는가?
- 접근성: 키보드만으로 사용 가능한가?
- 반응형: 모바일 지원이 명세에 있는가?
- 버튼 상태: 처리 중 UI 피드백이 있는가?

빠진 항목이 있으면 spec.md 에 무엇을 추가해야 하는지 알려줘.
결과를 .specify/specs/001-login-page/checklists/ux.md 로 저장해줘.
```

**터미널** 에서 저장 여부를 확인한다.

```bash
cat .specify/specs/001-login-page/checklists/ux.md
```

#### 4-3. 체크리스트 결과 처리

`[ ]` (미통과) 항목이 있으면 다음과 같이 처리한다.

```
[미통과 항목 발견 시 처리 순서]

1. 채팅창에서 수정 방법을 물어본다
   예) "CHK003 항목 — spec.md 에 어떤 내용을 추가해야 해?
       .specify/specs/001-login-page/spec.md 를 직접 수정해줘."

2. Copilot 이 파일 수정을 제안하면 Accept 클릭

3. 체크리스트를 재실행하여 통과 여부를 확인한다
   채팅창에 "@workspace 방금 수정한 spec.md 기준으로 ux.md 체크리스트를 재실행해줘." 입력

4. 모든 항목 통과 → Step 5 (Plan) 로 진행
```

> **확인 포인트 ✅**  
> `checklists/requirements.md`, `checklists/ux.md` 생성 확인  
> 미통과 항목 spec.md 보완 완료 → Step 5 로 진행.

---

### Step 5. Plan — 기술 설계

> **[SDD 2단계: Plan]** — "어떻게 만들 것인가"를 기술적으로 설계하는 단계다.  
> `constitution.md` 의 원칙(단일 HTML 파일, 외부 라이브러리 금지)이 자동으로 반영된다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.plan
.specify/specs/001-login-page/spec.md 와 .specify/memory/constitution.md 를 읽고,
기술 구현 계획을 세워줘.

[구현 방향]
- 단일 HTML 파일(index.html) 하나로 모든 것을 구현한다
- CSS 는 <style> 태그 안에 작성한다
- JavaScript 는 <script> 태그 안에 작성한다
- 외부 라이브러리나 CDN 을 사용하지 않는다
- Flexbox 를 사용하여 중앙 정렬한다

결과를 .specify/specs/001-login-page/plan.md 로 저장해줘.
```

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-login-page/plan.md
```

---

### Step 6. Tasks — 작업 목록 생성

> **[SDD 3단계: Task]** — `plan.md` 를 바탕으로 즉시 실행할 수 있는 작업 목록을 만든다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.tasks
.specify/specs/001-login-page/plan.md 를 읽고,
개발자가 순서대로 실행할 수 있는 작업 목록을 만들어줘.
각 Task 는 체크박스([ ]) 형식으로 작성해줘.
결과를 .specify/specs/001-login-page/tasks.md 로 저장해줘.
```

**터미널** 에서 확인한다.

```bash
cat .specify/specs/001-login-page/tasks.md
```

예상 출력:

```
[ ] Task 1: HTML 기본 구조 및 폼 요소 작성
[ ] Task 2: CSS로 전체 레이아웃 및 카드 스타일 작성
[ ] Task 3: 입력 필드 및 버튼 스타일 작성
[ ] Task 4: JavaScript 유효성 검사 로직 작성
[ ] Task 5: 비밀번호 보이기/숨기기 기능 작성
[ ] Task 6: Enter 키 이벤트 처리 작성
[ ] Task 7: 성공/오류 메시지 표시 기능 작성
```

---

### Step 7. Analyze — 최종 점검

> **[SDD 2단계: Plan 검증]** — `spec.md`, `plan.md`, `tasks.md` 간 불일치를 자동으로 탐지한다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.analyze
아래 세 파일을 모두 읽고, 내용이 서로 일치하는지 점검해줘.

- .specify/specs/001-login-page/spec.md
- .specify/specs/001-login-page/plan.md
- .specify/specs/001-login-page/tasks.md

spec.md 의 모든 수용 기준이 plan.md 와 tasks.md 에 반영되어 있는지 확인해줘.
불일치하는 항목이 있으면 어떻게 수정해야 하는지 알려줘.
```

"문제 없음" 또는 "모든 항목 일치" 메시지가 나오면 다음 단계로 진행한다.  
불일치가 발견되면 Copilot 이 안내하는 수정 사항을 `spec.md` 또는 `plan.md` 에 반영한 후 재실행한다.

---

### Step 8. Implement — 코드 생성

> **[SDD 4단계: Implement]** — 사양을 코드로 번역하는 단계다.  
> `spec.md` 의 수용 기준이 코드에 1:1 로 반영되는지 이후 테스트에서 확인한다.

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.implement
아래 파일들을 모두 읽고, index.html 을 생성해줘.

참고 파일:
- .specify/memory/constitution.md  (개발 원칙 + 테스트 원칙)
- .specify/specs/001-login-page/spec.md
- .specify/specs/001-login-page/plan.md
- .specify/specs/001-login-page/tasks.md

[생성 조건]
- 파일명: index.html (login-project/ 폴더 바로 아래에 저장)
- constitution.md 의 [개발 원칙] 을 반드시 준수한다
  (단일 HTML 파일, 외부 라이브러리 금지, 한국어 주석, 모바일 지원)
- spec.md 의 모든 수용 기준을 코드에 반영한다
- tasks.md 의 모든 Task 항목을 구현한다
```

Copilot 이 `index.html` 파일 생성을 제안하면 **"Accept"** 를 클릭한다.

**터미널** 에서 파일 확인한다.

```bash
ls -la
# index.html 이 생성되어 있어야 한다

wc -l index.html
# 줄 수가 50줄 이상이면 정상적으로 생성된 것이다
```

---

### Step 9. Checklist — 구현 품질 검증 (implement 후)

> **[품질 게이트]** — `implement` 완료 후 생성된 코드가 `spec.md` 의 모든 수용 기준을 충족하는지  
> 테스트 시나리오 관점에서 점검한다.
>
> **constitution.md [테스트 원칙] 이 여기서 작동한다**  
> Step 1 에서 constitution.md 에 `[테스트 원칙]` 을 작성해 두었기 때문에,  
> Copilot(Auto) 은 체크리스트를 만들 때 **두 가지를 동시에 참조**한다.
> - `spec.md` → "이 기능이 무엇을 해야 하는가" (수용 기준)
> - `constitution.md` → "이 프로젝트는 어떻게 테스트해야 하는가" (테스트 원칙)

#### 9-1. constitution 기반 테스트 체크리스트 실행

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist testing
아래 파일들을 모두 읽고, 테스트 체크리스트를 만들어줘.

참고 파일:
- .specify/memory/constitution.md  ← [테스트 원칙] 섹션 반드시 참조
- .specify/specs/001-login-page/spec.md
- index.html

[체크리스트 구성 기준]
1. 수용 기준 검증 (spec.md 기반)
   - 정상 흐름(Happy Path): admin/1234 로그인 성공 시나리오
   - 오류 흐름(Error Path): 빈 칸 제출, 틀린 정보, Enter 키 동작
   - 기능 동작: 비밀번호 보이기/숨기기

2. constitution.md [개발 원칙] 준수 검증
   - 단일 파일 원칙: CSS/JS 가 외부 파일 없이 index.html 안에 있는가?
   - 외부 라이브러리 미사용: CDN 링크가 없는가?
   - 한국어 주석: 주요 함수에 한국어 주석이 달려 있는가?

3. 반응형 검증
   - 브라우저 폭 375px 에서 레이아웃이 깨지지 않는가?

각 항목은 [ ] 체크박스 형식으로 작성하고,
어느 파일의 어느 항목(AC-001 등)에 근거한 것인지 표시해줘.
결과를 .specify/specs/001-login-page/checklists/testing.md 로 저장해줘.
```

**터미널** 에서 저장 여부를 확인한다.

```bash
cat .specify/specs/001-login-page/checklists/testing.md
```

#### 9-2. 브라우저에서 직접 시나리오 실행

> constitution.md 테스트 원칙: "브라우저에서 직접 시나리오를 실행하는 방식으로 진행한다"

체크리스트를 보면서 브라우저에서 **순서대로** 각 항목을 직접 실행하여 체크한다.

**수용 기준 검증 순서:**

```
1. admin/1234 입력 → 로그인 버튼 클릭    → "로그인 성공!" 초록색 메시지 확인
2. 빈 칸 상태로 제출                     → "아이디와 비밀번호를 입력하세요" 확인
3. admin/0000 입력 후 제출              → 오류 메시지 확인
4. 비밀번호 필드에 값 입력 후 👁 클릭  → 비밀번호 텍스트 보임 확인
5. 다시 👁 클릭                         → 비밀번호 가려짐 확인
6. admin/1234 입력 후 Enter 키          → 로그인 성공 확인
```

**constitution 원칙 검증 순서:**

```
7. VS Code 에서 index.html 열어 <style src=...> 없는지 확인  → 단일 파일 원칙
8. <script src=...> 또는 CDN 링크 없는지 확인               → 외부 라이브러리 금지
9. 주요 함수에 한국어 주석 존재 여부 확인                    → 한국어 주석 원칙
10. Chrome DevTools → 모바일 아이콘 클릭 → 폭 375px 설정     → 반응형 확인
```

#### 9-3. 미통과 항목 처리

**채팅창** 에 아래와 같이 수정을 요청한다.

```
# 수용 기준 미통과 예시
@workspace index.html 을 열어봐.
로그인 성공 후 비밀번호 입력 필드가 초기화되지 않아.
spec.md 의 수용 기준에 맞게 index.html 을 수정해줘.

# constitution 원칙 미통과 예시
@workspace index.html 에 한국어 주석이 없는 함수가 있어.
constitution.md 의 한국어 주석 원칙에 맞게 주석을 추가해줘.
```

Copilot 이 수정을 제안하면 **"Accept"** 클릭 → 브라우저에서 `Ctrl+Shift+R` 강제 새로고침 → 재확인

> **확인 포인트 ✅**  
> `checklists/testing.md` 생성 확인 (`cat .specify/specs/001-login-page/checklists/testing.md`)  
> 수용 기준 + constitution 원칙 항목 모두 체크 완료 → Part 4 완료.

---

## Part 5. 결과물 실행 및 테스트 (약 15분)

> **이 파트는 터미널과 브라우저에서 실행한다.**

### 5-1. 브라우저에서 열기

**방법 1: Live Server 확장 프로그램 (권장)**

1. VS Code 왼쪽 사이드바에서 확장 프로그램 아이콘(네모 4개)을 클릭한다.
2. 검색창에 `Live Server` 를 입력한다.
3. `Live Server` (작성자: Ritwick Dey) 를 설치한다.
4. 파일 탐색기에서 `index.html` 을 우클릭한다.
5. `Open with Live Server` 를 선택한다.

**방법 2: Python 내장 서버**

**터미널** 에서 실행한다.

```bash
python3 -m http.server 8080
```

VS Code 하단 `포트(PORTS)` 탭에서 8080 포트의 지구본 아이콘을 클릭한다.

> 서버를 실행한 터미널은 종료하지 않는다.  
> 이후 파트에서 기능이 추가되면 브라우저에서 `Ctrl+Shift+R` (강제 새로고침) 으로 변경 사항을 확인한다.

### 5-2. 기본 동작 테스트

아래 시나리오를 직접 실행하여 결과를 확인한다.

| 시나리오 | 아이디 | 비밀번호 | 예상 결과 |
|---|---|---|---|
| 빈 칸 제출 | (비워둠) | (비워둠) | "아이디와 비밀번호를 입력하세요" |
| 로그인 성공 | admin | 1234 | "로그인 성공!" (초록색) |
| 로그인 실패 | admin | 0000 | "아이디 또는 비밀번호가 올바르지 않습니다" (빨간색) |
| 비밀번호 보이기 | 아무 값 | 아무 값 | 비밀번호 텍스트가 보임 |
| Enter 키 로그인 | admin | 1234 | Enter 키로도 로그인 성공 |

### 5-3. 예상 결과물 화면 구성

```
+------------------------------------------+
|                                          |
|   +----------------------------------+   |
|   |          로그인                  |   |
|   |                                  |   |
|   |  아이디                          |   |
|   |  [                          ]    |   |
|   |                                  |   |
|   |  비밀번호                        |   |
|   |  [                        👁]    |   |
|   |                                  |   |
|   |  [        로그인 버튼        ]   |   |
|   |                                  |   |
|   |  ← 메시지 표시 영역              |   |
|   +----------------------------------+   |
|                                          |
+------------------------------------------+
  배경: 밝은 회색     카드: 흰색 + 그림자
```

---

## Part 6. SDD — 기존 명세(001) 변경 대처 (약 30분)

### 이 파트에서 배우는 것

실제 개발에서 요구사항은 항상 변한다.  
이 파트는 **이미 완성된 기능(001)의 요구사항이 바뀔 때** SDD 에서 어떻게 대처하는지를 체험한다.

### 변경 시나리오

> **기획자 요청**:  
> "로그인 성공 후 '로그인 성공!' 메시지를 2초 동안 보여준 뒤,  
> '환영합니다, admin 님!' 으로 교체하고 비밀번호 입력 필드를 초기화해 주세요."

### 나쁜 방법 vs 올바른 SDD 방법

```
[나쁜 방법 — 코드만 바꾸는 경우]
  index.html 수정 → 배포
       ↓
  spec.md 는 여전히 "로그인 성공!" 만 표시하라고 적혀 있음
  → 6개월 후 신규 팀원이 spec.md 를 보고 잘못된 방향으로 개발
  → 명세와 코드가 어긋나는 기술 부채 누적

[올바른 SDD 방법 — 명세부터 수정]
  spec.md 수정 → clarify → checklist → analyze
                             ↓
                   plan/tasks 영향 범위 자동 탐지 → 수정 → implement
```

---

### Step 1. spec.md 직접 수정

> **[SDD 1단계: Specify 수정]** — 변경 요구사항을 명세에 반영한다.

**터미널** 에서 spec.md 를 연다.

```bash
nano .specify/specs/001-login-page/spec.md
```

또는 VS Code 파일 탐색기에서 `.specify/specs/001-login-page/spec.md` 를 직접 클릭하여 편집한다.

**수정 전 — 기존 수용 기준 (예시):**

```markdown
## 수용 기준

Given: 사용자가 올바른 아이디(admin)와 비밀번호(1234)를 입력했을 때
When:  로그인 버튼을 클릭하면
Then:  "로그인 성공!" 초록색 메시지가 표시된다
```

**수정 후 — 변경된 수용 기준:**

```markdown
## 수용 기준

Given: 사용자가 올바른 아이디(admin)와 비밀번호(1234)를 입력했을 때
When:  로그인 버튼을 클릭하면
Then:  "로그인 성공!" 초록색 메시지가 2초 동안 표시된다
Then:  2초 후 메시지가 "환영합니다, admin 님!" 으로 교체된다
Then:  비밀번호 입력 필드가 초기화된다 (아이디 필드는 유지)

## 변경 이력
- 2026-05-25: 로그인 성공 후 환영 메시지 및 필드 초기화 요구사항 추가 (기획팀 요청)
```

---

### Step 2. Clarify — 변경된 명세 보완

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.clarify
.specify/specs/001-login-page/spec.md 가 수정되었어.
로그인 성공 후 동작 변경에 관해 다음 내용을 명확히 반영해줘.

- 2초 타이머는 setTimeout 을 사용한다
- 메시지 교체 시 애니메이션(페이드) 효과는 사용하지 않는다
- 비밀번호 필드만 초기화하고 아이디 필드는 그대로 둔다
- 이미 타이머가 동작 중이면 새 타이머는 무시한다 (중복 실행 방지)

변경된 내용을 .specify/specs/001-login-page/spec.md 에 반영해줘.
```

---

### Step 3. Checklist — 변경된 명세 점검

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist
.specify/specs/001-login-page/spec.md 가 수정되었어.
변경된 "로그인 성공 후 동작" 부분을 중심으로 누락된 요구사항이 없는지 점검해줘.

특히 아래 항목을 확인해줘:
- 2초 대기 중 사용자가 버튼을 다시 클릭하면 어떻게 되는가?
- 기존 오류 시나리오 수용 기준과 충돌이 없는가?
- constitution.md 의 "단순하게 작성" 원칙을 위반하지 않는가?

누락된 항목이 있으면 spec.md 에 추가해줘.
```

---

### Step 4. Analyze — 변경 영향 범위 탐지

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.analyze
.specify/specs/001-login-page/spec.md 가 수정되었어.
아래 파일들을 모두 읽고, 변경된 내용이 plan.md 및 tasks.md 와 일치하는지 분석해줘.

- .specify/specs/001-login-page/spec.md  (수정됨)
- .specify/specs/001-login-page/plan.md
- .specify/specs/001-login-page/tasks.md

불일치하는 항목이 있으면 각 파일에서 무엇을 수정해야 하는지 구체적으로 알려줘.
```

---

### Step 5. Plan 재실행 — 계획 업데이트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.plan
.specify/specs/001-login-page/spec.md 가 수정되었어.
기존 .specify/specs/001-login-page/plan.md 를 읽고,
다음 변경 사항을 추가하는 계획으로 plan.md 를 업데이트해줘.

- 로그인 성공 시 setTimeout(2000) 으로 2초 대기
- 2초 후 메시지를 "환영합니다, admin 님!" 으로 교체
- 비밀번호 입력 필드 값을 빈 문자열로 초기화
- 타이머 중복 실행 방지 로직 추가

변경된 내용을 .specify/specs/001-login-page/plan.md 에 반영해줘.
```

---

### Step 6. Tasks 재실행 — 작업 목록 업데이트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.tasks
.specify/specs/001-login-page/spec.md 와 plan.md 가 수정되었어.
기존 .specify/specs/001-login-page/tasks.md 를 읽고,
변경된 내용을 반영하여 tasks.md 를 업데이트해줘.
기존 Task 는 유지하고 변경에 필요한 Task 만 추가해줘.
결과를 .specify/specs/001-login-page/tasks.md 에 저장해줘.
```

---

### Step 7. Implement — 변경 코드 반영

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.implement
.specify/specs/001-login-page/spec.md 가 수정되었어.
현재 index.html 을 읽고, 로그인 성공 후 동작을 아래와 같이 수정해줘.

- 로그인 성공 시 setTimeout(2000) 으로 2초 대기 후 메시지 교체
- "환영합니다, admin 님!" 으로 교체
- 비밀번호 필드 초기화 (아이디 필드는 유지)
- 타이머 중복 실행 방지

기존 코드 구조는 최대한 유지하고,
변경된 부분에는 한국어 주석으로 변경 이유를 달아줘.
수정된 내용을 index.html 에 반영해줘.
```

---

### 6-2. 변경 결과 확인

브라우저에서 `Ctrl+Shift+R` (강제 새로고침) 을 누른다.

| 시나리오 | 예상 결과 |
|---|---|
| admin/1234 로 로그인 | "로그인 성공!" 초록색 메시지 표시 |
| 2초 대기 | 메시지가 "환영합니다, admin 님!" 으로 교체 |
| 비밀번호 필드 확인 | 자동으로 비워짐 (아이디 필드는 'admin' 유지) |
| 버튼 재클릭 (2초 내) | 타이머 중복 실행 없음 |
| 틀린 정보 로그인 | 오류 메시지는 기존과 동일 |

#### constitution 테스트 원칙 기반 재검증

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist testing
.specify/specs/001-login-page/spec.md 가 수정되었어.
아래 파일들을 읽고, 변경된 "로그인 성공 후 동작" 부분을 중심으로
constitution.md 의 [테스트 원칙] 을 반영하여 테스트 항목을 추가해줘.

참고 파일:
- .specify/memory/constitution.md
- .specify/specs/001-login-page/spec.md
- index.html

기존 .specify/specs/001-login-page/checklists/testing.md 에
새 항목만 추가해줘 (기존 항목은 삭제하지 마).
추가된 항목에는 "변경분 추가 (2026-05-25)" 라고 표시해줘.
```

---

### 6-3. Git 커밋으로 변경 이력 남기기

**터미널** 에서 실행한다.

```bash
git add .specify/specs/001-login-page/spec.md
git add .specify/specs/001-login-page/plan.md
git add .specify/specs/001-login-page/tasks.md
git add .specify/specs/001-login-page/checklists/testing.md
git add index.html
git commit -m "feat(001): 로그인 성공 후 환영 메시지 + 필드 초기화 추가

- spec.md: 2초 후 메시지 교체 수용 기준 추가
- plan.md: setTimeout 로직 구현 계획 추가
- tasks.md: 관련 Task 항목 추가
- index.html: setTimeout, 메시지 교체, 비밀번호 초기화 구현
변경 요청: 기획팀 (2026-05-25)"
```

> **확인 포인트 ✅**  
> 변경된 동작 테스트 통과, `testing.md` 에 변경분 항목 추가 확인, Git 커밋 완료 → Part 6 완료.

---

## Part 7. SDD 실습 2단계 — 로그인 횟수 제한 추가 (약 30분)

```
이미 완료: constitution → specify(001) → ... → implement  [명세 수정 완료]
지금부터:               → specify(002) → clarify → checklist → plan → tasks → analyze → implement
```

---

### Step 1. Specify 002 — 로그인 횟수 제한 요구사항 정의

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.specify
기존 로그인 화면에 로그인 횟수 제한 기능을 추가해줘.

[기능 요구사항]
- 로그인 실패 횟수를 화면에 표시한다 (예: "로그인 시도: 1/3")
- 로그인 실패를 3회 반복하면 로그인 버튼을 비활성화한다
- 버튼이 비활성화되면 "5초 후 다시 시도하세요" 메시지를 표시한다
- 5초가 지나면 실패 횟수가 초기화되고 버튼이 다시 활성화된다
- 카운트다운 숫자가 화면에 표시된다 (5, 4, 3, 2, 1)

결과를 .specify/specs/002-login-attempts/spec.md 로 저장해줘.
사용자 스토리, 수용 기준(Given-When-Then), 엣지 케이스를 포함해줘.
```

**터미널** 에서 확인한다.

```bash
ls .specify/specs/
# 예상 출력: 001-login-page/   002-login-attempts/

cat .specify/specs/002-login-attempts/spec.md
```

---

### Step 2. Clarify

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.clarify
.specify/specs/002-login-attempts/spec.md 를 읽고,
다음 내용을 명확히 반영하여 spec.md 를 보완해줘.

- 실패 횟수 표시는 로그인 버튼 위에 작게 표시한다
- 버튼 비활성화 상태에서는 버튼 색상이 회색으로 바뀐다
- 카운트다운 메시지는 빨간색으로 표시한다
- 로그인 성공 시에는 실패 횟수가 초기화된다
- 페이지를 새로고침하면 횟수가 초기화된다 (교육용: 세션 유지 불필요)

변경된 내용을 .specify/specs/002-login-attempts/spec.md 에 반영해줘.
```

---

### Step 3. Checklist — 명세 완성도 점검

> 횟수 제한 기능은 **UX** 와 **보안** 이 교차하는 영역이다.  
> 두 관점에서 모두 점검하여 구현 전에 빠진 요구사항을 찾는다.

#### 3-1. UX 관점 체크리스트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist ux
.specify/specs/002-login-attempts/spec.md 를 읽고,
UX 요구사항이 완전히 정의되어 있는지 점검해줘.

다음 항목을 중심으로 확인해줘:
- 카운트다운 숫자 표시 방식이 명확한가?
- 버튼 비활성화/재활성화 시 사용자에게 주는 피드백이 명세에 있는가?
- 버튼 비활성화 중 입력 필드 상태가 정의되어 있는가?
- 로그인 성공 시 실패 횟수 초기화가 명세에 있는가?

빠진 항목이 있으면 spec.md 에 무엇을 추가해야 하는지 알려줘.
결과를 .specify/specs/002-login-attempts/checklists/ux.md 로 저장해줘.
```

**터미널** 에서 확인한다.

```bash
cat .specify/specs/002-login-attempts/checklists/ux.md
```

#### 3-2. 보안 관점 체크리스트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist security
.specify/specs/002-login-attempts/spec.md 를 읽고,
보안 요구사항이 충분히 정의되어 있는지 점검해줘.

다음 항목을 중심으로 확인해줘:
- 실패 허용 횟수(3회)가 명세에 명시되어 있는가?
- 잠금 해제 조건(5초 경과 후 자동)이 명세에 있는가?
- 페이지 새로고침으로 잠금이 우회 가능한지 여부가 명세에 있는가? (교육용임을 명시)
- 실패 횟수를 클라이언트(JS 변수)에서만 관리하는 제약이 명시되어 있는가?

빠진 항목이 있으면 spec.md 에 무엇을 추가해야 하는지 알려줘.
결과를 .specify/specs/002-login-attempts/checklists/security.md 로 저장해줘.
```

**터미널** 에서 확인한다.

```bash
cat .specify/specs/002-login-attempts/checklists/security.md
```

#### 3-3. 미통과 항목 처리

미통과(`[ ]`) 항목이 있으면 채팅창에서 수정을 요청한다.

```
@workspace .specify/specs/002-login-attempts/spec.md 를 열어봐.
[미통과 항목 내용] 부분이 명세에 빠져 있어.
spec.md 에 해당 내용을 추가해줘.
```

> **확인 포인트 ✅**  
> `checklists/ux.md`, `checklists/security.md` 생성 확인  
> 미통과 항목 spec.md 보완 완료 → Step 4 로 진행.

---

### Step 4. Plan

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.plan
.specify/specs/002-login-attempts/spec.md 를 읽고,
기존 index.html 에 횟수 제한 기능을 추가하는 기술 계획을 세워줘.

[구현 방향]
- 기존 index.html 파일을 수정하여 기능을 추가한다
- JavaScript 의 setInterval 또는 setTimeout 을 사용하여 카운트다운을 구현한다
- 실패 횟수는 JavaScript 변수로 관리한다
- 별도 파일 없이 index.html 안에서 모두 처리한다

결과를 .specify/specs/002-login-attempts/plan.md 로 저장해줘.
```

---

### Step 5. Tasks

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.tasks
.specify/specs/002-login-attempts/plan.md 를 읽고,
개발자가 순서대로 실행할 수 있는 작업 목록을 만들어줘.
각 Task 는 체크박스([ ]) 형식으로 작성해줘.
결과를 .specify/specs/002-login-attempts/tasks.md 로 저장해줘.
```

---

### Step 6. Analyze

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.analyze
아래 세 파일을 모두 읽고, 내용이 서로 일치하는지 점검해줘.

- .specify/specs/002-login-attempts/spec.md
- .specify/specs/002-login-attempts/plan.md
- .specify/specs/002-login-attempts/tasks.md

불일치하는 항목이 있으면 어떻게 수정해야 하는지 알려줘.
```

---

### Step 7. Implement

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.implement
아래 파일들을 모두 읽고, 기존 index.html 에 로그인 횟수 제한 기능을 추가해줘.

참고 파일:
- .specify/memory/constitution.md
- .specify/specs/002-login-attempts/spec.md
- .specify/specs/002-login-attempts/plan.md
- .specify/specs/002-login-attempts/tasks.md
- index.html  (기존 파일 수정)

기존 코드 구조는 최대한 유지하고,
추가된 부분에는 한국어 주석으로 설명을 달아줘.
수정된 내용을 index.html 에 반영해줘.
```

---

### 7-2. 동작 테스트

브라우저에서 `Ctrl+Shift+R` (강제 새로고침) 을 눌러 변경 사항을 반영한다.

| 시나리오 | 예상 결과 |
|---|---|
| 틀린 비밀번호 1회 입력 | "로그인 시도: 1/3" 표시 |
| 틀린 비밀번호 3회 입력 | 버튼 비활성화(회색), 카운트다운 시작 |
| 카운트다운 진행 | "5초 후 다시 시도하세요 (5, 4, 3, 2, 1)" 빨간색 표시 |
| 5초 대기 | 버튼 재활성화(파란색), 횟수 초기화 |
| 올바른 정보로 로그인 | "로그인 성공!" → 2초 후 "환영합니다, admin 님!" |

#### constitution 테스트 원칙 기반 검증

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist testing
아래 파일들을 읽고, 횟수 제한 기능에 대한 테스트 체크리스트를 만들어줘.

참고 파일:
- .specify/memory/constitution.md  ← [테스트 원칙] 참조
- .specify/specs/002-login-attempts/spec.md
- index.html

[포함할 항목]
- 정상 흐름: 1회, 2회, 3회 실패 → 카운트다운 → 재활성화
- 오류 흐름: 버튼 비활성화 중 Enter 키, 새로고침 초기화
- constitution 원칙: 한국어 주석, 단일 파일, 모바일 375px 확인

결과를 .specify/specs/002-login-attempts/checklists/testing.md 로 저장해줘.
```

> **확인 포인트 ✅**  
> `checklists/testing.md` 생성 확인, 모든 항목 체크 완료 → Part 7 완료.

---

## Part 8. SDD 실습 3단계 — 아이디 저장 기능 추가 (약 30분)

---

### Step 1. Specify 003 — 아이디 저장 요구사항 정의

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.specify
기존 로그인 화면에 아이디 저장 기능을 추가해줘.

[기능 요구사항]
- 아이디 입력 필드 아래에 "아이디 저장" 체크박스가 있다
- 체크박스를 체크하고 로그인 성공하면 아이디가 브라우저에 저장된다
- 다음에 페이지를 열면 저장된 아이디가 자동으로 입력 필드에 채워진다
- 저장된 아이디가 있으면 체크박스도 자동으로 체크된 상태로 표시된다
- 체크박스를 해제하고 로그인하면 저장된 아이디가 삭제된다

결과를 .specify/specs/003-save-id/spec.md 로 저장해줘.
사용자 스토리, 수용 기준(Given-When-Then), 엣지 케이스를 포함해줘.
```

**터미널** 에서 확인한다.

```bash
ls .specify/specs/
# 예상 출력: 001-login-page/   002-login-attempts/   003-save-id/
```

---

### Step 2. Clarify

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.clarify
.specify/specs/003-save-id/spec.md 를 읽고,
다음 내용을 명확히 반영하여 spec.md 를 보완해줘.

- 아이디 저장에는 localStorage 를 사용한다
- 비밀번호는 저장하지 않는다 (보안 원칙)
- 체크박스는 아이디 입력 필드와 비밀번호 입력 필드 사이에 배치한다
- 저장된 아이디가 있을 때는 비밀번호 입력 필드에 자동으로 포커스가 이동한다
- localStorage 저장 키 이름은 'saved_username' 으로 한다
- 로그인 실패 시 저장된 아이디는 유지된다

변경된 내용을 .specify/specs/003-save-id/spec.md 에 반영해줘.
```

---

### Step 3. Checklist — 명세 완성도 점검

> 아이디 저장 기능은 **localStorage** 를 사용하므로 **보안** 과 **UX** 관점을 모두 점검한다.

#### 3-1. 보안 관점 체크리스트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist security
.specify/specs/003-save-id/spec.md 를 읽고,
보안 요구사항이 충분히 정의되어 있는지 점검해줘.

다음 항목을 중심으로 확인해줘:
- 저장 대상이 아이디만임이 명시되어 있는가?
- 비밀번호는 저장하지 않는다는 제약이 명세에 있는가?
- localStorage 에 평문 저장됨에 대한 교육적 주의사항이 있는가?
- 저장/삭제 조건이 명확히 정의되어 있는가?
- 로그인 실패 시 저장된 아이디 처리가 명세에 있는가?

빠진 항목이 있으면 spec.md 에 무엇을 추가해야 하는지 알려줘.
결과를 .specify/specs/003-save-id/checklists/security.md 로 저장해줘.
```

#### 3-2. UX 관점 체크리스트

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist ux
.specify/specs/003-save-id/spec.md 를 읽고,
UX 요구사항이 충분히 정의되어 있는지 점검해줘.

다음 항목을 중심으로 확인해줘:
- 체크박스 위치와 레이블 텍스트가 명세에 있는가?
- 페이지 로드 시 자동 입력 동작이 명세에 있는가?
- 저장된 아이디가 있을 때 비밀번호 필드 포커스 이동이 명세에 있는가?
- 처음 방문(저장값 없음) 시 초기 화면 상태가 명세에 있는가?
- 아이디를 수동으로 지우고 로그인하면 어떻게 되는가?

빠진 항목이 있으면 spec.md 에 무엇을 추가해야 하는지 알려줘.
결과를 .specify/specs/003-save-id/checklists/ux.md 로 저장해줘.
```

#### 3-3. 미통과 항목 처리

```
보안 관련(비밀번호 미저장, 평문 저장 주의사항) → 반드시 spec.md 에 추가
UX 세부사항(체크박스 스타일) → 팀 논의 후 추가 여부 결정
```

> **확인 포인트 ✅**  
> `checklists/security.md`, `checklists/ux.md` 생성 확인 → Step 4 로 진행.

---

### Step 4. Plan

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.plan
.specify/specs/003-save-id/spec.md 를 읽고,
기존 index.html 에 아이디 저장 기능을 추가하는 기술 계획을 세워줘.

[구현 방향]
- localStorage 를 사용하여 아이디를 브라우저에 저장한다
- 저장 키 이름은 'saved_username' 으로 한다
- 페이지 로드 시 localStorage 를 확인하여 저장된 아이디를 불러온다
- 별도 파일 없이 index.html 안에서 처리한다

결과를 .specify/specs/003-save-id/plan.md 로 저장해줘.
```

---

### Step 5. Tasks

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.tasks
.specify/specs/003-save-id/plan.md 를 읽고,
개발자가 순서대로 실행할 수 있는 작업 목록을 만들어줘.
각 Task 는 체크박스([ ]) 형식으로 작성해줘.
결과를 .specify/specs/003-save-id/tasks.md 로 저장해줘.
```

---

### Step 6. Analyze

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.analyze
아래 세 파일을 모두 읽고, 내용이 서로 일치하는지 점검해줘.

- .specify/specs/003-save-id/spec.md
- .specify/specs/003-save-id/plan.md
- .specify/specs/003-save-id/tasks.md

불일치하는 항목이 있으면 어떻게 수정해야 하는지 알려줘.
```

---

### Step 7. Implement

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.implement
아래 파일들을 모두 읽고, 기존 index.html 에 아이디 저장 기능을 추가해줘.

참고 파일:
- .specify/memory/constitution.md
- .specify/specs/003-save-id/spec.md
- .specify/specs/003-save-id/plan.md
- .specify/specs/003-save-id/tasks.md
- index.html  (기존 파일 수정)

기존 코드 구조는 최대한 유지하고,
추가된 부분에는 한국어 주석으로 설명을 달아줘.
수정된 내용을 index.html 에 반영해줘.
```

---

### 8-2. 동작 테스트

브라우저에서 `Ctrl+Shift+R` (강제 새로고침) 으로 변경 사항을 반영한다.

| 시나리오 | 예상 결과 |
|---|---|
| 체크박스 체크 후 로그인 성공 | 아이디가 localStorage 에 저장됨 |
| 페이지 새로고침 | 아이디 자동 입력, 체크박스 자동 체크 |
| 저장된 아이디 상태에서 비밀번호 필드 | 자동으로 포커스 이동 |
| 체크박스 해제 후 로그인 | localStorage 에서 아이디 삭제됨 |
| 페이지 새로고침 | 아이디 입력 필드가 비어 있음 |

**localStorage 저장 확인 방법** (브라우저 개발자 도구):

```
1. 브라우저에서 F12 (개발자 도구) 를 연다.
2. Application 탭 → Storage → Local Storage → 현재 주소를 클릭한다.
3. 'saved_username' 키에 'admin' 값이 저장되어 있는지 확인한다.
```

#### constitution 테스트 원칙 기반 검증

**채팅창** 에 아래를 입력한다.

```
@workspace /speckit.checklist testing
아래 파일들을 읽고, 아이디 저장 기능에 대한 테스트 체크리스트를 만들어줘.

참고 파일:
- .specify/memory/constitution.md  ← [테스트 원칙] 참조
- .specify/specs/003-save-id/spec.md
- index.html

[포함할 항목]
- 저장 흐름: 체크 후 로그인 → localStorage 저장 확인 (F12)
- 자동 입력 흐름: 새로고침 후 아이디 자동 입력, 포커스 이동
- 삭제 흐름: 체크 해제 후 로그인 → localStorage 삭제 확인
- 보안 검증: 비밀번호가 localStorage 에 저장되지 않는가?
- constitution 원칙: 한국어 주석, 모바일 375px 확인

결과를 .specify/specs/003-save-id/checklists/testing.md 로 저장해줘.
```

> **확인 포인트 ✅**  
> `checklists/testing.md` 생성 확인, 모든 항목 체크 완료 → Part 8 완료.

---

## Part 9. 전체 산출물 구조 확인

실습이 모두 끝나면 **터미널** 에서 전체 구조를 확인한다.

```bash
find . -not -path '*/\.*' | sort
```

### constitution.md 최종 확인

```bash
cat .specify/memory/constitution.md
```

반드시 아래 두 섹션이 모두 포함되어 있어야 한다.

```
✅ [개발 원칙] 섹션 — 단일 파일, 한국어 주석, 외부 라이브러리 금지, 모바일 지원 등
✅ [테스트 원칙] 섹션 — implement 후 체크리스트, 브라우저 직접 테스트, 수용 기준 1:1 검증 등
```

### 예상 디렉터리 구조

```
login-project/
├── .github/
│   └── prompts/                          ← Copilot 채팅창이 인식하는 명령 파일
│       ├── speckit.analyze.prompt.md
│       ├── speckit.checklist.prompt.md
│       ├── speckit.clarify.prompt.md
│       ├── speckit.constitution.prompt.md
│       ├── speckit.implement.prompt.md
│       ├── speckit.plan.prompt.md
│       ├── speckit.specify.prompt.md
│       └── speckit.tasks.prompt.md
├── .specify/
│   ├── memory/
│   │   └── constitution.md              ← [개발 원칙] + [테스트 원칙] (1회 작성)
│   └── specs/
│       ├── 001-login-page/
│       │   ├── spec.md                  ← 기본 로그인 요구사항 (Part 6 에서 수정됨)
│       │   ├── plan.md
│       │   ├── tasks.md
│       │   └── checklists/
│       │       ├── requirements.md      ← Step 4-1 (specify 후)
│       │       ├── ux.md                ← Step 4-2 (specify 후)
│       │       └── testing.md           ← Step 9 + Part 6 변경분 누적
│       ├── 002-login-attempts/
│       │   ├── spec.md
│       │   ├── plan.md
│       │   ├── tasks.md
│       │   └── checklists/
│       │       ├── ux.md
│       │       ├── security.md
│       │       └── testing.md           ← 7-2 constitution 기반 검증 결과
│       └── 003-save-id/
│           ├── spec.md
│           ├── plan.md
│           ├── tasks.md
│           └── checklists/
│               ├── security.md
│               ├── ux.md
│               └── testing.md           ← 8-2 constitution 기반 검증 결과
└── index.html                           ← 최종 결과물 (명세 수정 + 3가지 기능)
```

### 산출물 전체 확인

```bash
find .specify/specs -name "*.md" | sort
```

예상 출력:

```
.specify/specs/001-login-page/checklists/requirements.md
.specify/specs/001-login-page/checklists/testing.md
.specify/specs/001-login-page/checklists/ux.md
.specify/specs/001-login-page/plan.md
.specify/specs/001-login-page/spec.md
.specify/specs/001-login-page/tasks.md
.specify/specs/002-login-attempts/checklists/security.md
.specify/specs/002-login-attempts/checklists/testing.md
.specify/specs/002-login-attempts/checklists/ux.md
.specify/specs/002-login-attempts/plan.md
.specify/specs/002-login-attempts/spec.md
.specify/specs/002-login-attempts/tasks.md
.specify/specs/003-save-id/checklists/security.md
.specify/specs/003-save-id/checklists/testing.md
.specify/specs/003-save-id/checklists/ux.md
.specify/specs/003-save-id/plan.md
.specify/specs/003-save-id/spec.md
.specify/specs/003-save-id/tasks.md
```

### 최종 완성 기능 전체 테스트

```
기본 로그인 (001 — Part 6 에서 수정된 버전)
[ ] 빈 칸 제출 시 안내 메시지 표시
[ ] admin/1234 로 로그인 성공 → "로그인 성공!" 표시
[ ] 2초 후 "환영합니다, admin 님!" 으로 메시지 교체
[ ] 비밀번호 필드 자동 초기화 (아이디 필드는 유지)
[ ] 틀린 정보 입력 시 오류 메시지 표시
[ ] 비밀번호 보이기/숨기기 동작
[ ] Enter 키로 로그인 가능

로그인 횟수 제한 (002)
[ ] 실패 시 "로그인 시도: N/3" 표시
[ ] 3회 실패 시 버튼 비활성화 (회색)
[ ] 카운트다운 5→1 표시
[ ] 5초 후 버튼 재활성화 및 횟수 초기화

아이디 저장 (003)
[ ] 체크박스 체크 후 로그인 성공 시 아이디 저장
[ ] 새로고침 후 아이디 자동 입력 및 체크박스 자동 체크
[ ] 체크박스 해제 후 로그인 시 아이디 삭제
```

> **확인 포인트 ✅**  
> 아래 세 가지가 모두 확인되면 실습 완주다.
> 1. 각 기능 폴더 아래 `checklists/testing.md` 파일이 존재한다
> 2. 001 의 `spec.md` 에 Part 6 의 변경 이력이 기록되어 있다
> 3. `constitution.md` 에 `[테스트 원칙]` 섹션이 포함되어 있다

### 실습 전체 흐름 복습

```
[최초 구현 001]
constitution(개발원칙 + 테스트원칙) → specify(001) → clarify
    → checklist(requirements + ux) → plan → tasks → analyze → implement
    → checklist testing(constitution 기반: 수용기준 + 원칙 준수)

[명세 수정 — Part 6]
spec.md 수정 → clarify → checklist → analyze
    → plan(재실행) → tasks(재실행) → implement
    → checklist testing(변경분 추가 — testing.md 에 누적) → Git 커밋

[기능 추가 002]
specify(002) → clarify → checklist(ux + security)
    → plan → tasks → analyze → implement → checklist testing

[기능 추가 003]
specify(003) → clarify → checklist(security + ux)
    → plan → tasks → analyze → implement → checklist testing
```

---

## 실습 종료 후 Codespace 정지 방법

정지하지 않으면 무료 시간(월 60시간)이 계속 소모된다.  
**실습이 끝나면 반드시 Codespace 를 정지한다.**

**방법 1 (VS Code 화면에서)**

화면 왼쪽 하단의 `>< Codespaces` 를 클릭한다.  
`Stop Current Codespace` 를 선택한다.

**방법 2 (GitHub 웹에서)**

https://github.com/codespaces 에 접속한다.  
본인 Codespace 오른쪽 `...` 메뉴에서 `Stop codespace` 를 선택한다.

---

## 트러블슈팅

### specify 명령어를 찾을 수 없다 (`command not found`)

```bash
source ~/.bashrc
specify --version
```

그래도 안 되면 다시 설치한다.

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git
```

---

### `.github/prompts/` 폴더가 없다

`specify init` 이 copilot 연동 없이 실행된 것이다.  
아래 명령어로 연동을 추가한다.

```bash
specify integration install copilot
ls .github/prompts/
```

---

### 채팅창에서 `/speckit.*` 명령이 동작하지 않는다

Copilot 채팅창은 `/speckit.*` 을 슬래시 명령어로 자동 인식하지 않는다.  
**`/speckit.specify` 는 명령어가 아니라 프롬프트의 시작 문장**으로 사용하는 것이다.  
채팅창에 `@workspace /speckit.specify ...` 형태로 입력하면 Copilot 이  
`.github/prompts/speckit.specify.prompt.md` 를 참조하여 응답한다.

만약 응답이 엉뚱하게 나온다면:

```
@workspace .github/prompts/speckit.specify.prompt.md 파일을 읽고,
그 지침에 따라 아래 요구사항에 대한 spec.md 를 작성해줘.
[요구사항 내용...]
```

---

### Copilot 이 파일을 저장하지 않고 내용만 출력한다

Copilot(Auto) 이 파일 내용을 채팅창에 출력하기만 하는 경우,  
채팅창에서 명시적으로 저장을 요청한다.

```
위 내용을 .specify/specs/001-login-page/spec.md 파일로 저장해줘.
파일이 없으면 새로 만들고, 있으면 덮어써줘.
```

그래도 저장되지 않으면 터미널에서 직접 저장한다.

```bash
# 폴더 먼저 생성
mkdir -p .specify/specs/001-login-page/checklists

# 채팅창에서 출력된 내용을 복사하여 파일로 저장
nano .specify/specs/001-login-page/spec.md
# Ctrl+X → Y → Enter 로 저장
```

---

### constitution.md 에 `[테스트 원칙]` 이 없다

터미널에서 직접 추가한다.

```bash
nano .specify/memory/constitution.md
```

아래 내용을 파일 끝에 추가한다.

```markdown
[테스트 원칙]
- implement 완료 후 반드시 checklist testing 으로 품질을 검증한다
- 테스트는 브라우저에서 직접 시나리오를 실행하는 방식으로 진행한다
- 각 수용 기준(Given-When-Then)에 대응하는 테스트 시나리오를 1:1 로 검증한다
- 정상 흐름(Happy Path)과 오류 흐름(Error Path)을 모두 테스트한다
- 모바일 화면(375px 기준)에서도 동작을 확인한다
- 개발 원칙 준수 여부(단일 파일, 한국어 주석, 외부 라이브러리 미사용)도 테스트 항목에 포함한다
```

저장 후 확인한다.

```bash
cat .specify/memory/constitution.md
```

---

### Copilot 응답이 영어로 나온다

채팅창 입력 앞에 아래 문장을 추가한다.

```
모든 응답은 한국어로 해줘. 파일 내용도 한국어로 작성해줘.
@workspace /speckit.specify ...
```

또는 constitution.md 의 `[개발 원칙]` 에 "모든 응답과 파일 내용은 한국어로 작성한다" 가  
포함되어 있는지 확인한다.

```bash
cat .specify/memory/constitution.md | grep 한국어
```

---

### index.html 이 생성되지 않았다

채팅창에 아래와 같이 명시적으로 파일 저장을 요청한다.

```
@workspace implement 명령을 실행했지만 index.html 이 생성되지 않았어.
.specify/specs/001-login-page/ 폴더의 spec.md, plan.md, tasks.md 를 읽고,
index.html 을 현재 폴더(login-project/)에 생성해줘.
파일을 직접 저장해줘.
```

---

### 브라우저에서 화면이 열리지 않는다 (포트 포워딩 문제)

VS Code 하단 `포트(PORTS)` 탭을 클릭한다.  
8080 포트의 지구본 아이콘을 클릭한다.  
목록에 없으면 `포트 추가` 버튼을 클릭하고 `8080` 을 입력한다.  
포트 가시성(Visibility) 이 `Private` 이면 `Public` 으로 변경한다.

---

### Part 6 에서 spec.md 수정 후 plan.md 가 자동으로 바뀌지 않는다

채팅창에서 명시적으로 재실행을 요청해야 한다.

```
@workspace .specify/specs/001-login-page/spec.md 가 수정되었어.
변경된 내용을 반영하여 plan.md 를 다시 작성해줘.
결과를 .specify/specs/001-login-page/plan.md 에 저장해줘.
```

---

## 전체 명령어 요약표

| 단계 | 위치 | 입력 내용 |
|---|---|---|
| uv 설치 | 터미널 | `curl -LsSf https://astral.sh/uv/install.sh \| sh && source ~/.bashrc` |
| specify 설치 | 터미널 | `uv tool install specify-cli --from git+https://github.com/github/spec-kit.git` |
| 프로젝트 생성 | 터미널 | `specify init login-project --integration copilot` |
| 폴더 이동 | 터미널 | `cd login-project` |
| 명령 파일 확인 | 터미널 | `ls .github/prompts/` |
| 채팅창 열기 | VS Code | 우측 상단 채팅 아이콘 클릭 또는 `Ctrl+Alt+I` |
| 원칙+테스트원칙 설정 (1회만) | 채팅창 | `@workspace /speckit.constitution ...` |
| constitution 확인 | 터미널 | `cat .specify/memory/constitution.md` |
| 요구사항 정의 | 채팅창 | `@workspace /speckit.specify ...` |
| 모호함 제거 | 채팅창 | `@workspace /speckit.clarify ...` |
| 명세 점검 — 기본 | 채팅창 | `@workspace /speckit.checklist ...` |
| 명세 점검 — UX | 채팅창 | `@workspace /speckit.checklist ux ...` |
| 명세 점검 — 보안 | 채팅창 | `@workspace /speckit.checklist security ...` |
| 구현 후 품질 검증 | 채팅창 | `@workspace /speckit.checklist testing ...` |
| 기술 설계 | 채팅창 | `@workspace /speckit.plan ...` |
| 작업 목록 | 채팅창 | `@workspace /speckit.tasks ...` |
| 일관성 점검 | 채팅창 | `@workspace /speckit.analyze ...` |
| 코드 생성 | 채팅창 | `@workspace /speckit.implement ...` |
| 파일 확인 | 터미널 | `cat .specify/specs/001-login-page/spec.md` |
| 서버 실행 | 터미널 | `python3 -m http.server 8080` |
| 강제 새로고침 | 브라우저 | `Ctrl+Shift+R` |

**채팅창 프롬프트 패턴**:

```
@workspace /speckit.[명령어]
[참고 파일 목록]
[구체적인 요청 내용]
[저장 위치 명시]
```

**체크리스트 실행 시점 요약**:

| 시점 | 도메인 | 참조 기준 | 채팅창 입력 시작 |
|---|---|---|---|
| clarify 직후 / plan 이전 | (기본), ux | spec.md | `@workspace /speckit.checklist ...` |
| clarify 직후 / plan 이전 | security | spec.md + constitution.md | `@workspace /speckit.checklist security ...` |
| spec.md 수정 직후 | (기본) | 변경된 spec.md | `@workspace /speckit.checklist ...` |
| implement 직후 | **testing** | **spec.md + constitution.md [테스트 원칙]** | `@workspace /speckit.checklist testing ...` |

---

## 실습 진행 시간 배분

| 파트 | 내용 | 시간 |
|---|---|---|
| Part 1 | Codespace 생성 + 채팅창 확인 | 15분 |
| Part 2 | 도구 설치 (uv, specify) | 10분 |
| Part 3 | 프로젝트 초기화 (copilot 연동) + 채팅창 워크스페이스 확인 | 10분 |
| Part 4 | 기본 로그인 화면 구현 (SDD 9단계) | 65분 |
| Part 5 | 결과물 실행 및 테스트 | 15분 |
| Part 6 | 기존 명세 변경 대처 (SDD 변경 워크플로) | 30분 |
| Part 7 | 로그인 횟수 제한 추가 | 30분 |
| Part 8 | 아이디 저장 기능 추가 | 30분 |
| Part 9 | 전체 구조 확인 및 완료 테스트 | 10분 |
| **합계** | | **약 3시간 35분** |

> **강사 운영 팁**  
> Part 2~3 도구 설치·초기화는 네트워크 상태에 따라 시간이 길어질 수 있다.  
> 실습 전날 Codespace 를 미리 생성하고 도구 설치 + `specify init` 까지 완료해 두면  
> Part 4 부터 바로 진행 가능하다.
>
> 시간이 부족한 경우 아래 순서로 축소 가능하다.
>
> - **2시간 반 버전**: Part 6 (명세 변경) 은 강사 시연으로 대체, Part 7·8 중 하나를 과제로 전환
> - **2시간 버전**: Part 4까지 완료 후 Part 6 강사 시연, Part 7·8 과제

---

*본 실습 자료는 GitHub Spec Kit 공식 문서를 기반으로 작성되었습니다.*  
*참고: https://github.com/github/spec-kit*  
*명령 파일 경로 기준: Spec Kit 0.8.5 이상 / GitHub Copilot 통합 (`--integration copilot`)*
