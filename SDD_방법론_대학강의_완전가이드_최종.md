# SDD(Specification-Driven Development) 방법론
### AI-Native 설계 패러다임 완전 가이드 — 대학 강의용

> **강의 대상**: 소프트웨어공학, 컴퓨터공학 전공 학부생
>
> **강의 목표**: AI 시대의 소프트웨어 개발 패러다임을 이해하고, SDD 방법론을 실제 프로젝트에 적용할 수 있는 역량 습득
>
> *"코드가 진실의 원천이던 시대는 끝났다. 이제 명세(Spec)가 왕이고, AI가 그것을 코드로 변환한다."*

---

## 📌 목차

**Part 1 — 배경과 문제 인식**
1. [시대적 배경: AI가 코딩 방식을 바꾸다](#1-시대적-배경-ai가-코딩-방식을-바꾸다)
2. [AI 코딩의 현실](#2-ai-코딩의-현실)

**Part 2 — SDD 이론**
3. [SDD란 무엇인가?](#3-sdd란-무엇인가)
4. [SDD의 4가지 핵심 원칙](#4-sdd의-4가지-핵심-원칙)
5. [Less is More: 스펙 설계 전략](#5-less-is-more-스펙-설계-전략)

**Part 3 — SDD 실전**
6. [SDD 4단계 워크플로](#6-sdd-4단계-워크플로)
7. [실전 도구 비교](#7-실전-도구-비교)
8. [엔터프라이즈 레거시 전환에서의 SDD 적용](#8-엔터프라이즈-레거시-전환에서의-sdd-적용)

**Part 4 — AI-Native 심화**
9. [SDD와 AI-Native 설계 패러다임의 연결](#9-sdd와-ai-native-설계-패러다임의-연결)
10. [AI-Native 아키텍처 심화: 6계층 레퍼런스 모델](#10-ai-native-아키텍처-심화-6계층-레퍼런스-모델)
11. [AI 제어 계층과 가드레일 설계](#11-ai-제어-계층과-가드레일-설계)
12. [Agent-Oriented 소프트웨어 설계](#12-agent-oriented-소프트웨어-설계)

**Part 5 — 주의사항과 미래**
13. [Anti-Pattern: 피해야 할 것들](#13-anti-pattern-피해야-할-것들)
14. [미래 전망: AI-Native SDLC](#14-미래-전망-ai-native-sdlc)

**부록**
15. [정리 및 핵심 교훈](#15-정리-및-핵심-교훈)
16. [빠른 참조 카드](#16-빠른-참조-카드)
17. [강의 토론 질문 및 실습 과제](#17-강의-토론-질문-및-실습-과제)

---

## 🎯 강의를 시작하기 전에 — 학습 목표

이 강의를 마치면 다음을 할 수 있습니다:

| 영역 | 학습 목표 |
|---|---|
| **이해** | 바이브 코딩의 한계와 SDD 등장의 필연성을 설명할 수 있다 |
| **이해** | SDD의 3가지 레벨과 4가지 핵심 원칙을 구분할 수 있다 |
| **적용** | Good Spec / Bad Spec을 판별하고, 스펙을 직접 작성할 수 있다 |
| **적용** | SDD 4단계 워크플로에 따라 기능 명세서를 완성할 수 있다 |
| **분석** | AI-Native 아키텍처에서 뉴로-심볼릭 경계를 설계할 수 있다 |
| **평가** | 프로젝트 규모에 따라 적절한 SDD 레벨과 도구를 선택할 수 있다 |

---

# Part 1 — 배경과 문제 인식

---

## 1. 시대적 배경: AI가 코딩 방식을 바꾸다

SDD를 이해하려면 먼저 AI 시대에 개발 방식이 어떻게 변화해 왔는지를 살펴볼 필요가 있습니다.

---

### 바이브 코딩(Vibe Coding)의 등장

2025년 2월, OpenAI 공동 창업자이자 전 Tesla AI 총괄 책임자인 **안드레이 카파시(Andrej Karpathy)** 는 소셜미디어(X)에 짧은 글을 올렸습니다.

> *"There's a new kind of coding I call 'vibe coding', where you fully give in to the vibes, embrace exponentials, and forget that the code even exists."*
>
> "나는 이것을 '바이브 코딩'이라고 부른다. 분위기에 완전히 몸을 맡기고, 지수적 성장을 받아들이며, 코드가 존재한다는 사실조차 잊어버리는 것이다."

이 글은 전 세계 개발자 커뮤니티에 즉각적인 반향을 일으켰습니다. 바이브 코딩이란, 개발자가 AI에게 자연어로 원하는 것을 말하면 AI가 코드를 생성해 주는 방식입니다. 개발자는 코드를 한 줄씩 작성하는 대신, **무엇을 만들지(What)** 를 설명하고 AI가 **어떻게 만들지(How)** 를 결정하는 구조입니다.

```
[기존 방식]   개발자 → 직접 코드 작성 → 실행
[바이브 코딩] 개발자 → 자연어 지시 → AI가 코드 생성 → 실행
```

실제로 Y Combinator의 2025년 동계 배치(Winter 2025)에 참여한 스타트업 중 **21%가 전체 코드베이스의 91% 이상을 AI로 생성**했다고 알려져 있습니다.

> **💡 핵심 개념**
>
> 바이브 코딩은 AI가 코딩의 진입 장벽을 획기적으로 낮춰준 혁신이었습니다. 비개발자도 소프트웨어를 만들 수 있게 되었고, 개발자들은 반복적인 코드 작성에서 해방되기 시작했습니다.

---

### 바이브 코딩의 한계

그러나 바이브 코딩이 빠르게 확산되면서 한계도 분명하게 드러났습니다.

**문제 1: Context Rot (맥락 부식)**

```
[바이브코딩 시나리오]

1단계: "데미지 시스템 만들어줘"
       → 방어력 공식, 크리티컬 배율 설계  ✅

2단계: "이번엔 적 AI 만들어줘"
       → AI는 방금 전 데미지 시스템의 존재를 모름  ❌
       → 방어력 공식과 맞지 않는 별도 공격 로직 생성

3단계: "UI에 데미지 표시해줘"
       → 또 다른 데미지 계산 로직이 등장  ❌❌

결과: 각 작업은 멀쩡하지만, 전체를 합치면 엉망.
```

> **Context Rot (맥락 부식)**: 작업이 전환될 때마다 이전 맥락이 증발하여 코드 일관성이 붕괴되는 현상.

**문제 2: 품질과 일관성 부족** — AI가 생성한 코드가 당장 동작하더라도, 전체 시스템과의 정합성이나 장기적인 유지보수를 고려하지 않는 경우가 많았습니다.

**문제 3: 추적 불가능한 코드베이스** — 바이브 코딩으로 빠르게 생성된 코드는 왜 이런 구조로 만들어졌는지 근거가 없었습니다.

**문제 4: 프로덕션 수준에 미치지 못하는 품질** — 보안, 성능, 규정 준수 등 엔터프라이즈 환경의 요구 사항을 만족시키기 위해서는 단순히 '동작하는 코드' 이상이 필요했습니다.

---

### 에이전틱 엔지니어링(Agentic Engineering)으로의 진화

2026년 초, 카파시는 바이브 코딩의 후계 개념으로 **에이전틱 엔지니어링(Agentic Engineering)** 을 제시했습니다.

> *"'Agentic' because the new default is that you are not writing the code directly 99% of the time, you are orchestrating agents who do and acting as oversight."*

| 구분 | 바이브 코딩 (2025) | 에이전틱 엔지니어링 (2026~) |
|---|---|---|
| **주요 사용자** | 비개발자, 초보자, 빠른 프로토타이핑 | 전문 개발자, 엔터프라이즈 팀 |
| **AI의 역할** | 코드 생성기 | 자율 에이전트 |
| **개발자의 역할** | 지시자 | 조율자(Orchestrator) + 감독자(Oversight) |
| **품질 기준** | 일단 동작하면 성공 | 프로덕션 품질 유지 |
| **핵심 스킬** | 자연어 지시 | 구조 설계 + 에이전트 관리 |

---

### Software 3.0과 SDD의 연결고리

카파시는 이러한 흐름을 **Software 3.0** 시대로 설명합니다.

```
Software 1.0  →  개발자가 직접 명시적인 코드 작성
Software 2.0  →  데이터와 학습을 통해 모델이 동작 결정 (머신러닝)
Software 3.0  →  자연어 지시로 AI가 소프트웨어 생성 및 운영
```

> **🔑 핵심 메시지**
>
> 바이브 코딩이 AI 시대의 가능성을 열었다면, 에이전틱 엔지니어링은 그 가능성을 프로덕션 수준의 품질로 끌어올리는 방법론입니다. 그리고 **SDD는 에이전틱 엔지니어링을 실천하기 위한 구체적인 프레임워크**입니다.

---

## 2. AI 코딩의 현실

---

### 2.1 "그냥 시키면 되지 않나?" — 자유 형식 지시의 한계

이 직관은 절반은 맞고 절반은 틀립니다. **자유 형식(Free-form) 지시만으로는 일관된 결과를 보장할 수 없습니다.**

```
개발자: "로그인 기능 만들어 줘."

AI가 가정해야 하는 것들:
├── 어떤 인증 방식? (JWT? 세션? OAuth?)
├── 어떤 데이터베이스? (MySQL? PostgreSQL? MongoDB?)
├── 비밀번호 해싱 방식? (bcrypt? SHA-256?)
├── 실패 시 처리? (잠금? 재시도 제한?)
├── 어떤 프레임워크? (FastAPI? Django? Express?)
└── 어떤 보안 수준? (금융급? 일반 웹?)
```

AI는 이 모든 것을 **스스로 가정(Assumption)** 합니다.

#### 반복적인 수정 루프

```
[1회] "로그인 기능 만들어 줘"
      → AI가 세션 기반으로 구현  (JWT를 원했는데)

[2회] "JWT로 바꿔줘"
      → AI가 refresh token 없이 구현

[3회] "refresh token도 추가해줘"
      → AI가 이전 코드와 충돌하는 새 코드 생성

[4회] "이전 코드와 합쳐줘"
      → AI가 컨텍스트를 잃고 다시 처음부터 시작...
```

이 루프에서 빠져나오려면 평균 **5~15회의 추가 대화**가 필요합니다.

> **💡 핵심 교훈**: AI는 마법이 아닙니다. 입력이 모호하면 출력도 모호해집니다.

---

### 2.2 "더 많이 알려주면 되지 않나?" — 정보 과부하의 역설

**정보의 양이 많다고 해서 AI가 더 잘 이해하는 것은 아닙니다.**

#### "Lost in the Middle" 현상

AI는 긴 텍스트를 처리할 때 처음과 끝 부분을 잘 처리하지만, **중간 부분의 중요한 제약사항과 비즈니스 규칙은 상대적으로 누락**하는 경향이 있습니다.

```
긴 프롬프트:
┌──────────────────────────────────────────┐
│ [앞부분] 잘 처리됨  ✅                    │
│ [중간부분] 중요한 제약사항, 비즈니스 규칙 ⚠️ │
│ [끝부분] 잘 처리됨  ✅                    │
└──────────────────────────────────────────┘
```

| 상황 | 기대 | 실제 결과 |
|---|---|---|
| 짧은 지시 | 빠를 것 같다 | AI가 스스로 가정 → 일관되지 않은 결과 |
| 긴 비구조화 정보 | 더 잘 알겠지 | AI가 중요도 판단 못해 핵심 요구사항 누락 |
| 구조화된 명세 | 번거롭다 | AI가 정확하게 이해 → 예측 가능한 결과 생성 |

> **💡 핵심 교훈**: AI에게 필요한 것은 **정보의 양**이 아니라 **정보의 구조**입니다.

---

### 2.3 진짜 문제: 커뮤니케이션 구조의 부재

```
[인간 간 커뮤니케이션]
시니어 → 주니어: 조직 문화, 코딩 컨벤션, 도메인 지식을 공유
                  암묵적 지식 + 명시적 지시의 혼합

[AI와의 커뮤니케이션]
개발자 → AI: 공유하는 맥락이 없음. 모든 것을 명시적으로 구조화해야 함
```

```
커뮤니케이션 구조 부재
        │
        ├── AI가 임의로 기술 스택 선택  → 조직 표준과 불일치
        ├── AI가 임의로 보안 수준 결정  → 규정 준수 실패
        ├── AI가 임의로 아키텍처 패턴 선택 → 기존 시스템과 불일치
        └── 세션마다 다른 결과          → 일관성 없는 코드베이스
```

SDD가 제공하는 커뮤니케이션 구조:

```
constitution.md → "우리 팀은 이렇게 개발합니다" (공통 규칙)
spec.md         → "이 기능은 이렇게 동작해야 합니다" (명세)
plan.md         → "이렇게 구현할 것입니다" (설계)
tasks.md        → "이 순서로 작업합니다" (실행 계획)
```

> **🔑 핵심 메시지**: "좋은 명세는 좋은 코드보다 먼저 존재한다."

---

# Part 2 — SDD 이론

---

## 3. SDD란 무엇인가?

---

### 3.1 SDD의 정의와 등장 배경

#### Birgitta Böckeler의 분석

사양 기반 개발(Specification-Driven Development, SDD)을 가장 체계적으로 정리한 인물 중 한 명은 **Birgitta Böckeler** 입니다. 그녀는 전 세계 45개국 1만 명 이상의 기술 전문가 조직인 **Thoughtworks의 Distinguished Engineer(수석 기술자)** 입니다.

Böckeler는 소프트웨어 아키텍처의 세계적 권위자인 **Martin Fowler**가 운영하는 **martinfowler.com**에 SDD에 관한 분석 글을 게재했습니다.

> Böckeler의 분석은 SDD를 **"AI 에이전트가 단순한 코드 생성기를 넘어 실제 개발 파트너로 기능하게 만드는 방법론"** 으로 정의합니다.

#### SDD의 핵심 정의

> **"코드보다 사양(Specification)을 먼저 정의하라. 사양이 명확하면, AI는 올바른 코드를 만들 수 있다."**

```
[기존 패러다임]
요구사항(말로) → 설계(머릿속) → 코드(실체)  ← 진실의 근원
                                    ↑
                     문서는 코드를 따라가며 구식이 됨

[SDD 패러다임]
사양(문서) ← 진실의 근원 → AI가 코드로 변환
    ↑                              ↓
코드 변경 시 사양 먼저 수정      코드는 사양의 표현
```

개발 방법론의 역사적 진화:

```
전통적 개발:   Think → Code → Document
               (코드가 왕, 문서는 부산물)

TDD:           Test → Code
               (테스트가 먼저, 코드는 테스트를 통과하도록)

SDD:           Specify → Generate → Validate
               (명세가 왕, 코드는 명세의 표현)
```

---

### 3.2 기존 개발 방식과 SDD의 차이

| 구분 | 기존 방식 | SDD |
|---|---|---|
| **진실의 근원** | 코드 | 사양 |
| **사양의 역할** | 코드를 만들기 위한 수단 | 핵심 아티팩트 |
| **코드의 역할** | 최종 결과물 | 사양의 구현체 |
| **유지보수 대상** | 코드 | 사양 + 코드 |
| **AI 활용 방식** | 코드 생성 보조 도구 | 사양을 코드로 번역하는 실행자 |
| **변경 시작점** | 코드 수정 | 사양 수정 → 코드 재생성 |
| **문서 상태** | 코드와 점점 괴리됨 | 코드와 항상 동기화 |

---

### 3.3 SDD의 3가지 구현 레벨

Böckeler는 SDD를 성숙도에 따른 **3가지 레벨**로 구분했습니다.

```
코드 중심 ◄──────────────────────────────────────► 명세 중심
          │                   │                   │
     Spec-First          Spec-Anchored       Spec-as-Source
    (명세 우선)          (명세 고정)          (명세가 원천)
      낮은 규율           중간 규율            높은 규율
```

---

#### Level 1: Spec-First (명세 우선)

```
수준: 입문 단계
핵심 원칙: "코딩 전에 먼저 문서로 정의한다"
```

코드를 작성하기 전에 무엇을 만들 것인지를 먼저 문서로 정의합니다. 사양은 **참조 문서(Reference Document)** 의 역할입니다.

- 기능 개발 전에 spec.md를 먼저 작성
- 사양은 AI 대화에서 맥락 제공 용도로 활용
- 코드 완성 후 사양은 보관 또는 일부 업데이트

**적합한 상황**: 프로토타입, 단발성 기능, AI 코딩 어시스턴트 초기 도입 팀

> ⚠️ **한계**: 시간이 지나면서 코드와 명세의 드리프트(drift) 발생 가능

> **Level 1은 지금 당장 실천할 수 있는 가장 기본적인 SDD 형태입니다.**

---

#### Level 2: Spec-Anchored (명세 고정)

```
수준: 중급 단계
핵심 원칙: "사양이 개발의 기준점이다. 코드는 사양을 증명해야 한다"
```

사양이 개발의 기준점(Anchor)이 됩니다. 코드가 변경될 때마다 사양과의 일치 여부를 검증합니다.

```markdown
코드 리뷰 체크리스트:
  □ 모든 수용 기준(AC)에 대응하는 코드가 있는가?
  □ 엣지 케이스가 처리되었는가?
  □ 사양에 없는 기능이 무단으로 추가되지 않았는가?
```

**적합한 상황**: 대부분의 프로덕션 시스템, 장기 유지보수가 필요한 엔터프라이즈 프로젝트

**대표 도구**: Cucumber, Gherkin, GitHub Spec Kit

---

#### Level 3: Spec-as-Source (명세가 소스)

```
수준: 고급 단계
핵심 원칙: "사양이 코드의 소스다. 코드는 사양에서 생성된다"
```

사양 자체가 코드를 생성하는 소스입니다. AI는 사양을 입력받아 코드, 테스트, 문서를 모두 생성합니다.

```
사양 작성/수정 → AI가 해석 → 구현 코드, 단위 테스트, API 문서 자동 생성
                              → 개발자가 결과 검증 → 사양 정제
```

**적합한 상황**: 완전한 AI-Native 개발팀, 미션 크리티컬 시스템(항공, 의료, 금융), 멀티 에이전트 환경

**대표 도구**: AWS Kiro, GitHub Spec Kit, Google Antigravity

---

#### 3가지 레벨 비교

| 구분 | Level 1: Spec-First | Level 2: Spec-Anchored | Level 3: Spec-as-Source |
|---|---|---|---|
| **핵심 변화** | 코딩 전 문서화 | 코드가 사양을 증명 | 사양이 코드를 생성 |
| **사양의 역할** | 참조 문서 | 기준점 | 소스 |
| **도입 난이도** | 낮음 | 중간 | 높음 |
| **적용 대상** | 모든 팀 | 체계적인 팀 | 성숙한 팀 |

> **💡 실용적 조언**: Level 1부터 시작해서 팀의 역량과 함께 점진적으로 이동하는 것이 현실적입니다.

---

### 3.4 스펙과 Memory Bank: 두 가지 컨텍스트의 역할

AI와 효과적으로 협업하려면 두 가지 컨텍스트를 모두 제공해야 합니다.

```
AI가 필요로 하는 정보
  ├── [1] 스펙(Spec): "무엇을 만들어야 하는가?"
  │         → 기능별, 작업별로 달라지는 정보
  └── [2] Memory Bank: "어떤 맥락에서 만들어야 하는가?"
            → 프로젝트 전체에 걸쳐 일관되게 유지되는 정보
```

**스펙(Specification)**은 특정 기능에 대한 구체적인 요구사항으로, 새 기능 개발 시마다 작성됩니다.

**Memory Bank**는 프로젝트 전체에 적용되는 맥락 정보입니다. GitHub Spec Kit의 `constitution.md`, Cursor의 `.cursorrules`, Gemini의 `GEMINI.md`가 모두 Memory Bank의 구현 형태입니다.

```
[두 컨텍스트가 없을 때] → 매번 다른 스타일, 일관성 없는 코드
[스펙만 있을 때]        → 기능 구현되지만 기존 코드베이스와 스타일 불일치
[Memory Bank만 있을 때] → 프로젝트에 맞는 스타일이지만 엉뚱한 기능 구현
[스펙 + Memory Bank]    → 의도한 기능을 프로젝트 표준에 맞게 일관성 있게 구현 ✅
```

> **🔑 핵심**: 스펙은 기능을 정의하고, Memory Bank는 맥락을 제공합니다. 두 가지가 결합될 때 AI는 진정한 개발 파트너가 됩니다.

---

## 4. SDD의 4가지 핵심 원칙

### 원칙 1. 기본 아티팩트로서의 사양

사양은 진실의 중심 소스입니다. 코드는 사양을 표현한 결과물이며, 소프트웨어를 유지보수한다는 것은 **사양 자체를 발전시키는 것**을 의미합니다.

### 원칙 2. 실행 가능한 사양(Executable Specification)

사양은 작동하는 시스템을 생성할 수 있을 만큼 **정확하고, 완전하며, 명확**해야 합니다. "로그인 기능 만들어 줘"라는 지시는 SDD에서 사용자 스토리, 수용 기준, 요구 사항, 엣지 케이스로 구체화됩니다.

### 원칙 3. 살아있는 문서(Living Documentation)

- 버그를 수정한다는 것은 잘못된 코드를 만들어 낸 **사양을 수정**하는 것입니다.
- 리팩터링한다는 것은 명확성을 위해 **사양을 재구성**하는 것입니다.
- 사양은 구현과 항상 동기화된 상태를 유지합니다.

### 원칙 4. AI-인간 협업 구조

```
[기존 바이브 코딩]
  개발자 ──(모호한 지시)──▶ AI ──▶ 예측 불가능한 코드

[SDD + 에이전틱 엔지니어링]
  개발자 ──(명확한 사양)──▶ AI ──▶ 검증된 코드
     ▲          ▲                        │
  Memory    Spec 문서              피드백/검증
  Bank      (기능 명세)                  │
     └──────────┴────────────────────────┘
```

---

## 5. Less is More: 스펙 설계 전략

---

### 5.1 Less is More 원칙이란?

> **"스펙의 가치는 길이가 아니라 밀도에 있다. 필요한 것만, 정확하게, 구조적으로."**

```
스펙 길이 vs AI 이해도:

짧고 핵심적인 스펙 → AI의 주의가 집중 → 높은 정확도
긴 비구조화 스펙   → AI의 주의가 분산 → 중요한 부분 누락

최적의 스펙: 충분히 구체적이되, 불필요한 내용 없이
```

Less is More 원칙의 3가지 실천 방향:

- **JES(Just Enough Specification)**: 구현하기에 충분한 정보만. 구현 방법의 세부사항은 AI에게 위임
- **단일 책임 원칙**: 하나의 스펙 파일이 하나의 기능에만 집중
- **계층적 구조**: 범위에 따라 스펙을 계층으로 분리

---

### 5.2 계층적 스펙 구조

효과적인 스펙 시스템은 범위(Scope)에 따라 **4개 레이어**로 구성됩니다.

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 0: 프로젝트 헌법 (constitution.md)                    │
│  "이 프로젝트는 이렇게 만든다"                               │
│  → 기술 스택, 보안 원칙, 코딩 컨벤션, 팀 규칙               │
│  → 변경 빈도: 매우 낮음 (분기 1회 이하)                     │
├─────────────────────────────────────────────────────────────┤
│  Layer 1: 도메인/시스템 명세 (domain.md)                    │
│  "이 시스템은 어떤 도메인으로 구성되는가"                   │
│  → 주요 도메인, 서비스 간 관계, API 계약 개요               │
│  → 변경 빈도: 낮음 (새 도메인 추가 시)                      │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: 기능 명세 (features/NNN-기능명/spec.md)           │
│  "이 기능은 이렇게 동작한다"                                │
│  → 사용자 스토리, 수용 기준, 엣지 케이스                   │
│  → 변경 빈도: 중간 (기능 변경 시)                           │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: 구현 작업 명세 (features/NNN-기능명/tasks.md)     │
│  "이 기능을 어떤 순서로 구현한다"                           │
│  → 구체적 작업, 의존 관계, 완료 기준                        │
│  → 변경 빈도: 높음 (구현 진행에 따라)                       │
└─────────────────────────────────────────────────────────────┘
```

#### Layer 0: 프로젝트 헌법 (constitution.md)

프로젝트 전체에 적용되는 **변하지 않는 원칙들**입니다.

| 포함해야 할 것 (✅) | 포함하지 말아야 할 것 (❌) |
|---|---|
| 승인된 기술 스택과 버전 | 특정 기능의 요구사항 |
| 보안 원칙 (암호화 알고리즘 등) | 구현 세부사항 |
| 코딩 컨벤션 | 변경될 가능성이 있는 비즈니스 규칙 |
| 테스트 커버리지 기준 | 임시 결정사항 |
| 배포 환경 정보 | 특정 팀원에게만 해당되는 규칙 |

> **규모 기준**: 한 화면에 보일 정도(약 50~80줄)가 적절합니다. 그 이상이 되면 AI가 중요한 원칙을 놓칩니다.

```yaml
# constitution.md 실제 예시
project_principles:
  architecture:
    - "모든 기능은 독립 라이브러리로 먼저 구현"
    - "마이크로서비스 간 직접 DB 접근 금지"

  quality:
    - "구현 코드 전 반드시 테스트 선작성 (TDD)"
    - "코드 커버리지 80% 미만 PR 자동 거부"

  ai_governance:
    - "AI 생성 코드는 반드시 결정적 엔진 검증 통과 필요"
    - "핵심 비즈니스 로직은 AI 직접 실행 금지"

  change_management:
    - "API 변경 시 반드시 Spec 먼저 업데이트"
    - "프로덕션 인시던트 → 명세 개선으로 반드시 반영"
```

#### Layer 1: 도메인/시스템 명세

시스템의 **전체 지도(Map)**입니다.

```markdown
# 시스템 도메인 명세 예시

## 주요 도메인
- 인증(Auth): 로그인, 회원가입, 세션 관리
- 주문(Order): 주문 생성, 조회, 취소
- 결제(Payment): 결제 처리, 환불 (외부 연동)

## 서비스 간 통신 원칙
- Auth → 다른 서비스: JWT 토큰 검증
- Order → Payment: REST API (동기)
- Payment → Order: 이벤트 메시지 (비동기)

## 외부 의존성
- 결제 게이트웨이: TossPayments API v2
- SMS: NCP SMS API
- 이메일: AWS SES
```

#### Layer 2: 기능 명세 (spec.md)

특정 기능이 **무엇을, 왜** 해야 하는지를 정의합니다. AI가 코드를 생성할 때 가장 직접적으로 참조하는 레이어입니다.

```markdown
# 기능 명세: [기능 이름]
Feature-ID: NNN

## 사용자 스토리
[역할]로서, [목표]를 위해 [기능]을 원한다.

## 수용 기준 (Acceptance Criteria)
- Given: [전제 조건]
  When:  [사용자 행동]
  Then:  [예상 결과]

## 기능 요구사항
- MUST:   [반드시 구현해야 함]
- SHOULD: [가능하면 구현]
- MAY:    [선택적 구현]

## 비기능 요구사항
- 성능: [측정 가능한 기준]
- 보안: [보안 제약사항]

## 엣지 케이스
- [예외 상황 1]: [처리 방법]
- [예외 상황 2]: [처리 방법]

## ❌ How는 여기서 작성하지 않는다 (구현 방법은 plan.md에)
```

#### Layer 3: 구현 작업 명세 (tasks.md)

```markdown
## 작업 목록: 로그인 기능

[P] TASK-001: User 데이터 모델 및 DB 스키마 생성  (병렬 가능)
    → spec.md AC-login-1, AC-login-2 참조
    → 완료 기준: 마이그레이션 스크립트 실행 가능

[P] TASK-002: 로그인 API 엔드포인트 구현  (병렬 가능)
    → 완료 기준: 단위 테스트 통과

[ ] TASK-003: 프론트엔드 로그인 폼 구현  (TASK-001, 002 완료 후)
    → 완료 기준: E2E 테스트 시나리오 통과

[P] = 병렬 실행 가능   [ ] = 선행 작업 완료 후 실행
```

---

### 5.3 스펙 작성 실전 가이드: 좋은 스펙과 나쁜 스펙

---

#### 나쁜 패턴 1: 모호한 형용사 사용

```markdown
❌ 나쁜 스펙:
  "검색 결과는 빠르게 반환되어야 한다."
  "사용자 인터페이스가 직관적이어야 한다."

✅ 좋은 스펙:
  "검색 결과는 500ms 이내에 반환되어야 한다. (p95 기준)"
  "모바일 화면(375px 이상)에서 버튼의 터치 영역이 44px 이상이어야 한다."
```

#### 나쁜 패턴 2: 구현 방법을 지시하는 스펙

```markdown
❌ 나쁜 스펙:
  "React의 useState 훅을 사용하여 로그인 상태를 관리한다."
  → 이것은 "어떻게(How)"에 해당. 스펙은 "무엇을(What)"에 집중.

✅ 좋은 스펙:
  "사용자의 로그인 상태는 페이지 이동 시에도 유지되어야 한다."
```

#### 나쁜 패턴 3: 검증 불가능한 요구사항

```markdown
❌ 나쁜 스펙:
  "좋은 사용자 경험을 제공해야 한다."
  "확장 가능한 아키텍처로 구현해야 한다."

✅ 좋은 스펙 (Given-When-Then):
  Given: 사용자가 잘못된 비밀번호를 입력했을 때
  When:  로그인 버튼을 클릭하면
  Then:  "이메일 또는 비밀번호가 올바르지 않습니다" 메시지가
         input 필드 아래에 빨간색으로 표시되고, 비밀번호 필드가 초기화된다.
```

#### 나쁜 패턴 4: 맥락 없는 요구사항

```markdown
❌ 나쁜 스펙:
  "에러 처리를 해야 한다."  "로깅을 추가한다."

✅ 좋은 스펙:
  "에러 처리:
    - 네트워크 오류: '서버에 연결할 수 없습니다. 잠시 후 다시 시도해 주세요.' 표시
    - 세션 만료: 로그인 페이지로 리다이렉트
  로깅:
    - 모든 로그인 시도(성공/실패)를 기록 (사용자 ID, IP, 타임스탬프)
    - 5회 연속 실패 시 보안 알림 이벤트 발행"
```

#### 나쁜 패턴 5: 너무 긴 스펙 (모든 것을 한 파일에)

```markdown
❌ 나쁜 구조:
  # 전체 시스템 요구사항 명세서 (500줄)
  → AI가 현재 작업과 무관한 내용에서 노이즈를 구분하지 못함

✅ 좋은 구조:
  constitution.md           (50줄: 프로젝트 전체 원칙)
  features/001-login/spec.md   (50줄: 로그인 기능만)
  features/002-signup/spec.md  (40줄: 회원가입 기능만)
```

#### 스펙 품질 자가 체크리스트

```
[ ] 이 스펙만 보고 AI가 구현할 수 있는가?
[ ] 모든 수용 기준이 테스트 가능한가?
[ ] 모호한 형용사("빠르게", "쉽게")가 없는가?
[ ] 구현 방법(How)이 아닌 요구사항(What)에 집중했는가?
[ ] 엣지 케이스(예외 상황)를 정의했는가?
[ ] constitution.md의 원칙과 충돌하지 않는가?
[ ] 다른 기능의 요구사항이 섞여 있지 않은가?
[ ] 한 화면에 볼 수 있는 길이인가? (50~100줄 권장)
```

> **🔑 핵심 메시지**: 스펙 작성에 30분을 투자하면 구현과 수정에서 3~5시간을 절약합니다.

---

# Part 3 — SDD 실전

---

## 6. SDD 4단계 워크플로

```
[1단계: 지정] ──▶ [2단계: 계획] ──▶ [3단계: 작업] ──▶ [4단계: 구현]
  Specify              Plan              Task             Implement
  "무엇을?"           "어떻게?"        "어떤 순서로?"      "코드 생성"
```

---

### 1단계: 지정(Specify) — What & Why

**"소프트웨어가 무엇을 해야 하는지, 그리고 왜 해야 하는지를 정의합니다."**

#### 사용자 스토리(User Story)

```
형식: "나는 [사용자 유형]으로서, [목표]를 달성하기 위해 [기능]을 원한다."
예시: "나는 쇼핑몰 회원으로서, 주문 내역을 한눈에 확인하기 위해
       마이페이지에서 최근 주문 목록을 조회하고 싶다."
```

#### 수용 기준(Acceptance Criteria)

```
Given: 로그인한 사용자가 마이페이지에 접속했을 때
When:  주문 내역 탭을 클릭하면
Then:  최근 3개월 이내 주문 목록이 날짜 내림차순으로 표시된다.
```

#### 요구 사항(Requirements)

**기능적 요구 사항:**
- `MUST`: 회원가입 시 이메일 중복 여부를 확인해야 한다.
- `MUST`: 비밀번호는 최소 8자 이상, 영문·숫자·특수문자를 각각 1개 이상 포함해야 한다.
- `SHOULD`: 주문 완료 후 사용자에게 확인 이메일을 발송해야 한다.

**비기능적 요구 사항:**
- 성능: 주문 목록 조회 응답 시간은 2초 이내여야 한다.
- 보안: 개인정보는 AES-256 암호화로 저장해야 한다.

#### 엣지 케이스(Edge Cases)

- **경계값**: 주문 수량이 0개일 때, 재고가 정확히 1개 남았을 때
- **빈 값 및 null**: 검색어 없이 검색 버튼을 눌렀을 때
- **권한 예외**: 로그인하지 않은 사용자가 마이페이지에 접근할 때
- **동시성**: 두 사용자가 동시에 마지막 재고 1개를 구매하려 할 때

> **중요**: Specify 단계에서 `[NEEDS CLARIFICATION]` 태그를 달아 모호성을 명시적으로 표시합니다. 이 태그가 해소되지 않으면 Plan 단계로 넘어갈 수 없습니다.

---

### 2단계: 계획(Plan) — How

**"어떻게 구축할 것인지를 결정합니다."**

```markdown
# Implementation Plan: 003-realtime-chat

## 기술 선택 및 근거
| 기술        | 용도                     | 근거                                     |
|-------------|--------------------------|------------------------------------------|
| WebSocket   | 실시간 양방향 통신       | HTTP Long-polling 대비 70% 오버헤드 감소 |
| PostgreSQL  | 메시지 이력 영구 저장    | ACID 보장, 기존 인프라와 통합            |
| Redis       | 사용자 Presence 상태 관리 | TTL 지원으로 자동 오프라인 처리         |

## 데이터 모델
Message:  { id, userId, roomId, content, type, createdAt, deletedAt }
Room:     { id, name, type(DM/GROUP), maxMembers, createdAt }

## API 계약
WebSocket Events:
  · message:send    → message:receive
  · presence:update → presence:broadcast

REST:
  · GET  /api/rooms/{roomId}/messages?cursor=...&limit=50
  · POST /api/rooms/{roomId}/messages

## [GATE 검증 결과]
✅ 헌법 원칙 준수 확인
✅ 테스트 시나리오 포함 확인
✅ 보안 요구사항 반영 확인
→ TASK 단계 진행 승인
```

---

### 3단계: 작업(Task) — When & Who

**"계획을 구체적이고 실행 가능한 개발 작업 단위로 분해합니다."**

좋은 작업의 크기는 **하루 이내에 완료할 수 있는 수준**이 이상적입니다.

```
[계획] 회원가입 기능 구현
  ├── [P] 작업 1: User 데이터 모델 및 DB 스키마 생성  (병렬 가능)
  ├── [P] 작업 2: 이메일 중복 확인 API 구현           (병렬 가능)
  ├── [P] 작업 3: 비밀번호 유효성 검증 로직 구현      (병렬 가능)
  ├── [ ] 작업 4: 회원가입 API 엔드포인트 구현        (1,2,3 완료 후)
  ├── [ ] 작업 5: 가입 완료 확인 이메일 발송 구현     (4 완료 후)
  └── [ ] 작업 6: 단위 테스트 작성                   (5 완료 후)
```

---

### 4단계: 구현(Implement) — Code

**"사양, 계획, 작업 목록을 기반으로 코드를 작성합니다."**

```
[작업 선택] ──▶ [사양 확인] ──▶ [코드 생성] ──▶ [수용 기준 검증] ──▶ [다음 작업]
                                                         │
                                        (실패 시 사양 재검토 또는 코드 수정)
```

이 시점에서 AI 에이전트가 보유한 맥락:

```
✅ 프로젝트 헌법 (불변 원칙 — constitution.md)
✅ 기능 명세 (무엇을, 왜 — spec.md)
✅ 명확화된 제약 조건 (모호성 제거 완료)
✅ 기술 계획 (어떻게 — plan.md)
✅ 세분화된 작업 목록 (병렬화 정보 — tasks.md)

바이브코딩과의 차이:
  바이브코딩: "채팅 기능 만들어줘" → AI가 모든 것을 추측
  SDD:        위의 모든 맥락 제공  → AI가 명세에 맞는 일관된 코드 생성
```

추적성(Traceability) 확보 예시:

```python
# Implements: User Story #12 - 주문 목록 조회
# Acceptance Criteria: AC-12-1, AC-12-2
# Task: TASK-045
def get_order_list(user_id: str) -> list:
    ...
```

---

### Context Engineering과 SDD

> **프롬프트 엔지니어링**: "어떻게 말하느냐" (How to say)
> **Context Engineering**: "무엇을 알려주느냐" (What to tell)

```
낮은 맥락: "적 AI 만들어줘"
  → Update()에서 기본 추적/이동 코드 생성 (품질 낮음)

높은 맥락: "우리 프로젝트는 NavMesh 기반 길찾기를 씀.
            적 상태 관리는 FSM으로 하고 있음.
            감지 범위는 Physics.OverlapSphere로 처리함."
  → 프로젝트 아키텍처와 완전히 일치하는 정교한 코드 생성
```

**SDD = 구조화된 Context Engineering:**

```
Constitution (프로젝트 불변 원칙)      ← 가장 상위 맥락
      +
Spec (기능 요구사항 + 비즈니스 목표)   ← 기능 수준 맥락
      +
Plan (기술 설계 + API 계약)            ← 구현 수준 맥락
      +
Tasks (실행 단위 + 병렬화 정보)        ← 실행 수준 맥락
      ↓
Context Rot 원천 방지  ✅
```

---

## 7. 실전 도구 비교

| 도구 | 만든 곳 | 접근법 | SDD 레벨 |
|---|---|---|---|
| **AWS Kiro** | Amazon | 통합 AI IDE | Level 2~3 |
| **GitHub Spec Kit** | GitHub | 명령어 기반 확장 | Level 1~3 |
| **Tessl** | Tessl | AI 네이티브 플랫폼 | Level 2~3 |

---

### 7.1 AWS의 Kiro

**Kiro(카이로)** 는 2025년 AWS가 발표한 **명세 주도 AI IDE**입니다. "스펙에서 코드까지(Spec to Code)"를 핵심 슬로건으로 내세우며, SDD를 IDE 수준에서 완전히 통합합니다.

자연어로 기능 아이디어를 입력하면 `requirements.md`, `design.md`, `tasks.md`를 자동으로 생성합니다. **Steering Documents**는 GitHub Spec Kit의 `constitution.md`에 해당하며, 프로젝트 전체 AI 작업에 자동 적용됩니다.

| 항목 | 내용 |
|---|---|
| **SDD 지원 레벨** | Level 2~3 |
| **강점** | 스펙 → 코드 변환이 워크플로에 완전 통합 |
| **제한** | 기존 프로젝트에 도입 시 학습 비용 |
| **적합 대상** | 새 프로젝트, AWS 생태계를 활용하는 팀 |

---

### 7.2 GitHub Spec Kit

VS Code와 GitHub Copilot을 연동하는 **명령어 기반 SDD 확장 도구**입니다.

```
/speckit.specify   → spec.md 생성 (1단계)
/speckit.clarify   → 명세 보완
/speckit.checklist → 품질 점검
/speckit.plan      → plan.md 생성 (2단계)
/speckit.tasks     → tasks.md 생성 (3단계)
/speckit.analyze   → 일관성 검사
/speckit.implement → 코드 생성 (4단계)
```

**개발 시간 비교:**

| 단계 | 전통 방식 | SDD (GitHub Spec Kit) | 절감 |
|---|---|---|---|
| PRD 작성 | 2~3시간 | `/specify` 5분 | 97% ↓ |
| 설계 문서 | 2~3시간 | `/plan` 5분 | 97% ↓ |
| 기술 명세 | 3~4시간 | Plan에 포함 | 100% ↓ |
| 테스트 계획 | 2시간 | 명세에서 자동 생성 | 100% ↓ |
| **합계** | **~12시간** | **~15분** | **~98% ↓** |

---

### 7.3 Tessl Framework

**"AI Native Software Factory"** 를 표방하는 플랫폼. 핵심 개념은 **"의도 주도 개발(Intent-Driven Development)"** 입니다.

```
개발자의 의도(Intent)
        ↓
AI가 스펙 생성 → AI가 코드 생성 → AI가 테스트 생성 → AI가 배포 실행
                  ← 개발자는 각 단계의 결과를 검토하고 승인
```

---

### 7.4 Böckeler가 던진 핵심 비판적 질문들

#### 질문 1: "하나의 워크플로우로 모든 크기의 문제를 커버할 수 있는가?"

**현실적 대응**: 작업 규모에 따른 워크플로 분기가 필요합니다.

```
소규모 (< 30분):   자유 형식 지시 → 코드 수정 → 완료
중규모 (30분~1일): 간략한 spec → 구현
대규모 (1일 이상): 전체 SDD 워크플로 적용
```

#### 질문 2: "스펙 리뷰가 코드 리뷰보다 나은가?"

스펙 리뷰는 코드 리뷰를 **대체**하는 것이 아닙니다. **보완** 관계입니다.

```
스펙 리뷰가 잡아내는 것:  요구사항 누락, 설계 방향 오류, 논리 오류
코드 리뷰가 잡아내는 것:  구현 수준의 버그, 성능 문제, 컨벤션 위반
```

#### 질문 3: "통제의 착각은 아닌가?"

```
착각: 스펙 작성 → AI가 스펙대로 코드 생성 → 완성
현실: 스펙 작성 → AI가 스펙을 나름대로 해석 → 반드시 검증 필요
```

스펙은 **필요 조건**이지 **충분 조건**이 아닙니다. 수용 기준을 테스트 케이스로 변환하고, 테스트가 통과할 때만 "스펙이 충족되었다"고 판단해야 합니다.

#### 질문 4: "MDD(Model-Driven Development)의 실패를 반복하는 것은 아닌가?"

| 비교 항목 | MDD | SDD |
|---|---|---|
| 스펙 언어 | 형식적 모델링 언어(UML, DSL) | 자연어 + 구조화된 마크다운 |
| 코드 생성 | 규칙 기반 컴파일러 | 자연어 이해 기반 LLM |
| 모호함 처리 | 모호함 불허 (컴파일 오류) | 맥락을 추론하여 처리 |
| 사람의 참여 | 최소화 지향 | 검증 단계에 필수 참여 |

**SDD가 MDD처럼 실패하는 패턴:**
```
1. 스펙 작성
2. AI가 코드 생성 (스펙과 약간 다름)
3. 개발자가 코드 직접 수정 (스펙 업데이트 없이) ← 이 순간부터 실패
4. 스펙과 코드 괴리 발생
5. 스펙이 다시 무용지물이 됨
```

**방지 원칙**: "코드를 수정하기 전에 반드시 스펙을 먼저 수정한다."

---

## 8. 엔터프라이즈 레거시 전환에서의 SDD 적용

---

### 8.1 엔터프라이즈 환경에서 SDD가 중요한 이유

세 가지 흐름이 엔터프라이즈 팀에게 SDD를 필수 방법론으로 만들고 있습니다.

| 이점 | 설명 |
|---|---|
| **일관성** | 조직 표준과의 지속적이고 일관된 정합성 유지 |
| **감사 가능성** | 요구사항과 설계 결정에 대한 추적 및 감사 가능한 문서화 |
| **규정 준수** | 보안 정책 및 규정 준수 요건의 체계적 적용 |
| **팀 협업** | 새 팀원이 사양 문서만으로 시스템을 빠르게 파악 가능 |
| **AI 활용 극대화** | 명확한 사양이 AI 코드 생성 품질을 직접적으로 향상 |

---

### 8.2 왜 레거시 전환에 SDD가 필요한가?

```
레거시 시스템의 전형적인 상태:
  ├── 문서 없음 또는 현행화되지 않은 문서
  ├── 원래 개발자가 이직하여 맥락 상실
  ├── 코드만이 "진실"이나 코드도 이해하기 어려움
  ├── 변경 시마다 예상치 못한 버그 발생
  └── 새 기능 추가가 두려운 상태 ("무너질까봐")
```

SDD는 레거시 전환의 순서를 바꿉니다:

```
[기존 접근법 (코드 → 코드)]
레거시 코드 → AI가 분석 → 새 코드 생성
                ↑
        비즈니스 규칙 누락 위험이 매우 높음

[SDD 접근법 (코드 → 스펙 → 코드)]
레거시 코드 → 역분석하여 스펙 도출 → 스펙 검증 → 새 코드 생성
                ↑                          ↑
        비즈니스 규칙을 먼저          개발자가 스펙을 검토하여
        명시적으로 문서화             누락 여부 확인 가능
```

**비용 비교:**

| 구분 | SDD 없이 | SDD 적용 |
|---|---|---|
| 전환 기간 | 예상 6개월 → 실제 18~24개월 | 예상 대비 30~40% 단축 |
| 주요 원인/이점 | 예상치 못한 비즈니스 규칙 발견 반복 | 비즈니스 규칙을 전환 전에 모두 파악 |
| 스펙 도출 추가 비용 | 없음 | 2~4주 추가 |

---

### 8.3 기존 산출물에서 스펙으로: 핵심 정보 압축

```
기존 1,000페이지 요구사항 정의서의 실제 구성:

AI가 사용할 수 있는 정보 (약 5~10%):
  ├── 실제 비즈니스 규칙과 로직
  ├── 인터페이스 정의 (입력/출력)
  ├── 예외 처리 조건
  └── 비기능 요구사항 수치

AI가 사용할 수 없는 정보 (약 90~95%):
  ├── 회의록과 결정 배경 (맥락적 히스토리)
  ├── 현재 구현과 달라진 구식 설계 내용
  ├── 반복된 서술 (같은 규칙이 5번 등장)
  └── UML 다이어그램 (텍스트로 변환 불가)

→ 1,000페이지 → 핵심 50~80페이지로 압축
```

**기존 산출물에서 스펙 도출하는 5단계 프로세스:**

1단계 — 기존 문서 신뢰도 평가
```
코드 자체:                가장 신뢰도 높음 (현재 동작의 정확한 반영)
테스트 코드:              높음 (실제 검증된 동작 기록)
최근 릴리즈 노트:         중간
2~3년 이내 설계 문서:     중간 (일부 구식 내용 포함 가능)
5년 이상 된 요구사항 문서: 낮음 (대부분 구식)
```

2단계 — 코드 역분석으로 비즈니스 규칙 추출 (AI 활용)

3단계 — 중복 및 모순 제거

4단계 — 도메인 전문가와 검증 (비즈니스 이해관계자 참여)

5단계 — SDD 구조(Layer 0~3)로 정리

---

### 8.4 수백 명 규모 프로젝트에서의 SDD 운영

```
소규모 작업 (1~2시간):
  → spec 없이 자유 형식 지시 + constitution.md 참조

중간 규모 작업 (반나절):
  → 간략한 spec + plan + tasks

대규모 기능/아키텍처 변경:
  대상: 신규 기능, 아키텍처 변경, 서비스 신설
  스펙: 전체 SDD 워크플로 (spec + plan + tasks)
  리뷰: 스펙 리뷰 → 아키텍처 리뷰 → 코드 리뷰
```

---

### 8.5 점진적 전환 전략: Big Bang은 실패한다

```
Big Bang 전환의 실패 패턴:
  1. 전체 조직이 동시에 새 방법론 학습 시작
  2. 생산성이 즉시 30~50% 하락 (학습 곡선)
  3. 일정 압박으로 "이번 건 그냥 예전 방식으로"
  4. 일부 팀은 SDD, 일부는 기존 방식 혼용
  5. 혼용 상태가 고착화 → 사실상 실패

교훈: 방법론 전환은 기술 도입이 아니라 문화 변화입니다.
      문화 변화는 강제할 수 없고, 설득해야 합니다.
```

**성공하는 전환 전략 — 4단계:**

**① 20/80 규칙 적용**
```
전체 코드의 20%가 전체 수정의 80%를 차지한다.
→ 상위 20%의 고위험 기능에 먼저 SDD 적용
→ 이 20%에서 얻은 효과가 전체의 80%의 가치를 만듦
```

**② 파일럿으로 시작** (파일럿 팀 선정 기준: 3~6명 소규모, 신규 기능 예정 팀, 측정 의지 있는 팀 / 파일럿 기간: 8~12주)

**③ 병행운영**
```
Phase 1 (1~3개월): 파일럿 팀만 SDD, 나머지는 기존 방식
Phase 2 (3~6개월): 파일럿 성공 시 자원하는 팀에게 확대
Phase 3 (6~12개월): 조직 전체 SDD 적용 (또는 조직 맞춤 변형)
```

**④ 측정**
```
측정 지표:
  개발 품질:  프로덕션 버그 발생률, 스펙-코드 불일치 건수
  개발 속도:  기능 개발 Lead Time, AI 반복 대화 평균 횟수
  팀 만족도:  개발자 스펙 작성 만족도, 신규 팀원 온보딩 시간
```

---

# Part 4 — AI-Native 심화

---

## 9. SDD와 AI-Native 설계 패러다임의 연결

SDD는 현재 AI와 협업하기 위한 실용적인 방법론입니다. 하지만 더 크게 보면, SDD는 소프트웨어 개발이 **AI-Native 패러다임**으로 전환되는 과정에서의 중간 다리 역할을 합니다.

---

### 9.1 AI-Native 설계 패러다임이란?

"AI를 도구로 사용하는 개발"과 "AI-Native 개발"은 다릅니다.

```
[AI를 도구로 사용하는 개발]
  기존 프로세스: 기획 → 설계 → 코딩 → 테스트 → 배포
                                ↑
                     AI가 이 단계에서 보조
  결과: 개발 속도 향상. 프로세스 자체는 변화 없음.

[AI-Native 개발]
  AI가 프로세스 자체를 재구성:
  의도(Intent) → 스펙 → [AI가 코드/테스트/문서 생성] → 검증 → 배포
  결과: 개발 패러다임 자체가 변화.
        개발자의 역할이 "코더"에서 "명세 설계자"로 전환.
```

> **AI-Native**란 인공지능을 시스템의 **본질적이고 신뢰할 수 있는 구성 요소**로 삼아, 모든 아키텍처 레이어에 지능을 **1차 설계 원칙(First-Class Design Principle)** 으로 내재화한 소프트웨어 설계 패러다임이다.

| 측면 | 전통 소프트웨어 | AI-Native |
|---|---|---|
| **AI 통합 시점** | 개발 완료 후 추가 | 설계 원칙부터 핵심 |
| **시스템 행동** | 고정된 규칙 실행 | 지속적 학습 및 적응 |
| **인터페이스** | 전통적 GUI | NLP, 음성, 의도 인식 |
| **의사결정** | 프로그래밍된 로직 | 실시간 컨텍스트 지능 |

---

### 9.2 규칙기반 → 의도기반으로의 전환

AI-Native 설계의 출발점은 시스템 구조를 두 영역으로 명확히 분리하는 것입니다.

```python
# ❌ 기존 규칙기반 설계
if language == "ko":
    route_to(korean_handler)
elif language == "en":
    route_to(english_handler)
# 새 언어 추가 시마다 코드 수정 필요 → 유지보수 폭증

# ✅ AI-Native 의도기반 설계
spec = """
입력의 언어, 맥락, 의도를 해석하여 적절한 처리 경로를 반환하라.
출력 형식: {"language": "...", "intent": "...", "confidence": 0.0~1.0}
"""
result = llm.interpret(user_input, spec)
route_by_intent(result)
# 새 언어/의도 추가 시 명세(Spec)만 업데이트 → 코드 수정 불필요
```

---

### 9.3 SDD가 AI-Native를 가능하게 하는 방법

```
[일반 자연어 요청]
"로그인 기능을 만들어줘."
→ AI가 해석의 자유도가 너무 높아 예측 불가능한 결과

[SDD 스펙 기반 요청]
constitution.md: 기술 스택, 보안 원칙 (Memory Bank)
spec.md:         사용자 스토리, 수용 기준, 엣지 케이스 (What)
plan.md:         아키텍처, API 설계 (How)
tasks.md:        구현 순서, 의존 관계 (When/Who)

→ AI가 명확한 기준으로 코드 생성, 예측 가능한 결과  ✅
```

SDD가 제공하는 구조화된 스펙은 AI가 제약된 범위 내에서 정확하게 동작하도록 만드는 **가이드레일(Guardrail)** 역할을 합니다.

---

### 9.4 AI 능력과 스펙의 가치: 비례 관계

흥미로운 역설: **AI가 더 강력해질수록 스펙의 중요성은 줄어드는 것이 아니라 더 커집니다.**

```
2023년 AI (GPT-3.5 수준):
  → 스펙의 가치: 보통 (품질 낮아 인간이 대부분 수정)

2024~2025년 AI (GPT-4, Claude 3 수준):
  → 복잡한 기능도 구현 가능
  → 스펙의 가치: 중요

2026년 이후 AI (에이전틱 수준):
  → 독립적으로 전체 시스템을 구축 가능
  → 스펙의 가치: 결정적
```

> **비유**
> 자전거는 느리지만 방향이 틀려도 쉽게 돌아올 수 있습니다.
> 고속열차는 빠르지만, 방향이 틀리면 돌이키기 어렵습니다.
> AI가 강력해질수록 고속열차와 같아집니다. **스펙은 열차의 레일(Rail)입니다.**

---

### 9.5 SDD의 위치: 현재와 미래 사이의 다리

```
현재 (2026):   Human → Spec → AI → Code → Human Verification
가까운 미래:   Human → Spec → AI → Code → AI Verification → Human Approval
먼 미래:       Human → Intent → AI → Spec → AI → Code → AI Verification
```

```
AI 발전에 따라 변하는 것:
  ├── AI가 담당하는 작업의 범위 (점점 확대)
  ├── 스펙의 자동화 수준 (AI가 스펙 초안을 생성)
  └── 검증의 주체 (인간 → AI 보조 → AI 주도)

변하지 않는 것:
  ├── 스펙이 AI 협업의 핵심 인터페이스라는 사실
  ├── 인간이 의도(Intent)와 가치(Value)를 정의해야 한다는 사실
  └── 검증과 최종 승인에 인간의 판단이 필요하다는 사실
```

---

## 10. AI-Native 아키텍처 심화: 6계층 레퍼런스 모델

---

### 10.1 뉴로-심볼릭 아키텍처 (Neuro-Symbolic Architecture)

AI-Native 설계의 가장 중요한 구조적 원칙. 시스템을 **두 계층**으로 명확히 분리합니다.

```
┌─────────────────────────┬─────────────────────────────────┐
│   NEURO (확률적 영역)   │    SYMBOLIC (결정적 영역)        │
│  ─────────────────────  │  ──────────────────────────      │
│  · 의도 해석            │  · 결제/트랜잭션 처리            │
│  · 자연어 처리 (NLP)    │  · DB 처리                       │
│  · 분류/변환            │  · 핵심 비즈니스 로직            │
│  · 패턴 인식            │  · 규정 준수 검증                │
│                         │                                  │
│  담당: AI (LLM)         │  담당: 기존 결정적 시스템        │
│  특성: 확률적           │  특성: 결정론적                  │
└─────────────────────────┴─────────────────────────────────┘

          핵심 원칙: "AI는 판단하고, 시스템은 실행한다."
```

> **왜 이 분리가 중요한가?**
>
> LLM은 본질적으로 확률 모델입니다. 99.9%의 정확도라도, 하루 100만 건의 트랜잭션을 처리하면 **1,000건의 오류**가 발생합니다. 과금 시스템에서 1,000건의 오류는 재앙입니다.

```
❌ 잘못된 설계:
   사용자 입력 → LLM → 직접 결제 처리
   (LLM의 확률적 오류가 핵심 비즈니스 로직에 침투)

✅ 올바른 뉴로-심볼릭 설계:
   사용자 입력
       ↓
   LLM: "이것은 국제 통화 요청이다"  (판단, 확률적)
       ↓
   [Confidence Score 검증]
   결정적 엔진: 환율 계산 + 송금 처리  (실행, 결정론적)  ✅
```

---

### 10.2 AI-Native 6계층 레퍼런스 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1: AI-Powered Experience Layer (사용자 접점)          │
│    · 자연어 UI / 음성 인터페이스                             │
│    · 실시간 개인화 엔진                                      │
│    · 예측적 UX (사용자 행동 예측)                            │
├─────────────────────────────────────────────────────────────┤
│  Layer 2: AI Intelligence & Decision Layer (의사결정 핵심)   │
│    · LLM 오케스트레이션 및 시맨틱 라우팅                     │
│    · AI 에이전트 프레임워크 (다단계 추론)                    │
│    · 지식 그래프 엔진 (시맨틱 이해)                          │
│    · 실시간 ML 파이프라인 (지속적 모델 업데이트)             │
├─────────────────────────────────────────────────────────────┤
│  Layer 3: Intelligent Application Layer (자가 최적화 서비스) │
│    · 적응형 마이크로서비스                                   │
│    · 지능형 워크플로우 (자가 최적화)                         │
│    · AI 강화 API 게이트웨이                                  │
├─────────────────────────────────────────────────────────────┤
│  Layer 4: AI-Enhanced Data Platform (지능형 데이터 관리)     │
│    · 벡터 데이터베이스 (시맨틱 검색)                         │
│    · Feature Store (ML 피처 중앙 관리)                       │
│    · RAG (검색 증강 생성) 파이프라인                         │
│    · 모델 레지스트리 (버전 관리 + 거버넌스)                  │
├─────────────────────────────────────────────────────────────┤
│  Layer 5: Autonomous Infrastructure Layer (자율 인프라)      │
│    · GPU/TPU 클러스터 (지능형 워크로드 스케줄링)             │
│    · 예측적 오토스케일링 (AI 기반 리소스 예측)               │
│    · 자가 치유 시스템 (장애 자동 감지·복구)                  │
├─────────────────────────────────────────────────────────────┤
│  Layer 6: AI Governance & Security (전 계층 교차 적용)       │
│    · AI 윤리 프레임워크 (편향 탐지, 공정성 모니터링)         │
│    · 모델 설명 가능성 플랫폼 (XAI)                           │
│    · 지능형 보안 (AI 기반 위협 탐지)                         │
│    · MLOps 플랫폼 (CI/CD for AI)                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 10.3 AI-Native Reference Flow (통합 흐름)

```
[요청 수신]
    ↓
Input Security Layer
  · PII(개인식별정보) 마스킹
  · 프롬프트 인젝션 방어
  · 악의적 입력 필터링
    ↓
LLM Layer
  · 시맨틱 라우팅 (의도 분류)
  · Confidence Score 산출
  · 구조화된 JSON 출력 반환
    ↓
Validation Layer
  · Confidence 분기 처리:
      ≥ 0.95          → 자동 처리
      0.7 ~ 0.95      → 경량 검증 후 처리
      < 0.7           → HITL 에스컬레이션
    ↓
Execution Layer
  · 결정적 비즈니스 로직 실행 (AI 개입 없음)
    ↓
Monitoring Layer
  · Observability 기록
  · 이상 탐지 및 자동 알림
  · 프로덕션 데이터 → 명세 자동 피드백
```

---

## 11. AI 제어 계층과 가드레일 설계

> **핵심 전제**: "AI가 틀릴 수 있다"는 사실을 **아키텍처 수준에서 내재화**해야 한다.

---

### 11.1 정형 출력 계약 (Structured Output Contract)

LLM의 출력을 **자유 형식 텍스트가 아닌 JSON 스키마**로 강제합니다.

```json
{
  "intent": "code_conversion",
  "language": "java",
  "confidence": 0.92,
  "parameters": {
    "source_language": "c",
    "target_framework": "spring-boot"
  },
  "needs_clarification": false,
  "escalate_to_human": false
}
```

**장점**: 레거시 시스템과 안정적 연동, 파이프라인 자동화 용이, 예측 가능한 시스템 동작 확보

---

### 11.2 신뢰도 임계값 기반 분기 (Confidence Threshold)

```
┌─────────────────────────────────────────────────────┐
│  Confidence ≥ 0.95   → 자동 처리 (추가 검증 불필요) │
│  0.7 ≤ C < 0.95      → 경량 검증 후 처리            │
│  Confidence < 0.7    → HITL 에스컬레이션             │
└─────────────────────────────────────────────────────┘

실제 운영 환경 추정치:
  전체 요청의 60~70%  → 높은 신뢰도로 자동 처리
  나머지  30~40%      → 추가 검증 또는 고성능 모델 처리

효과: AI 운영 비용 40~60% 절감 + 응답 속도 향상
```

---

### 11.3 HITL 아키텍처 (Human-In-The-Loop)

AI 판단의 불확실성을 사람이 개입하는 구조로 흡수합니다.

```
HITL 설계 4대 원칙:

① 우아한 퇴보 (Graceful Degradation)
   AI 실패 시 → 사람이 대체하는 경로 항상 확보

② 시간 초과 대체 (Timeout Fallback)
   "10분 내 미검토 시 → 기본값 처리 or 대기열 이동"

③ 감사 추적 (Audit Trail)
   AI 판단 여부 + 사람 개입 여부 + 결과 → 반드시 로깅
   → 컴플라이언스 감사 대응 근거

④ 에스컬레이션 경로 (Escalation Path)
   동일 유형 반복 실패 → 자동 에스컬레이션
```

---

### 11.4 AI 출력 검증 4가지 전략

| 전략 | 설명 | 활용 시점 |
|---|---|---|
| **① 동작 동등성 테스트** | 기존 vs AI 시스템에 동일 입력, 비즈니스 허용 범위 내 차이 판단 | 마이그레이션 초기 |
| **② 섀도우 모드** | AI+기존 로직 병렬 실행, 실제 반영은 기존 로직만 (AI는 로깅만) | 신뢰 확보 단계 |
| **③ AI 회귀 테스트** | 골든 데이터셋으로 프롬프트/모델 변경 시 정확도 측정 | 지속적 운영 |
| **④ 적대적 테스트** | 엣지 케이스, 모호한 입력, 악의적 입력으로 취약점 발견 | 보안 검증 |

---

## 12. Agent-Oriented 소프트웨어 설계

---

### 12.1 Human Interface → Agent Interface

| 전통 소프트웨어 설계 원칙 | AI-Native 소프트웨어 설계 원칙 |
|---|---|
| **사용성** — 사람이 이해하기 쉬운 UI | **기계 해석 가능성** — AI가 이해하기 쉬운 구조 |
| **시각적 명확성** — 사람이 보기 좋은 레이아웃 | **구성 가능성** — AI 에이전트가 동적으로 조합 가능 |
| **인지 부하 감소** — 사람의 기억 부담 최소화 | **호출 신뢰성** — AI가 안정적으로 실행 가능 |

---

### 12.2 Invocable Capability — AI-Native 소프트웨어의 기본 단위

```
전통 소프트웨어의 기본 단위: 사용자 기능 (Feature/Page)
  → 사람이 클릭하고 네비게이션하는 단위

AI-Native 소프트웨어의 기본 단위: 호출 가능한 역량 (Invocable Capability)
  → AI 에이전트가 명세에 따라 직접 호출하는 단위

Invocable Capability 설계 원칙:
  1. 명확한 입력/출력 스키마  (AI가 파라미터를 추론하지 않아도 됨)
  2. 원자적 실행              (한 번 호출로 완결)
  3. 멱등성 보장              (동일 호출 반복 시 같은 결과)
  4. 자기 서술적              (capability 자체가 사용법을 설명)
```

**모놀리식 → 역량 기반 시스템 전환 예시:**

```
기존:  "주문 관리 페이지"  (모놀리식 UI 기능)

변환:
  · create_order(spec)                   → OrderID
  · get_order_status(orderId)            → OrderStatus
  · cancel_order(orderId, reason)        → CancellationResult
  · update_order_shipping(orderId, addr) → UpdateResult
  (각각 독립적으로 AI 에이전트가 호출 가능)
```

---

### 12.3 Multi-Agent 협업 패턴

```
Orchestrator-Worker 패턴 (Anthropic 제안):

  ┌─────────────────────┐
  │   Orchestrator      │  ← 전체 명세를 받아 계획 수립
  │      Agent          │    작업을 서브 에이전트에 분배
  └──────────┬──────────┘
             │
      ┌──────┼──────┐
      ↓      ↓      ↓
  [Worker] [Worker] [Worker]
  DB처리    API구현  테스트작성

성능 효과:  단일 에이전트 대비 90.2% 성능 향상 (Anthropic 측정치)
SDD와 연결: tasks.md의 [P] 태그 → 병렬 Worker 에이전트 할당
```

---

### 12.4 멀티 에이전트 시스템에서의 스펙

```
[멀티 에이전트 시스템 예시]

스펙 → 에이전트 A: 백엔드 코드 생성
      ↓ (공유 스펙 참조)
      에이전트 B: 프론트엔드 코드 생성
      ↓ (동일 수용 기준 참조)
      에이전트 C: 테스트 코드 생성
      ↓ (동일 API 계약 참조)
      에이전트 D: API 문서 생성

→ 여러 에이전트가 동일한 스펙을 참조하기 때문에
  각자 생성한 결과물이 서로 호환됨  ✅

스펙이 없는 멀티 에이전트 시스템:
  → 에이전트마다 다른 해석으로 서로 충돌하는 결과물 생성  ❌
```

---

# Part 5 — 주의사항과 미래

---

## 13. Anti-Pattern: 피해야 할 것들

---

### ❌ Anti-Pattern 1. AI를 핵심 비즈니스 로직에 직접 연결

```
❌ 잘못된 설계:
   사용자 주문 → LLM이 직접 결제 처리
   이유: LLM은 확률적. 1%의 오류 = 하루 100만 건 중 1,000건 실패

✅ 올바른 설계:
   사용자 주문 → LLM이 "이건 일반 주문" 판단 (확률적)
               → 결정적 엔진이 결제 처리 (결정론적)
```

---

### ❌ Anti-Pattern 2. 구조 없는 원시 AI 생성 (Vibe Coding)

```
❌ 잘못된 접근:
   "인벤토리 만들어줘" → AI 코드 생성
   "이번엔 상점 만들어줘" → AI 코드 생성 (인벤토리와 불일치)
   → Context Rot 발생, 전체가 엉망

✅ 올바른 접근:
   Constitution + Spec + Plan → AI 코드 생성
   = 항상 동일한 맥락에서 일관된 코드 생성
```

---

### ❌ Anti-Pattern 3. 명세 없는 AI 코드 병합

```
❌ 잘못된 방식:
   AI가 생성한 코드를 검증 없이 바로 Merge
   → 헌법 원칙 위반, 아키텍처 불일치, 보안 취약점 포함 가능

✅ 올바른 방식:
   AI 코드 생성
       → Gate 검증 (원칙 준수 확인)
       → 골든 데이터셋 회귀 테스트
       → 승인 후 Merge
```

---

### ❌ Anti-Pattern 4. 명세 유지 없이 코드만 수정 (Spec Drift)

```
❌ 잘못된 방식:
   기능 변경 시 → 코드만 수정
   → 명세와 코드의 괴리 발생 (Spec Drift)
   → 다음 AI 생성 시 명세 기반으로 이전 상태 복원

✅ 올바른 방식:
   기능 변경 시 → 명세 먼저 수정 → AI가 코드 재생성
```

---

### ❌ Anti-Pattern 5. Confidence Score 없는 AI 판단 자동화

```
❌ 잘못된 방식:
   AI 판단 결과를 Confidence 검증 없이 자동 처리
   → 불확실한 판단이 핵심 로직에 그대로 반영

✅ 올바른 방식:
   AI 판단 → Confidence Score 확인
           → 0.95 이상만 자동 처리
           → 미만은 HITL 에스컬레이션
```

---

## 14. 미래 전망: AI-Native SDLC

---

### 14.1 V-Bounce 모델 — AI-Native 개발 생명주기

```
전통적 V-Model (선형):
  요구사항 → 설계 → 구현 → 단위테스트 → 통합테스트 → 배포
  (각 단계 수동 처리, 구현 단계에 시간이 집중됨)

AI-Native V-Bounce 모델:
  명세 작성 ─────────────────────────── 시스템 검증
      ↓  AI 명세 검증·보완                     ↑
    아키텍처 설계 ─────────── 통합 검증        │
         ↓  AI 설계 패턴 제안       ↑          │
           구현 계획 ─── 컴포넌트 검증         │
                ↓  AI 코드 생성  ↑            │
                 ┌── 코드 (AI 생성) ──┐        │
                 └───────────────────┘        │
  프로덕션 피드백 ──────────────────────────────┘
  (명세 자동 갱신)
```

**V-Bounce의 핵심 변화:**
- 구현 단계: AI가 코드 생성으로 90% 이상 자동화
- 인간의 집중 영역: 구현 → 요구사항·명세·검증으로 이동
- 프로덕션 피드백이 명세를 직접 갱신하는 루프 형성

---

### 14.2 Gartner 2026 예측

Gartner는 2026년을 AI-Native 개발이 경쟁 우위에서 **생존 필수 요건**으로 전환되는 결정적 시점으로 규정합니다.

| 예측 항목 | 수치 |
|---|---|
| 엔터프라이즈 앱 중 AI 에이전트 통합 비율 | 40% (2025년 5% 미만에서 급증) |
| 기업 중 네트워크 활동 50% 이상 자동화 비율 | 30% |
| AI-Native 아키텍처 성능 향상 (레이턴시/처리량) | 2~5배 |
| 복잡한 AI 워크로드 성능 향상 | 90% 이상 |

---

### 14.3 SDD와 TDD의 역사적 유사성

```
TDD의 교훈:
  → 모든 팀이 TDD를 하는가?  아니다.
  → 테스트를 안 짜는 팀이 있는가?  거의 없다.
  → TDD는 방법론이지만, "테스트"는 업계 표준이 됨.

SDD의 미래 예측:
  → AI가 발전하면서 단계가 줄어들 수 있다
  → 그러나 "Spec"이라는 산출물은 살아남을 것이다
  → 이유: AI가 아무리 발전해도, 맥락(Context) 없이는
           좋은 코드를 짤 수 없다.
```

| 방법론 | 핵심 산출물 | 미래 전망 |
|---|---|---|
| **TDD** | Test | SDD를 안 해도 테스트는 짠다 (업계 표준) |
| **SDD** | Spec | SDD를 안 해도 Spec은 쓰게 될 것이다 |

---

### 14.4 개발자 역할의 변화

```
Before AI-Native:
  개발자 = 코드 작성자
  주요 스킬: 프로그래밍 언어, 알고리즘, 디버깅

After AI-Native (SDD 시대):
  개발자 = 명세 설계자 + 맥락 엔지니어 + AI 검증자
  주요 스킬:
    · 비즈니스 요구사항을 명확한 명세로 변환하는 능력
    · AI 출력을 검증하고 품질을 보장하는 능력
    · 뉴로-심볼릭 경계를 설계하는 아키텍처 능력
    · Context Engineering — 구조화된 맥락 설계 능력
    · AI 에이전트 오케스트레이션 능력
```

> **🔑 최종 메시지**
>
> AI가 코드를 생성하는 시대에서, 개발자의 가장 중요한 역량은 **"AI가 이해할 수 있는 언어로 의도를 정확하게 표현하는 능력"** 입니다.
>
> SDD는 그 언어를 배우고 실천하는 방법론입니다.

---

# 부록

---

## 15. 정리 및 핵심 교훈

```
바이브 코딩        AI 코딩의 현실        SDD           에이전틱 엔지니어링   AI-Native
(2025년 초)  →   (한계 발견)      →  (해결책)  →   (실천 프레임워크)  →  (미래)
"일단 만들자"     "왜 안 되지?"        "구조가            "잘 만들자"        "스펙이
                                       필요하다"                             소프트웨어다"
```

**AI 코딩의 현실에서 배운 것:**
- 자유 형식 지시는 AI를 혼란스럽게 만든다
- 구조 없는 많은 정보도 AI에게 도움이 되지 않는다
- 진짜 문제는 커뮤니케이션 구조의 부재다

**SDD 이론에서 배운 것:**
- 사양이 코드의 소스이자 진실의 근원
- Level 1(Spec-First) → Level 2(Spec-Anchored) → Level 3(Spec-as-Source)
- 스펙은 "무엇을", Memory Bank는 "어떻게" — 두 가지가 함께 완전한 맥락 제공

**Less is More 설계에서 배운 것:**
- 계층적 스펙 구조: Layer 0(헌법) → Layer 1(도메인) → Layer 2(기능) → Layer 3(작업)
- 좋은 스펙: 측정 가능하고, 테스트 가능하고, 결과 중심적이다
- 나쁜 스펙: 모호하고, 구현을 지시하고, 검증 불가능하다

**엔터프라이즈 레거시 전환에서 배운 것:**
- 코드 전환 전에 스펙을 먼저 도출한다
- 기존 문서의 5~10%만이 AI가 실제 사용할 수 있는 핵심 정보다
- Big Bang 전환은 실패한다 → 20/80 규칙 + 파일럿 + 병행운영 + 측정

**AI-Native 패러다임에서 배운 것:**
- AI가 강력해질수록 스펙의 가치는 더 커진다
- 뉴로-심볼릭 경계: AI는 판단, 결정적 시스템은 실행
- 스펙은 멀티 에이전트 시스템의 공통 인터페이스다

---

## 16. 빠른 참조 카드

### ⚡ SDD 단계별 체크리스트

**명세 작성 전:**
- [ ] Constitution(헌법)이 정의되어 있는가?
- [ ] Feature-ID가 부여되었는가?
- [ ] What(무엇을)과 Why(왜)가 분리되어 있는가?

**명세 작성 중 (Specify):**
- [ ] `[NEEDS CLARIFICATION]` 태그가 모두 해소되었는가?
- [ ] Acceptance Criteria가 측정 가능한 형태인가?
- [ ] How(어떻게)가 Spec 단계에 섞여 있지 않은가?

**Plan 단계:**
- [ ] 기술 선택 근거가 명시되었는가?
- [ ] 데이터 모델이 정의되었는가?
- [ ] API 계약이 문서화되었는가?
- [ ] GATE 검증을 통과했는가?

**Task 단계:**
- [ ] `[P]` 병렬 실행 가능 작업이 표시되었는가?
- [ ] 선행 작업 의존성이 명시되었는가?

**구현 후:**
- [ ] 골든 데이터셋 회귀 테스트 통과?
- [ ] 헌법 원칙 준수 확인?
- [ ] Confidence Score 임계값 설정 완료?

---

### 🚦 Confidence Score 빠른 가이드

| 범위 | 처리 방법 | 예상 비율 |
|---|---|---|
| `≥ 0.95` | 자동 처리 | 전체의 60~70% |
| `0.70 ~ 0.95` | 경량 검증 후 처리 | 전체의 20~30% |
| `< 0.70` | HITL 에스컬레이션 | 전체의 10% |

---

### 📚 핵심 용어 사전

| 용어 | 설명 |
|---|---|
| **SDD** | Specification-Driven Development. 명세를 1차 산출물로 삼는 개발 방법론 |
| **SSoT** | Single Source of Truth. 명세가 유일한 진실의 원천 |
| **Vibe Coding** | 명세 없이 즉흥적 프롬프트로 코드를 생성하는 방식 |
| **Context Rot** | 작업 전환 시 AI의 맥락이 증발하여 코드 일관성이 깨지는 현상 |
| **Memory Bank** | 프로젝트 전체에 걸쳐 일관되게 유지되는 맥락 정보 (constitution.md 등) |
| **Neuro-Symbolic** | 확률적 AI 판단 + 결정적 시스템 실행의 이중 구조 |
| **HITL** | Human-In-The-Loop. AI 불확실성 구간에 사람이 개입하는 구조 |
| **Spec Drift** | 코드가 변경되었지만 명세가 업데이트되지 않아 발생하는 불일치 |
| **Invocable Capability** | AI 에이전트가 직접 호출할 수 있는 원자적 기능 단위 |
| **Golden Dataset** | AI 출력 회귀 테스트에 사용하는 정답 데이터셋 |
| **V-Bounce Model** | AI-Native SDLC에서 양방향 피드백이 존재하는 개발 생명주기 모델 |
| **Shadow Mode** | AI와 기존 로직을 병렬 실행하되 실제 반영은 기존 로직만 하는 검증 방식 |
| **Agentic Engineering** | AI 에이전트를 조율하고 감독하는 전문 개발 방식 |

---

### 🎯 핵심 공식

```
AI에게 코드를 잘 짜게 하려면,
코드가 아니라 명세를 잘 짜야 한다.

전통 개발:        Code → Docs
TDD:              Test → Code
SDD + AI-Native:  Spec → Code (AI 생성)
```

---

## 17. 강의 토론 질문 및 실습 과제

---

### 💬 토론 질문

**토론 1 — 개념 이해**
> 여러분이 최근에 AI에게 코드 생성을 요청했던 경험을 떠올려보세요. 그 요청이 Vibe Coding이었다면, SDD 방식으로 어떻게 바꿀 수 있었을까요?

**토론 2 — 비판적 사고**
> Böckeler는 "통제의 착각"이라는 비판을 제기했습니다. 스펙을 완벽하게 작성해도 AI가 스펙대로 코드를 생성하지 않을 수 있습니다. 이 문제를 어떻게 해결할 수 있을까요?

**토론 3 — 미래 전망**
> AI가 점점 더 강력해질수록 스펙의 중요성이 더 커진다는 주장에 동의하십니까? AI가 스스로 스펙을 작성할 수 있는 날이 온다면, 개발자의 역할은 어떻게 변할까요?

**토론 4 — 조직 변화**
> Big Bang 전환이 실패하는 이유와 점진적 전략이 필요한 이유를 학교 수업 방식 변화에 빗대어 설명해 보세요.

**토론 5 — 윤리**
> AI-Native 시스템에서 AI가 내린 판단이 잘못되어 피해가 발생했을 때, 책임은 누구에게 있을까요? Confidence Score와 HITL은 이 문제를 어떻게 완화할 수 있을까요?

---

### 📝 실습 과제

**실습 1 — Bad Spec → Good Spec 변환 (초급)**

다음 나쁜 스펙을 좋은 스펙으로 변환하세요.

```
Bad Spec:
  "사용자가 로그인할 수 있어야 한다.
   보안이 강화되어야 하고, 빠르게 동작해야 한다."
```

요구 사항: Given-When-Then 형식으로, 측정 가능한 기준을 포함하여 작성하세요.

---

**실습 2 — constitution.md 작성 (중급)**

여러분의 팀 프로젝트(또는 가상 프로젝트)에 대한 `constitution.md`를 작성하세요. Layer 0 기준(50~80줄)을 지켜 다음을 포함해야 합니다:
- 기술 스택 및 버전
- 보안 원칙 최소 3가지
- 코딩 컨벤션 최소 3가지
- AI 거버넌스 원칙 최소 2가지

---

**실습 3 — SDD 4단계 워크플로 적용 (중급)**

"도서관 도서 대출 시스템"의 `도서 반납 기능`에 대해 SDD 4단계를 작성하세요.
- spec.md (Specify): 사용자 스토리, 수용 기준, 요구사항, 엣지 케이스
- plan.md (Plan): 기술 선택, 데이터 모델, API 계약
- tasks.md (Task): 병렬 가능 작업 포함

---

**실습 4 — Neuro-Symbolic 아키텍처 설계 (고급)**

"의료 AI 진단 보조 시스템"을 설계할 때, 뉴로-심볼릭 아키텍처를 적용하여:
1. NEURO 영역(AI 담당)과 SYMBOLIC 영역(결정적 시스템 담당)을 구분하세요.
2. Confidence Threshold를 어떻게 설정할지, 그 근거를 설명하세요.
3. HITL이 필요한 시나리오를 3가지 이상 제시하세요.

---

**실습 5 — 레거시 전환 계획 수립 (고급)**

여러분의 학교 수강신청 시스템이 레거시 시스템이라고 가정합니다. SDD 방식으로 전환 계획을 수립하세요:
1. 기존 시스템 문서의 신뢰도 평가 방법
2. 핵심 비즈니스 규칙 5가지 추출
3. 점진적 전환 전략 (Phase 1~3) 수립
4. 측정 지표 3가지 선정 및 근거

---

### 📊 자기 평가 루브릭

| 역량 | 입문 (1) | 기초 (2) | 실무 (3) | 숙련 (4) |
|---|---|---|---|---|
| **스펙 작성** | 모호한 요구사항 나열 | Given-When-Then 형식 사용 | 측정 가능한 기준 포함 | 엣지 케이스, 비기능 요구사항 완비 |
| **SDD 레벨 판단** | 레벨 구분 불가 | 3가지 레벨 설명 가능 | 상황에 맞는 레벨 선택 | 팀 역량에 따른 단계적 도입 계획 수립 |
| **아키텍처 설계** | AI 직접 연결 설계 | NEURO/SYMBOLIC 구분 | Confidence Threshold 설정 | HITL + 감사 추적 포함한 완전한 설계 |
| **Anti-Pattern 인식** | 인식 불가 | 5가지 패턴 암기 | 실제 코드에서 패턴 발견 | 예방 설계 가능 |

---

*최종 업데이트: 2026-05-31*

**참고 문헌:**
- Birgitta Böckeler, "Specification-Driven Development", martinfowler.com
- Andrej Karpathy, "Vibe Coding" and "Agentic Engineering", X(Twitter)
- arXiv, "Spec-Driven Development: From Code to Contract in the Age of AI" (2025)
- arXiv, "The AI-Native Software Development Lifecycle" (2024)
- arXiv, "From Human Interfaces to Agent Interfaces" (2025)
- AWS Kiro 공식 문서
- GitHub Spec Kit 공식 문서
- Tessl.io 공식 문서
- Indium Tech AI-Native Reference Architecture
- Gartner AI-Native Predictions 2026

---

*※ 본 문서는 강의용으로 작성되었습니다. 내용 관련 문의는 강의 담당자에게 연락하세요.*
